## Modification History

| Date       | Summary of Changes |
|------------|--------------------|
| 2026-07-15 | Recorded Checkpoint 78 final independent corrected-v3 APPROVE and WATCH reconciliation |
| 2026-07-15 | Recorded Checkpoint 76 for formatter-consistent corrected-v3 completion, strict merge, and audit reconciliation |
| 2026-07-15 | Recorded Checkpoint 75 for GB300 completion, corrected-v2 merge, strict resume, and semantic verification |
| 2026-07-15 | Recorded corrected-v2 blocker remediation, completed-shard audit, and final native code-review APPROVE |
| 2026-07-14 | Recorded Checkpoint 70 corrected final completion verification |
| 2026-07-14 | Recorded Checkpoint 69 final independent completion APPROVE and WATCH reconciliation |
| 2026-07-14 | Recorded Checkpoint 68 for the corrected final static gate |
| 2026-07-14 | Recorded Checkpoints 66-67 for full merge, strict resume, and merged semantic validation |
| 2026-07-10 | Created review checkpoint log for Step4 predefined ops planning |
| 2026-07-10 | Recorded first grilling decision checkpoint |
| 2026-07-12 | Recorded independent critic BLOCK after Step4Air YAML review |
| 2026-07-12 | Recorded user adjudication resolving dense-gating part of BLOCK |
| 2026-07-12 | Recorded user adjudication resolving shared-expert precision part of BLOCK |
| 2026-07-13 | Recorded normalized checkpoint nextn and comparison-matrix decision |
| 2026-07-13 | Recorded independent Claude APPROVE verdict for conditional attention source precedence |
| 2026-07-13 | Recorded user-directed temporary MLA substitution and resulting scope gap |
| 2026-07-13 | Recorded all-92-layer MLA scope adjudication |
| 2026-07-13 | Recorded independent WATCH review of candidate MLA geometry |
| 2026-07-13 | Recorded DeepSeek-V3 generic MLA geometry adjudication |
| 2026-07-13 | Recorded local Step4 model identity adjudication |
| 2026-07-13 | Recorded test-side simulated H800 source adjudication |
| 2026-07-13 | Recorded binary ISL matrix adjudication |
| 2026-07-13 | Recorded primary OSL and independent decode-smoke adjudication |
| 2026-07-13 | Recorded `step-design` implementation-branch adjudication |
| 2026-07-13 | Recorded explicit `prefix=0` workload adjudication |
| 2026-07-13 | Recorded both-mode `agg`/`disagg` validation adjudication |
| 2026-07-13 | Recorded referenced TTFT-only SLA and ranking adjudication |
| 2026-07-13 | Recorded chunked-prefill mode-asymmetry root-cause checkpoint |
| 2026-07-13 | Recorded aligned chunked-prefill-disabled adjudication |
| 2026-07-13 | Recorded Step4 uneven-EP feasibility and SOL-fidelity checkpoint |
| 2026-07-13 | Recorded common 21-config parallel and exhaustive batch-cap adjudication |
| 2026-07-13 | Recorded final independent Claude APPROVE verdict and low-risk findings |
| 2026-07-13 | Recorded search-space parity repair and independent roofline-correction APPROVE verdict |
| 2026-07-13 | Recorded Phase 0 implementation-worktree and clean-baseline checkpoint |
| 2026-07-13 | Recorded fresh source-backed search-space parity revalidation and independent Claude APPROVE verdict |
| 2026-07-13 | Recorded independent APPROVE for the vLLM `MoEDispatch.reduce_results` root-cause fix |
| 2026-07-13 | Recorded recursive Step4 strict-SOL integration gate |
| 2026-07-13 | Recorded independent APPROVE and narrowed scope for native DeepSeek-V4-Pro on vLLM Hopper |
| 2026-07-13 | Recorded independent APPROVE for deterministic ranking and comparison-delta semantics |
| 2026-07-13 | Recorded independent WATCH review and verification of typed single-point model OOM |
| 2026-07-13 | Recorded conditional disaggregate cluster-ranking architecture review |
| 2026-07-13 | Recorded fixed-cluster solver and comparison-runner implementation gate |
| 2026-07-13 | Recorded independent APPROVE for workload-specific Prefill-throughput ranking |
| 2026-07-13 | Recorded independent code-review APPROVE and integration-test remediation |
| 2026-07-14 | Recorded independent ISSUE-048 semantic-contract APPROVE and remaining-finding test audit |
| 2026-07-13 | Recorded compact checkpoint serialization and fail-fast realization gate |
| 2026-07-13 | Recorded CLI/artifact, real smoke, and strict shard-merge realization gate |
| 2026-07-13 | Recorded independent APPROVE for final current-plan search-space parity audit |
| 2026-07-13 | Recorded independent BLOCK on checkpoint integrity before the full 480-run matrix |
| 2026-07-13 | Recorded cross-model APPROVE for the ISSUE-046 transactional checkpoint design |
| 2026-07-14 | Recorded the three-turn ISSUE-046 adjudication impasse and goal-level blocked transition |
| 2026-07-14 | Recorded explicit user APPROVE adjudication and reopened the ISSUE-046 TDD implementation gate |
| 2026-07-14 | Recorded independent BLOCK on Step4 aggregate mixed-step attention semantics and stale aggregate artifacts |
| 2026-07-14 | Recorded explicit user APPROVE adjudication for ISSUE-048 root-cause remediation |
| 2026-07-14 | Recorded independent post-ISSUE-046 code-review BLOCK and user option `1` remediation approval |
| 2026-07-14 | Recorded native architecture refinement of the ISSUE-048 semantic contract without changing the approved search space |
| 2026-07-14 | Recorded focused ISSUE-048 through ISSUE-052 remediation evidence before broader regression and new independent review |
| 2026-07-14 | Recorded Gate 6/7 strict-TDD evidence and Gate 8/9/10 read-only audit verdicts |
| 2026-07-14 | Recorded independent StepCode Claude APPROVE for the Gate 8 query-provenance architecture |
| 2026-07-14 | Recorded execution-worktree search-space parity repair and zero-row-delta evidence |
| 2026-07-14 | Recorded final Gate 8 `CONTEXT_CAPTURE` adjudication and repaired checkpoint numbering |
| 2026-07-14 | Recorded Gate 8-10 implementation/focused verification checkpoint before independent post-change review |
| 2026-07-14 | Recorded ISSUE-059 TPOT zero-baseline independent APPROVE adjudication and H200 v2 failure evidence |
| 2026-07-14 | Recorded ISSUE-059 strict TDD and post-format verification checkpoint |
| 2026-07-14 | Recorded fresh H200 primary/decode, strict-resume, and real-shard representative checkpoint |
| 2026-07-14 | Recorded final independent post-change APPROVE verdict and non-blocking WATCH observations |
| 2026-07-14 | Recorded Gate 13 post-change full-unit PASS and Gate 14 release checkpoint |
| 2026-07-14 | Recorded independent APPROVE for shared full-contract system-shard execution and strict merge |

# Review: Step4 Predefined Ops Planning

## Checkpoint 1: Initial planning artifact review

| Field | Record |
|---|---|
| Target Component/Phase | Plan/discussion docs for Step4 predefined ops |
| Reviewer Agent Identity | Primary Codex planning pass |
| Inspected Artifacts | User request; `README.md`; `AGENTS.md`; architecture CSV; Step3.7 HuggingFace config; DS-V4-Pro config; `common.py`; `utils.py`; `models/base.py`; `models/moe.py`; `models/qwen35.py`; `models/deepseek_v4.py`; `operations/dsv4.py`; `operations/attention.py` |
| Identified Issues/Anomalies | Branch mismatch (`stepfun-design` requested, `step-design` active); pre-existing dirty workspace; Step4 non-full attention ambiguous; dense layer placement ambiguous; nextn/MTP ambiguous; model ID/architecture naming ambiguous; H800 system source test-side only; `iso` likely means `isl` but not confirmed |
| Remediation/Verification Code Actions Taken | No business code actions taken. Created docs to capture requirements, notes, issues, and draft plan. First grilling question queued to resolve non-full attention before implementation planning is finalized. |

## Independent Review Gate Status

Complete. Checkpoint 24 records the final independent `APPROVE` verdict after all grilling decisions were resolved. This approves the docs-only plan for a later TDD implementation pass; it does not approve starting implementation before user authorization and workspace-safety resolution.

## Checkpoint 2: First grilling decision

| Field | Record |
|---|---|
| Target Component/Phase | Step4 non-full attention modeling decision |
| Reviewer Agent Identity | Primary Codex grilling pass |
| Inspected Artifacts | User answer `[omx question answered] sliding_window_512`; `architecture_calculator_v1 - Main.csv`; Step3.7 HuggingFace config facts; generic attention op SOL support notes |
| Identified Issues/Anomalies | The original CSV did not disambiguate SWA vs linear/compressed/sparse attention, but user selected `sliding_window_512`. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Updated task docs to record the accepted decision and mark ISSUE-003 resolved. |

## Checkpoint 3: Step4Air YAML/AELLO architecture review

| Field | Record |
|---|---|
| Target Component/Phase | Corrected Step4 attention and FFN planning after Step4Air YAML review |
| Reviewer Agent Identity | Independent Codex native `critic` agent `019f56a1-c600-75e0-b1bf-527be1a3f98e` (`Zeno`) |
| Inspected Artifacts | `step4air_prefill.yaml`; `step4air_decode.yaml`; architecture CSV facts; AELLO dense/MoE components and precision behavior; updated `requirements.md`, `notes.md`, `issues.md`, and `plan.md` |
| Identified Issues/Anomalies | Verdict **BLOCK** for implementation readiness. Full GQA `64/8` and SWA GQA `96/8/window512` approved. Routed gated MoE and CSV-mandated shared branch approved structurally. Dense gating unresolved because CSV exact `H*I*3` formula conflicts with AELLO's omission-derived ungated default. Shared expert precision unsupported by YAML. CSV equal full/non-full attention parameter count also conflicts with distinct YAML head topology and needs numeric reconciliation. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Added dense gated-vs-ungated numeric comparison, exact block composition assertions (`4/23/65`), shared structure-vs-precision separation, and attention parameter reconciliation requirement. Per governance, implementation remains stopped until the user resolves the BLOCK through grilling. |

## Checkpoint 4: Dense-gating BLOCK adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Dense FFN algorithm decision |
| Reviewer Agent Identity | User adjudication after independent critic BLOCK |
| Inspected Artifacts | User answer `采用 gated SwiGLU，继续`; CSV `H * dense_inter_size * 3` formula; critic numeric comparison; Step4Air omission-derived AELLO default |
| Identified Issues/Anomalies | AELLO YAML omission realizes ungated dense FFN, but CSV directly encodes three projection matrices. The two interpretations differ by `56,623,104` projection parameters per layer. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed the plan to gated SwiGLU and recorded exact reconciliation: expected CSV=`169,869,312`, planned=`169,869,312`, absolute delta=`0`, relative delta=`0%`. Independent BLOCK remains active only for unresolved shared-expert precision and subsequent review. |

## Checkpoint 5: Shared-expert precision BLOCK adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Shared-expert algorithm and precision decision |
| Reviewer Agent Identity | User adjudication after independent critic BLOCK |
| Inspected Artifacts | User answer `确认，采用 FP8 projection + BF16 norm/merge；继续`; CSV `shared_intermediate_size=1536`; Step4Air YAML omission of shared-expert fields; existing routed-MoE precision evidence |
| Identified Issues/Anomalies | The architecture source requires a shared expert, while the implementation-reference YAML omits that branch and cannot establish its precision. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed the shared branch as gated SwiGLU with FP8 gate/up/down projections, BF16 norm, and BF16 routed/shared merge. The FFN-specific portions of the prior BLOCK are resolved; implementation remains blocked pending attention-source reconciliation, remaining grilling decisions, and a fresh independent review. |

## Checkpoint 6: MTP/nextn policy adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Step4 checkpoint MTP metadata and fair comparison policy |
| Reviewer Agent Identity | User adjudication through OMX grilling answer |
| Inspected Artifacts | User answer `[omx question answered] config3_normalized_matrix`; Step3.7 `num_nextn_predict_layers=3`; Step4Air `proposals_per_iter=3`; AIC checkpoint-default resolution and generation scale-factor behavior; DS-V4-Pro checkpoint `nextn=1` |
| Identified Issues/Anomalies | Using checkpoint defaults in the primary matrix would compare Step4 `nextn=3` against DS-V4-Pro `nextn=1`, confounding trunk architecture with unequal speculative-decode policies. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed Step4 checkpoint metadata at `nextn=3`, normalized the primary comparison to explicit `nextn=0` for both models, and added separate Step4 `nextn=3` structural/smoke coverage. |

## Checkpoint 7: Independent attention source-precedence review

| Field | Record |
|---|---|
| Target Component/Phase | Source precedence for Step4 attention op shapes while `Attention Detail` is unavailable |
| Reviewer Agent Identity | Independent StepCode Claude Opus 4.6 via `omx ask claude` |
| Inspected Artifacts | Main CSV values/descriptions; Step4Air full/SWA YAML topology; AELLO GQA projection source; workspace and Git-history search evidence; exact full/SWA parameter deltas; `.omx/artifacts/claude-you-are-an-independent-architecture-reviewer-review-one-sour-2026-07-12T17-05-12-553Z.md` |
| Identified Issues/Anomalies | Verdict `APPROVE` for conditional YAML-shape precedence. If CSV is correct and YAML stale, full-attention projection work may be underestimated by `29.584%`, which could alter bottleneck conclusions. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Reviewer requires explicit user acceptance, permanent CSV-vs-YAML numeric evidence, and a prohibition on claiming total-parameter reconciliation. User adjudication remains pending. |

## Checkpoint 8: Temporary MLA substitution adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Temporary Step4 attention approximation while `Attention Detail` is unavailable |
| Reviewer Agent Identity | User adjudication through OMX grilling answer, followed by primary Codex code-fact inspection |
| Inspected Artifacts | User answer `[omx question answered] 暂时使用mla来替代，并声明该点`; `src/aiconfigurator/sdk/operations/mla.py`; `src/aiconfigurator/sdk/models/deepseek.py`; DS-V4-specific SOL/measured operation guards |
| Identified Issues/Anomalies | Regular MLA supports direct SOL and therefore can avoid perfdb, but its formulas use fixed DeepSeek-style widths and expose no sliding-window parameter. The answer does not yet specify whether all `92` attention layers or only `69` non-full layers use MLA, nor which latent geometry is intended. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Recorded MLA as an explicitly declared temporary approximation, retained Full/SWA YAML as the target architecture, and opened a focused grilling decision for layer coverage and geometry. A fresh independent review remains required after that decision. |

## Checkpoint 9: Temporary MLA layer-scope adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Layer coverage of the temporary Step4 MLA latency substitute |
| Reviewer Agent Identity | User adjudication through OMX grilling answer |
| Inspected Artifacts | User answer `[omx question answered] mla_all_92_keep_labels`; Step4 CSV `23 Full / 69 non-full`; Step4Air target topology; generic MLA SOL limitations |
| Identified Issues/Anomalies | One shared MLA abstraction cannot reproduce the known Full/SWA topology or the SWA `window=512` behavior. Erasing the group labels would also lose architecture traceability. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed MLA coverage at all `92` layers while retaining separate `23 Full / 69 SWA` labels and counts. Required reports must state that both groups share the temporary latency model. Geometry source remains pending. |

## Checkpoint 10: Independent temporary MLA geometry review

| Field | Record |
|---|---|
| Target Component/Phase | Candidate DeepSeek-V3-style latent geometry for the all-92-layer temporary MLA substitute |
| Reviewer Agent Identity | Independent StepCode Claude Opus 4.6 via `omx ask claude` |
| Inspected Artifacts | User MLA/all-92 decisions; target YAML topology; AIC regular MLA SOL equations; granular DeepSeek projection shapes; `.omx/artifacts/claude-you-are-an-independent-architecture-modeling-reviewer-review-2026-07-12T17-35-46-941Z.md` |
| Identified Issues/Anomalies | Verdict `WATCH`. Candidate geometry is wrong in architecture kind but concrete and auditable. Full-context MLA replacing SWA window 512 causes the attention sequence-length term to scale by about `ISL/512`; long-ISL results can become approximation-dominated. Module-level MLA SOL can obscure or omit projection granularity. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Candidate recommendation is conditioned on explicit projection GEMMs plus granular SOL MLA core, no `FallbackOp`/profiled module, retained `23/69` labels, attention/non-attention breakdown, and long-ISL markers. Reconciled reviewer wording: growth is linear in `ISL/512`, not exponential, and the ratio is not a measured total-latency error. User geometry adjudication remains pending. |

## Checkpoint 11: Temporary MLA geometry adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Concrete latent geometry for the all-92-layer temporary MLA substitute |
| Reviewer Agent Identity | User adjudication through OMX grilling answer |
| Inspected Artifacts | User answer `[omx question answered] deepseek_v3_generic_mla_geometry`; DeepSeek-V3 local config; granular DeepSeek projection dimensions; independent WATCH review |
| Identified Issues/Anomalies | Geometry is intentionally borrowed and does not represent the known Step4 Full/SWA GQA architecture. Full-context treatment remains materially pessimistic for the 69 SWA layers at long ISL. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed heads=`128`, q_lora=`1536`, kv_lora=`512`, qk_nope=`128`, qk_rope=`64`, v=`128`, Step4 H=`4096`; required granular SOL projection/core ops, no fallback/profiled module, and retained all WATCH reporting safeguards. |

## Checkpoint 12: Local Step4 model identity adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Offline predefined model ID and architecture registry contract |
| Reviewer Agent Identity | User adjudication after local registry and external-source inspection |
| Inspected Artifacts | User answer `1`; local config filename conventions; `DefaultHFModels`; `ModelFamily`; `ARCHITECTURE_TO_MODEL_FAMILY`; HuggingFace API timeout evidence |
| Identified Issues/Anomalies | No local Step config exists, and public Step4 naming could not be verified. A proposed ID must not be represented as an observed public checkpoint. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed local ID=`stepfun-ai/Step4`, architecture=`Step4ForCausalLM`, ModelFamily=`STEP4`, config filename=`stepfun-ai--Step4_config.json`; retained explicit local-only provenance. |

## Checkpoint 13: H800 validation-source adjudication

| Field | Record |
|---|---|
| Target Component/Phase | H800 system specification and validation provenance |
| Reviewer Agent Identity | User adjudication after loader smoke verification |
| Inspected Artifacts | User answer `[omx question answered] h800_test_side_simulated_sol`; custom H800 YAML; SHA256; loader numeric output; built-in system directory inventory |
| Identified Issues/Anomalies | AIC has no built-in H800 system. The available YAML is explicitly simulated and contains no profiling database, so it cannot support silicon claims. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed the matrix source to the test-side YAML through the explicit systems path, required simulated/SOL-only labels, and excluded built-in H800 support from this task. |

## Checkpoint 14: Sequence-length field adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Interpretation of user-provided `iso=4k,16k,64k,256k,1m` |
| Reviewer Agent Identity | User adjudication through OMX grilling answer |
| Inspected Artifacts | User answer `[omx question answered] iso_means_isl_binary`; AIC `isl`/`osl` terminology; requested 1m boundary |
| Identified Issues/Anomalies | `iso` is not an AIC field, and decimal-vs-binary suffix interpretation would change every matrix point. OSL remains independently unspecified. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed the field to AIC `isl` and exact values `[4096,16384,65536,262144,1048576]`; opened one focused OSL decision. |

## Checkpoint 15: OSL and generation-coverage adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Output sequence length for the long-ISL matrix and independent generation-op coverage |
| Reviewer Agent Identity | User adjudication through OMX grilling answer |
| Inspected Artifacts | User answer `[omx question answered] matrix_osl1_decode_smoke_4k_1024`; accepted binary ISL matrix; temporary full-context MLA WATCH conditions; normalized `nextn=0` comparison policy |
| Identified Issues/Anomalies | A large OSL on every long-ISL point would mix sustained decode into the context matrix and make the accepted MLA approximation harder to interpret; `osl=1` alone would not cover sustained generation ops. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed all five primary-matrix rows to explicit `osl=1` and added an independent Step4-vs-DS-V4-Pro smoke at `isl=4096, osl=1024`; both use explicit `nextn=0`, and the Step4-only `nextn=3` structural check remains separate. |

## Checkpoint 16: Implementation branch adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Git branch target for the later implementation pass |
| Reviewer Agent Identity | User adjudication after primary Codex repository inspection |
| Inspected Artifacts | User answer `[omx question answered] use_step_design`; active branch from `git branch --show-current`; local/remote refs matching `*step*design*` |
| Identified Issues/Anomalies | Original request named `stepfun-design`, but that ref does not exist locally or remotely; `step-design` is active and is the only matching branch. |
| Remediation/Verification Code Actions Taken | No branch or business code action was taken. Fixed the later target to `step-design` and documented `stepfun-design` as a typo. |

## Checkpoint 17: Prefix-cache workload adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Prefix-cache state for primary comparison and independent decode workloads |
| Reviewer Agent Identity | User adjudication after primary Codex code-fact inspection |
| Inspected Artifacts | User answer `[omx question answered] prefix0_all`; AIC CLI/API/Task defaults; backend and attention/MLA prefix semantics |
| Identified Issues/Anomalies | Leaving `prefix` implicit would hide a workload assumption; a nonzero value would reduce context work and change the long-ISL question being tested. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed every primary-matrix row and the independent `4K/1024` decode smoke to explicit `prefix=0`; excluded extra prefix-cache reuse coverage from required scope. |

## Checkpoint 18: Serving-mode coverage adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Deployment modes covered by Step4-vs-DS-V4-Pro validation |
| Reviewer Agent Identity | User adjudication through OMX grilling answer |
| Inspected Artifacts | User answer `[omx question answered] both_agg_disagg`; AIC aggregate/disaggregate interfaces; requested total-GPU and worker constraints |
| Identified Issues/Anomalies | Aggregate-only validation would not exercise separate prefill/decode workers or the requested >8 worker allowance; disaggregate-only validation would omit the direct aggregate baseline. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed the matrix to run both `agg` and `disagg` with aligned model/system/workload settings. |

## Checkpoint 19: TTFT-only SLA and ranking adjudication

| Field | Record |
|---|---|
| Target Component/Phase | SLA filters and best-configuration ranking for the comparison matrix |
| Reviewer Agent Identity | User adjudication directing primary Codex to an existing approved task; primary Codex source verification |
| Inspected Artifacts | User free-text answer requesting the prior SLA convention; `todo_search_configs_deepseek-v4-pro.md` SHA256 `739fb928...8fb7`; approved summary SHA256 `1333d88c...1e8`; representative authoritative agg/disagg YAML |
| Identified Issues/Anomalies | Applying a normal TPOT SLA would contradict the referenced task and could discard rows for a dimension the user explicitly does not want constrained. The temporary MLA approximation also limits real performance conclusions even when TTFT filtering succeeds. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed TTFT targets to `200/500/1000/2000/5000` ms, encoded non-binding TPOT as `50000` ms with `pareto_sweep=false`, and fixed ranking to descending `tokens/s/gpu_cluster`; retained the prohibition on publishing true Step4 performance claims from the temporary attention approximation. |

## Checkpoint 20: Chunked-prefill mode-asymmetry root-cause review

| Field | Record |
|---|---|
| Target Component/Phase | Semantic parity of chunked-prefill modeling across `agg` and `disagg` validation |
| Reviewer Agent Identity | Primary Codex systematic-debugging pass using prior validated RCA evidence |
| Inspected Artifacts | `Task.sweep_agg_kwargs()`; `Task.sweep_disagg_kwargs()`; `sweep_agg()`; `sweep_disagg()`; prior RCA SHA256 `94c2e256...e173`; result-menu chunked-status logic |
| Identified Issues/Anomalies | Aggregate chunked prefill is genuinely modeled, but the disaggregate flag is not forwarded or consumed. Prior data shows identical disagg metrics for `22/22` common rows, so a both-mode enabled matrix would be semantically asymmetric. |
| Remediation/Verification Code Actions Taken | No fix or business code action taken. Documented the missing data-flow edge as root cause and opened a user decision; prohibited interpreting current disagg chunked flags as actual modeling. |

## Checkpoint 21: Chunked-prefill matrix adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Required chunked-prefill state after mode-asymmetry RCA |
| Reviewer Agent Identity | User adjudication through OMX grilling answer |
| Inspected Artifacts | User answer `[omx question answered] chunked_off_all`; Checkpoint 20 root-cause evidence; required both-mode validation scope |
| Identified Issues/Anomalies | Enabling chunked prefill in both specs would be semantically asymmetric, while fixing disagg plumbing would materially expand the Step4 predefined-ops task. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed both required paths to explicit chunked-prefill `false`, excluded an aggregate-only supplement, and kept the plumbing gap as future work. |

## Checkpoint 22: Step4 uneven-EP feasibility and SOL-fidelity review

| Field | Record |
|---|---|
| Target Component/Phase | Exact parallel search space for Step4 vs DeepSeek-V4-Pro |
| Reviewer Agent Identity | Primary Codex repository/upstream evidence audit; user adjudication was pending at this checkpoint and is resolved in Checkpoint 23 |
| Inspected Artifacts | `sdk/utils.py::enumerate_parallel_config`; `sdk/config.py::resolve_moe_parallelism`; `operations/moe.py` SOL arithmetic; Phase 2 Pattern A/B materialization; upstream vLLM commit `36484e464a6cf763c5b4c8af7be8e19df324997a` `layer.py` and `expert_map_manager.py`; reference cap-rerun logic |
| Identified Issues/Anomalies | AIC accepts Step4 `EP=64` but floors `352/64` to `5` experts/rank in the resident-weight SOL term. Upstream vLLM can place `32x6 + 32x5` without EPLB, so runtime support and AIC fidelity diverge. The aligned exact search through `EP=32` has `21` configs; DS-V4-Pro alone has four additional exact `EP=64` configs. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Prohibited Step4 `EP=64` from being treated as an accurate SOL row without an explicitly authorized generic uneven-EP change; prepared a user decision between common intersection, DS-only supplement, model-specific maxima, or scope expansion. |

## Checkpoint 23: Common parallel and batch-search adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Final grilling decision for parallel search-space fairness and batch-cap termination |
| Reviewer Agent Identity | User adjudication through OMX grilling answer |
| Inspected Artifacts | User answer `[omx question answered] common_21_ep32`; Checkpoint 22 uneven-EP evidence; AIC Pattern A/B materialization; reference batch/cap rerun policy |
| Identified Issues/Anomalies | Step4 `EP=64` is not faithfully modeled, a DS-only supplement would break the primary apples-to-apples space, and a single bounded rerun could still publish a cap-saturated false optimum. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed both models to the same `21` configurations (`17` pure-EP through `EP=32` plus `4` pure-MoE-TP), retained disaggregate `AA/AB/BA/BB` pairings, excluded all `EP=64` rows and generic uneven-EP changes, and required repeated selective cap doubling from agg/prefill/decode caps `1024/16/1024` until non-saturated, OOM, or TTFT-SLA-infeasible. |

## Checkpoint 24: Final independent architecture and implementation-plan review

| Field | Record |
|---|---|
| Target Component/Phase | Final docs-only Step4 architecture/modeling plan after completion of grilling |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | All 8 task Markdown docs; `.omx/artifacts/claude-you-are-the-independent-final-reviewer-for-a-docs-only-imple-2026-07-13T06-18-55-767Z.md` (SHA256 `01194872a8a19d962aa020541b9a710a79d7bc8da011e9cc88d63a985ec647b3`) |
| Identified Issues/Anomalies | No blocking or medium/high findings. LOW findings: long-ISL MLA dominance already tracked by ISSUE-017; CSV/YAML attention delta already tracked by ISSUE-014; dirty workspace already gated by ISSUE-002/Phase 0; Step4-only `nextn=3` smoke point was not yet dimension-pinned. Reviewer independently confirmed `240 x 2 = 480` matrix arithmetic, roofline-only safeguards, FFN precision, common `21` configs, batch-cap termination, and absence of hidden fallback/scaling/calibration requirements. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Existing safeguards require `approximation_dominated` at `isl>=64k`, attention/non-attention decomposition, `PerformanceResult.source == "sol"`, no `FallbackOp`/profiled/DS-V4 measured Step4 path, explicit workload fields, and the dirty-workspace gate. As a post-review planning-precision improvement, exact later source/test paths were recorded and the Step4-only `nextn=3` smoke was pinned to one H200 aggregate SOL point with Pattern-B `tp=moe_tp=8`; this narrows execution ambiguity without changing the reviewed architecture or comparison policy. |

## Checkpoint 25: DeepSeek-V4-Pro search-space parity review

| Field | Record |
|---|---|
| Target Component/Phase | Completeness of the Step4 validation search contract relative to `todo_search_configs_deepseek-v4-pro.md` |
| Reviewer Agent Identity | Primary Codex systematic-debugging and planning-doc audit |
| Inspected Artifacts | Reference TODO SHA256 `739fb928...8fb7`; representative generated `agg_patternA` and `disagg_AA` YAMLs; `Task` aggregate/prefill/decode/orchestration fields; `enumerate_parallel_config()`; `SystemSpec.get_p2p_bandwidth()`; `gb300`, H100, H200, and simulated H800 system YAMLs; current task docs |
| Identified Issues/Anomalies | Existing count `17+4=21` was correct, but the plan omitted full invariants, exact rows, role-prefixed realization, six-way experiment split, `441` disagg pair count, explicit worker/replica ceilings, OOM runtime adjudication, and GB300's 4-GPU node boundary. Reference `EP=64`, `isl=524288`, `osl=1024`, and `b300_sxm` differences were confirmed as deliberate later user overrides rather than omissions. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Repaired planning docs with exact rows and assertions; separate `agg_patternA/B` plus `disagg_AA/AB/BA/BB`; effective replica list and `64/64/64` ceilings; fail-fast OOM/error boundary; and topology-tier evidence. Materialization passed with Pattern A=`17`, B=`4`, union=`21`, `EP>8=8`, `EP16=4`, `EP32=4`, max worker width=`32`, and pairs `289/68/68/16=441`. |

## Checkpoint 26: Independent strict roofline correction/scaling review

| Field | Record |
|---|---|
| Target Component/Phase | Effective primary-matrix semantics after `DatabaseMode.SOL` selection |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | `Task` correction defaults and downstream data flow; reference generated YAML; proposed all-`1.0` primary policy; `.omx/artifacts/ask-claude-step4-roofline-corrections-20260713-0520.md` |
| Identified Issues/Anomalies | Defaults `1.1/1.08` scale per-op disagg latency, `0.9/0.92` alter disagg throughput, and `1.8` alters TTFT before SLA filtering. Equal non-neutral values are not necessarily fair because threshold filtering and serving-mode ranking react differently to each model's margins. Theoretical SOL outputs can be optimistic relative to production. |
| Remediation/Verification Code Actions Taken | No business code actions taken. Fixed all five effective fields to `1.0` for both models in every primary Task; required effective-config evidence and theoretical-SOL labeling; isolated any future calibrated-serving run from primary ranking. Claude found no blocking risk and approved the policy. |

## Checkpoint 27: Phase 0 implementation safety and clean baseline

| Field | Record |
|---|---|
| Target Component/Phase | Phase 0 isolated worktree, baseline root-cause fixes, and full unit baseline |
| Reviewer Agent Identity | Primary Codex systematic-debugging and verification pass |
| Inspected Artifacts | Original and feature-worktree Git state; `tests/unit/collector/test_parallel_run.py`; `tests/unit/collector/test_dsv4_megamoe_workload.py`; `src/aiconfigurator/sdk/operations/moe.py`; `tests/unit/sdk/database/test_moe_dispatch.py`; targeted and full pytest outputs |
| Identified Issues/Anomalies | Original checkout contained unrelated dirty changes; collector parallel tests depended on real/fake `torch` collection order and cached device state; seven MoEDispatch tests expected an NVL72-only path on eight GPUs. |
| Remediation/Verification Code Actions Taken | Created isolated branch/worktree without touching the original checkout. Fixed only the two stale test-isolation/contract problems. Verified collector regression=`30/30` in `7.87s`, MoEDispatch=`30/30` in `1.50s`, and full unit baseline=`1789 passed`, `12 skipped`, `0 failed`, exit code=`0`, elapsed=`757.49s`. Phase 0 approved complete; Step4 production implementation has not started. |

## Checkpoint 28: Fresh search-space parity revalidation

| Field | Record |
|---|---|
| Target Component/Phase | Current Step4 plan search-space completeness relative to the DeepSeek-V4-Pro Phase 2 TODO |
| Reviewer Agent Identity | Primary Codex source-backed validator plus independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | Complete reference `todo_search_configs_deepseek-v4-pro.md`; current `plan.md`, `requirements.md`, and `issues.md`; current `sdk/utils.py::enumerate_parallel_config`; plan's authoritative 21-row table; `.omx/artifacts/claude-you-are-the-independent-reviewer-for-a-planning-only-search--2026-07-13T10-21-44-156Z.md` (SHA256 `4630d0b290c5835b82143237825e9e530dcf73bb0c58410c36babb24d27330a8`) |
| Identified Issues/Anomalies | No remaining plan omission. The first local validator used stale candidate-keyword names and raised `TypeError`; this was a test-script/API mismatch and was corrected to the current list-based signature. The older reference's four `EP=64` rows, `isl=524288`, primary `osl=1024`, and `b300_sxm` remain explicit user-adjudicated differences rather than omissions. |
| Remediation/Verification Code Actions Taken | No production-code action and no further plan-content repair were required. Fresh assertions passed with reference=`25`, Pattern A/B=`17/4`, union=`21`, `EP>8/16/32=8/4/4`, max worker width=`32`, disaggregate pairs=`289/68/68/16=441`, matrix points=`240`, and mode-runs=`480`. Independent Claude found concrete omissions=`none` and returned `APPROVE`. |

## Checkpoint 29: Independent vLLM MoEDispatch reduction-gate review

| Field | Record |
|---|---|
| Target Component/Phase | Phase 1C generic vLLM `MoEDispatch` handling of `reduce_results=False` |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | `src/aiconfigurator/sdk/operations/moe.py`; `src/aiconfigurator/sdk/models/qwen35.py`; `src/aiconfigurator/sdk/models/deepseek.py`; `tests/unit/sdk/database/test_moe_dispatch.py`; Step4 attention/FFN communication contract; `.omx/artifacts/claude-you-are-the-independent-reviewer-for-a-critical-generic-comm-2026-07-13T11-02-19-581Z.md` (SHA256 `c2b7d9820425cd80eead2f02ea11a1f596912ab0a3d4b4e262f3f93b3ff2a3e4`) |
| Identified Issues/Anomalies | The vLLM branch unconditionally adds attention-TP `CustomAllReduce` when `attention_tp_size>1`, even though the constructor stores `_reduce_results`; this would double-count Step4's explicit post-projection reduction. Existing production callers retain the default `True`, and the TRT-LLM SM100 branch already honors the flag. Reviewer noted out-of-scope analogous latent behavior in SGLang and TRT-LLM SM<100 paths. |
| Remediation/Verification Code Actions Taken | Approved a strict TDD cycle that first reproduces the vLLM failure for both pre- and post-dispatch, then gates only the vLLM attention-TP allreduce on `_reduce_results`. Required regressions preserve the default-`True` reduction and DP all-gather/reduce-scatter behavior, followed by the complete `test_moe_dispatch.py` suite. No production code was changed during this review checkpoint. |

## Checkpoint 30: Recursive Step4 strict-SOL integration gate

| Field | Record |
|---|---|
| Target Component/Phase | Phase 1D complete context/generation graph provenance and no-perfdb verification |
| Reviewer Agent Identity | Primary Codex TDD and systematic-debugging verification pass |
| Inspected Artifacts | `tests/unit/sdk/database/test_step4_roofline.py`; Step4 context/generation graphs; nested `OverlapOp._group_a/_group_b`; `GEMM`, MLA, MoE, MoEDispatch, CustomAllReduce, NCCL loader boundaries; relevant pytest and Ruff outputs |
| Identified Issues/Anomalies | No remaining graph-provenance or loader defect was reproduced. The three newly added integration cases passed on their first run because ISSUE-031/032 root fixes were already present. Ruff initially found only an unsorted import block and formatter drift in the new test file; no production behavior issue was involved. |
| Remediation/Verification Code Actions Taken | Added recursive fail-on-loader coverage for Pattern-B baseline, pure EP16, and pure MoE-TP8; inspected every scalar result including zero/no-op values, verified positive context/generation totals and the nested generation overlap aggregate, then applied the standard formatter to the single test file. Recursive tests passed `3/3`; relevant regression passed `206/206`; Task regression passed `76/76`; Ruff check/format passed. No new production change was required in this checkpoint. |

## Checkpoint 31: Native DeepSeek-V4-Pro vLLM Hopper support review

| Field | Record |
|---|---|
| Target Component/Phase | Generic `Task` hardware admission and architecture-specific MoE quant resolution required by the DeepSeek-V4-Pro comparison matrix |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | `src/aiconfigurator/sdk/task_v2.py`; `tests/unit/sdk/task_v2/test_task_config.py`; focused RED output (`6` selected, `4` failed, `2` passed); `.omx/artifacts/claude-you-are-the-independent-reviewer-for-a-generic-task-behavior-2026-07-13T11-59-49-540Z.md` (SHA256 `7a8e3b9a1bafd7558206cde35eb52d9cb810513ff1a215cc154cd1dd17154a4d`) |
| Identified Issues/Anomalies | The current Hopper gate is backend-blind and blocks the required vLLM path before quant resolution. Native DeepSeek-V4-Pro also needs per-role vLLM MoE resolution: Hopper=`w4a16_mxfp4`, Blackwell=`w4a8_mxfp4_mxfp8`, with explicit overrides preserved. Reviewer classified scope risk as low and approved a backend-aware gate independent of `DatabaseMode`. One WATCH remained: a broad vLLM exception would also unblock untested `DeepSeek-V4-Flash`. |
| Remediation/Verification Code Actions Taken | Before production modification, narrowed the approved admission exception to the exact `deepseek-ai/DeepSeek-V4-Pro` model and added a regression guard that keeps `DeepSeek-V4-Flash` rejected on vLLM Hopper. Required GREEN conditions are H100/H200 aggregate SOL, H200-prefill/GB300-decode disaggregate role resolution, GB300 Blackwell resolution, explicit override precedence, non-vLLM Hopper rejection, and the Flash scope guard. |

## Checkpoint 32: Comparison delta and deterministic ranking review

| Field | Record |
|---|---|
| Target Component/Phase | Comparison-runner delta formula, zero-baseline behavior, and exact-throughput tie handling |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | Current comparison plan; proposed Step4-minus-DeepSeek formulas; no-fallback/fail-fast constraints; `.omx/artifacts/claude-you-are-the-independent-reviewer-for-a-step4-vs-deepseek-v4--2026-07-13T12-14-09-322Z.md` (SHA256 `5c599c71a900b9898380848db1e4b5316e30eec742522e8a94f200408fd975a1`) |
| Identified Issues/Anomalies | No mathematical flaw. WATCH: every metric needs an explicit higher-is-better/lower-is-better polarity so a positive latency delta is not misread; cross-model deltas require both aligned model-level results. The reviewer phrased this as an intersection of feasible configurations; the task plan instead compares each model's best result from the same 21-config search, so the applied intersection is at the aligned matrix-key level, not selected parallel-config identity. |
| Remediation/Verification Code Actions Taken | Fixed `absolute_delta=Step4-DeepSeek-V4-Pro`, `relative_delta=absolute_delta/DeepSeek-V4-Pro`, explicit zero-baseline failure, throughput-descending ranking with canonical identity-only ties, per-metric polarity metadata, and unpaired-key reporting. No implementation code was changed during the review gate. |

## Checkpoint 33: Single-point KV-cache OOM review

| Field | Record |
|---|---|
| Target Component/Phase | Generic `Task.run_single_agg()` and `Task.run_single_disagg()` KV-cache-only OOM observability |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | Current `run_single_*` control flow; aggregate sweep's typed memory outcomes; three focused RED cases; `.omx/artifacts/claude-you-are-the-independent-reviewer-for-a-generic-aic-task-beha-2026-07-13T12-28-57-069Z.md` (SHA256 `d5fe0ac70c151ea30eeecc4ec16b002b50349df11ec8f19a95dc0eb2a09b99a3`) |
| Identified Issues/Anomalies | Aggregate, disaggregate-prefill, and disaggregate-decode single-point paths ignored `check_kv_cache_oom()` and could return/rate-match an infeasible point. Reviewer confirmed model OOM must remain the first check. Converting existing model-OOM `RuntimeError` to `InsufficientMemoryError` is a separate behavior change and must be deferred to its own TDD/review cycle. |
| Remediation/Verification Code Actions Taken | After RED=`3/3 DID NOT RAISE`, added a `KVCacheCapacityError` immediately after each existing model-OOM block and before result extraction/rate matching, with role/parallelism/batch evidence. Kept model-OOM behavior unchanged and added no message parsing or fallback. Focused single-point regression passed `8/8`; full Task regression passed `92/92`; Ruff and format checks passed. |

## Checkpoint 34: Typed single-point model-OOM review

| Field | Record |
|---|---|
| Target Component/Phase | Generic model-memory OOM type narrowing in `Task.run_single_agg()` and both roles of `Task.run_single_disagg()` |
| Reviewer Agent Identity | Independent Codex native `code-reviewer` agent `019f5b82-5e29-7672-bd56-f96210b10a44` (`Newton`); verdict=`WATCH`. A preceding StepCode Claude review invocation produced no output or artifact and was safely terminated with exit code `130`; it was not treated as a verdict. |
| Inspected Artifacts | Existing three model-OOM branches, `InsufficientMemoryError` definition, comparison-runner error classifier, focused RED tests for exact type/message, dual model+KV OOM precedence, role early exit, and decode call order |
| Identified Issues/Anomalies | Bare `RuntimeError` cannot be safely classified as memory infeasibility without prohibited message parsing. The proposed narrowing is correct only if it changes exactly the three model-OOM constructors, preserves model-before-KV precedence, asserts exact messages, and does not broaden unrelated runtime errors. The existing `InsufficientMemoryError` docstring is somewhat sweep-oriented but is not a blocker and should not be expanded in this narrow patch. |
| Remediation/Verification Code Actions Taken | Added the requested dual-flag and exact-message tests; changed one import and exactly three constructors to `InsufficientMemoryError`; preserved control flow and added no fallback. RED=`3/3 failed` on bare `RuntimeError`; focused GREEN=`10/10`; fresh full Task regression=`94/94` in `41.16s`; error tests=`12/12`; classifier subset=`5/5`; Ruff, format, and `git diff --check` all passed with exit code `0`. |

## Checkpoint 35: Disaggregate cluster-ranking architecture review

| Field | Record |
|---|---|
| Target Component/Phase | Phase 2 execution adapter and disaggregate candidate-retention contract |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE with conditions` |
| Inspected Artifacts | `run_step4_comparison.py`; `sweep.py::_find_best_disagg_under_constraint`; `sweep_disagg`; `Task.sweep_disagg_kwargs`; plan ranking/cap contract; `.omx/artifacts/ask-claude-step4-disagg-cluster-ranking-20260713-213637.md` |
| Identified Issues/Anomalies | Confirmed that per-category and final raw `tokens/s/gpu` pruning can remove the `tokens/s/gpu_cluster` winner before runner ranking. Recommended a default-false `return_all_feasible` Task/sweep option and approved provisional normalization followed by global cluster rank one, selective cap expansion, final normalization, and deterministic ranking. The primary pass found additional upstream pruning in prefill `head(32)`, decode `head(16)`, and `_match_workers()` that the review did not adjudicate. |
| Remediation/Verification Code Actions Taken | No production code changed at this checkpoint. Recorded ISSUE-041, retained the Claude verdict as conditional evidence, and dispatched a separate read-only architecture audit covering every pruning stage before choosing the RED-test interface. |

## Checkpoint 36: Exact fixed-cluster disaggregate solver review

| Field | Record |
|---|---|
| Target Component/Phase | Exact experiment-local disaggregate rank-one search under a fixed total-GPU cluster objective |
| Reviewer Agent Identity | Independent Codex native `code-reviewer` agent `019f5b82-5e29-7672-bd56-f96210b10a44` (`Newton`); verdict=`WATCH`. A StepCode Claude attempt produced no output/artifact and exited `130` after termination; two replacement architect sidecars failed to return results and were closed without verdicts. |
| Inspected Artifacts | `src/aiconfigurator/sdk/sweep.py::_rate_match_dict`; `_find_best_disagg_under_constraint`; `sweep_disagg`; `src/aiconfigurator/sdk/task_v2.py`; comparison-runner cluster allocation, canonical identity, ranking, and cap-expansion helpers; proposed bottleneck grouping proof |
| Identified Issues/Anomalies | The proof `max_{p,d} min(P_p,D_d)=min(max_p P_p,max_d D_d)` is exact for a fixed worker-width/category/count choice after SLA filtering. Required safeguards: exclude deployment width above the cluster; retain full precision before ranking; compare a typed canonical tuple rather than rendered numeric strings; preserve all current feasibility constraints; reject cluster ranking with autoscale; keep the default path isolated; handle decode CP explicitly; and complete cap expansion independently for AA/AB/BA/BB before global ranking. Current early `.round(3)` and the coupled two-role pool-cap check are hazards. |
| Remediation/Verification Code Actions Taken | No production code changed. Approved entry into TDD only with brute-force-oracle equivalence, default-path parity, near-tie precision, canonical `2` versus `16`, `require_same_tp`, invalid cluster size, deployment-too-wide, autoscale conflict, Task propagation, cluster-rank cap selection, and four-experiment independent termination tests. ISSUE-041 was expanded with the complete conditions. |

## Checkpoint 37: Fixed-cluster solver and runner realization gate

| Field | Record |
|---|---|
| Target Component/Phase | Phase 2 fixed-cluster Task propagation, typed final ranking, row-specific cap saturation, and six-experiment cap orchestration |
| Reviewer Agent Identity | Primary Codex TDD/systematic-debugging implementation pass, applying independent Newton WATCH conditions from Checkpoint 36 |
| Inspected Artifacts | `src/aiconfigurator/sdk/utils.py`; `src/aiconfigurator/sdk/sweep.py`; `src/aiconfigurator/sdk/task_v2.py`; `tests/performance/aic_roofline_pareto/run_step4_comparison.py`; runner, cluster, sweep, Task, and utils tests |
| Identified Issues/Anomalies | Initial RED had four missing fixed-cluster Task assignments, missing row saturation, missing all-experiment orchestration, and missing typed sort keys. One H200 fixture searched only the simulated-H800 directory. After implementation, one parameterized test incorrectly requested decode bandwidth evidence from an aggregate row; the relevant aggregate and disaggregate bandwidth contracts were already covered separately. |
| Remediation/Verification Code Actions Taken | Corrected only the explicit H200 fixture path and the mode-specific test assertion. Implemented fixed `64`-GPU Task propagation and validation, shared cluster normalization, typed canonical keys, row-specific cap guards, and independent `agg_patternA/B` plus `disagg_AA/AB/BA/BB` cap completion. Strengthened RED=`14 failed, 2 passed`; focused GREEN=`16/16`; runner=`77/77`; combined regression=`323/323` in `41.23s`; Ruff and `git diff --check` passed. |

## Checkpoint 38: Primary `OSL=1` Prefill-throughput ranking review

| Field | Record |
|---|---|
| Target Component/Phase | Workload-specific ranking semantics for the five primary `OSL=1` Step4-vs-DeepSeek-V4-Pro workloads |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | Accepted user decision; aggregate/disaggregate formulas; fixed-64-GPU cluster normalization; typed canonical tie policy; `.omx/artifacts/claude-you-are-the-independent-reviewer-for-a-key-step4-vs-deepseek-2026-07-13T15-09-59-905Z.md` (SHA256 `0801c19ffd12e7a51d33d5c6f0e0dd976b958b9674f6da564eb5741d4b817da9`) |
| Identified Issues/Anomalies | Output-token throughput is structurally zero when `OSL=1`, so it cannot rank valid Prefill work. The approved metric must use fresh input tokens `isl-prefix`; disaggregate normalization must count the complete Prefill-plus-decode deployment rather than Prefill GPUs alone. Invalid or contradictory evidence (`ttft<=0`, `global_bs<=0`, or `isl-prefix<=0`) must fail fast. |
| Remediation/Verification Code Actions Taken | Approved implementation only for `workload_kind=primary` and `osl=1`: aggregate score=`global_bs*(isl-prefix)/(ttft_ms/1000)` followed by fixed-cluster normalization; disaggregate score=`prefill_global_bs*prefill_workers*(isl-prefix)/(ttft_ms/1000)` followed by normalization over all deployed Prefill and decode GPUs. Required `ranking_metric_kind=prefill_input_throughput`, retained output throughput for the `4K/1024` decode smoke, retained typed canonical identity for exact ties, and retained Step4-minus-DeepSeek delta/zero-baseline guards. No production code changed during this review checkpoint. |

## Checkpoint 39: Primary Prefill-throughput implementation code review

| Field | Record |
|---|---|
| Target Component/Phase | Implemented shared Prefill formula, Task/sweep opt-in, exact fixed-cluster solver branch, runner selection/normalization/ranking, and regression coverage |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | Current unstaged diff for `utils.py`, `sweep.py`, `task_v2.py`, comparison runner, and focused tests; real H200 Step4 `disagg_BB` probe; `.omx/artifacts/claude-you-are-the-independent-code-reviewer-for-the-step4-vs-deeps-2026-07-13T15-31-03-457Z.md` (SHA256 `5500ebb313da0c16c979efba2a8de66025a5e0b3746217ab9a1e6ebf971d4fd0`) |
| Identified Issues/Anomalies | No correctness defect. Minor findings: an earlier aggregate OOM early-exit optimization is generic and should remain documented; the full `Task.run()` Prefill metric path had only manual probe evidence; the exact solver is deeply nested but clear. Reviewer independently verified formulas, complete-deployment denominator, default output-path isolation, fail-fast guards, typed ties, decode-batch score independence, and both 40-case brute-force oracles. |
| Remediation/Verification Code Actions Taken | Kept the approved solver structure unchanged to avoid an unrelated refactor. Added `tests/integration/test_step4_prefill_ranking.py`, which executes the complete Step4 H200 `Task.run() -> sweep_disagg -> fixed-cluster solver -> runner metric` path and proves one `OSL=1` candidate, zero output throughput, positive Prefill throughput, full 64-GPU denominator, and exact formula parity. Integration=`1/1 passed`; affected unit regression=`401/401`; targeted Ruff/format/diff checks passed. |

## Checkpoint 39: Compact resumable mode-run checkpoint realization

| Field | Record |
|---|---|
| Target Component/Phase | Compact cap-search serialization, complete mode-run records, and exact successful-experiment normalization |
| Reviewer Agent Identity | Primary Codex TDD/systematic-debugging pass applying the earlier independent checkpoint-design `WATCH` conditions |
| Inspected Artifacts | `run_step4_comparison.py` strict JSON/checkpoint helpers; `CapSearchResult`, `SearchAttempt`, and `SinglePointEvaluation` evidence; `test_step4_comparison_checkpoint.py`; combined runner regression and Ruff output |
| Identified Issues/Anomalies | Initial feature RED exposed three missing APIs. Edge RED then proved four boundary defects: arbitrary malformed final lines were discarded, inconsistent success flags serialized, evidence could be attached to the wrong experiment key, and incomplete result sets leaked as `KeyError`. The first test command also imported the original checkout because `PYTHONPATH` was not pinned to the worktree. |
| Remediation/Verification Code Actions Taken | Added compact ordered candidate fingerprints and exact selected-point evidence, typed terminal serialization, complete experiment/normalized-row validation, and success-only Task reconstruction. The later ISSUE-043 TDD pass removed the initially allowed partial-tail recovery; every malformed checkpoint record now fails fast. Final checkpoint=`16/16`, combined regression=`116/116`, Ruff check/format and `git diff --check` passed. |

## Checkpoint 40: CLI, final-artifact, and shard-merge realization

| Field | Record |
|---|---|
| Target Component/Phase | Durable CLI execution, strict resume/finalization, JSON/CSV/Markdown artifacts, real H200 smoke validation, and disjoint shard merge before the full matrix |
| Reviewer Agent Identity | Primary Codex TDD/systematic-debugging pass; earlier independent ranking/modeling reviews remain the governing decision gates |
| Inspected Artifacts | `run_step4_comparison.py`; CLI/checkpoint/runner tests; real aggregate/disaggregate/two-model H200 checkpoints and artifacts; Step4 `nextn=3` single-point evidence |
| Identified Issues/Anomalies | Unit fixtures missed two real serialization boundaries: JSON tuple-to-list conversion broke canonical ranking, and `dataclasses.asdict()` failed on a nested `defaultdict`. Formal cap execution is substantially slower than cap=`1` smoke, making a serial 480-run execution approximately ten hours by the four-run sample. |
| Remediation/Verification Code Actions Taken | Added strict source/git/matrix-locked CLI resume, complete finalization, deterministic artifacts, and a validated disjoint shard merge. Root-caused both real failures with RED/GREEN tests; retained failed directories as evidence. Aggregate/disaggregate resume checkpoints remained two lines; cross-model smoke produced `12` successful experiments and `2` paired comparisons; nextn smoke used only SOL sources. Final affected regression=`147/147`; Ruff/format/diff checks passed. The full matrix will use no more than three concurrent system shards and will be merged only after every shard is complete. |

## Checkpoint 41: Independent current-plan search-space parity audit

| Field | Record |
|---|---|
| Target Component/Phase | Current `plan.md` search-space contract versus the authoritative DeepSeek-V4-Pro Phase 2 TODO |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | `task_memory/task_2026-07-10_step4_predefined_ops_plan/requirements.md`; `task_memory/task_2026-07-10_step4_predefined_ops_plan/plan.md`; `/data/ycfeng/stepfun-performance-optimization/aiconfigurator/task_memory/task_2026-07-04_aic_roofline_pareto_search/result-vllm-only-latest/todo_search_configs_deepseek-v4-pro.md`; `.omx/artifacts/claude-you-are-an-independent-planning-reviewer-read-these-two-loca-2026-07-13T15-52-14-710Z.md` (SHA256 `a63103213ff7d7f0a8bd6b3cbed86efcdd9d511146debf065178aa1180cf2cd7`) |
| Identified Issues/Anomalies | No true omission was identified. The differences `25 -> 21` rows, removal of `isl=524288`, primary `OSL=1` plus a separate `4K/1024` decode smoke, and `b300_sxm -> gb300` are explicit later user decisions rather than missing coverage. Two non-blocking WATCH observations remain for execution: select Prefill versus output-throughput ranking strictly by workload contract, and verify GB300 `tp=8` selects the inter-node bandwidth tier at runtime. |
| Remediation/Verification Code Actions Taken | Did not alter the accepted search space or implementation code. An independent Python tuple comparison proved reference=`25`, current=`21`, missing=`0`, unexpected=`0`; exact counts were Pattern A/B=`17/4`, `EP>8/EP16/EP32=8/4/4`, maximum worker width=`32`, disaggregate `AA/AB/BA/BB=289/68/68/16`, total=`441`, matrix points=`240`, and mode runs=`480`. Focused source tests passed `3/3` in `0.05s`. Updated only `progress.md` and `review.md` to preserve the fresh audit evidence. |

## Checkpoint 42: Checkpoint-integrity gate before full matrix execution

| Field | Record |
|---|---|
| Target Component/Phase | Resume/finalization integrity, execution-contract binding, crash durability, and exact-rerun placement before the full `480`-run matrix |
| Reviewer Agent Identity | Independent Codex native `architect` agent `019f5c1f-af19-7e40-9f98-54a99dd79883` (`Carver`); verdict=`BLOCK` for starting the full matrix |
| Inspected Artifacts | `tests/performance/aic_roofline_pareto/run_step4_comparison.py`; checkpoint/CLI unit tests; current JSONL header/load/append flow; cap-search and detailed selected-point rerun flow; representative H200 artifacts |
| Identified Issues/Anomalies | `load_checkpoint()` does not revalidate exact experiment sets, terminal/cap-history evidence, success-row ownership, or normalized row/run identity; existing load fixtures accept empty experiment records. Header identity omits initial caps and dirty-worktree source/config/system content. JSONL append is fail-fast on corruption but not crash-atomic. Detailed per-op reruns occur on every cap attempt before saturation is known. These defects do not change the approved search-space rows, but they can skip or publish incomplete completed work and multiply matrix runtime. |
| Remediation/Verification Code Actions Taken | No code change was made in this checkpoint. Opened ISSUE-046, blocked full-matrix launch, and retained the required remediation: one validator reused at append/load/finalize boundaries, an execution-contract content fingerprint, final-only detailed rerun, and independently reviewed crash-atomic persistence without malformed-tail fallback or unauthorized rename operations. |

## Checkpoint 43: ISSUE-046 cross-model architecture adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Root-cause design for serialized checkpoint validation, execution drift detection, final-only exact rerun, and crash-atomic persistence |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | ISSUE-046; current plan; `run_step4_comparison.py`; checkpoint and CLI tests; proposed centralized validator, execution-contract hash, deferred detailed rerun, and stdlib SQLite schema; `.omx/artifacts/claude-you-are-the-independent-architecture-adjudicator-for-issue-0-2026-07-13T16-01-57-237Z.md` (SHA256 `58423ddf540f14121998064420f14b5702feca271fa71a6cadf4c5ad60e896ec`) |
| Identified Issues/Anomalies | No architecture flaw in the proposal. WATCH conditions: hash only `sdk/**/*.py`, the runner, used model configs, used resolved system specs, and initial caps; cap-saturated attempts carry no detailed evaluation; replace the currently invalid empty-experiment round-trip fixture; reject semantic mismatches before transaction commit; make multi-shard merge one transaction; and fail resume when current execution content differs from the stored contract. |
| Remediation/Verification Code Actions Taken | No checkpoint code changed before user adjudication. Recorded the approved design and exact tests required for TDD. Independently cleared the unrelated Ruff format drift in two test files; their complete unit suites passed `34/34`, Ruff check/format passed, and `git diff --check` passed. |

## Checkpoint 44: ISSUE-046 user-adjudication gate

| Field | Record |
|---|---|
| Target Component/Phase | Governance transition after the checkpoint-integrity BLOCK remained unresolved for three consecutive goal turns |
| Reviewer Agent Identity | Primary Codex governance pass applying the native architect `BLOCK`, independent StepCode Claude `APPROVE`, and goal blocked-audit protocol |
| Inspected Artifacts | ISSUE-046; Checkpoints 42-43; current goal state; repeated binary user-decision prompts; search-space audit evidence; canonical Step4 `nextn=3` artifact; representative H200 artifacts; pre-change full unit regression summary |
| Identified Issues/Anomalies | The implementation design is independently approved, but the native architect's earlier `BLOCK` cannot be self-overridden. The user has not selected either explicit adjudication option. All safe work outside this gate is complete; implementing the new architecture or starting the full matrix would violate the separation-of-duties and BLOCK-adjudication rules. |
| Remediation/Verification Code Actions Taken | Did not modify checkpoint/business code and did not launch any matrix shard. Preserved the original objective and exact resume point. Recorded the pre-change full-unit evidence (`1917 passed`, `12 skipped`, `4 warnings`, exit code `0`, `766.82s`) and transitioned the active goal to `blocked` only after the same condition repeated for three consecutive goal turns. |

## Checkpoint 45: ISSUE-046 user APPROVE adjudication

| Field | Record |
|---|---|
| Target Component/Phase | User adjudication of the checkpoint-integrity BLOCK and reopening of the TDD implementation phase |
| Reviewer Agent Identity | User/human adjudicator; answer=`1` to the explicit two-option ISSUE-046 decision |
| Inspected Artifacts | Checkpoints 42-44; ISSUE-046; independent native architect BLOCK; independent StepCode Claude APPROVE design; pre-change full unit baseline |
| Identified Issues/Anomalies | No unresolved approval ambiguity remains. Implementation must still satisfy all six independent WATCH tests and must not start the full matrix before focused/full regression, representative resume verification, and independent code review pass. |
| Remediation/Verification Code Actions Taken | Reopened the active goal and authorized strict TDD for the centralized validator, execution-contract fingerprint, final-only exact rerun, SQLite transaction checkpoint, and transactional shard merge. No production code was changed in this checkpoint; the next action is to observe RED tests before implementation. |

## Checkpoint 46: Step4 aggregate mixed-step semantic review

| Field | Record |
|---|---|
| Target Component/Phase | Readiness of Step4 aggregate mixed-step scheduling, per-operation evidence, and full-matrix execution after ISSUE-046 validation |
| Reviewer Agent Identity | Independent StepCode Claude architecture review: `APPROVE with WATCH`; independent native code-reviewer `019f5c6c-6c68-7c13-b4f1-d774613c9dd1`: `REQUEST CHANGES`; independent native architect `019f5c6c-69e7-7c32-980a-228f19f5e457`: `BLOCK` |
| Inspected Artifacts | `src/aiconfigurator/sdk/backends/base_backend.py`; `src/aiconfigurator/sdk/models/step4.py`; `src/aiconfigurator/sdk/task_v2.py`; `tests/performance/aic_roofline_pareto/run_step4_comparison.py`; current Step4 aggregate H200 primary/decode artifacts; focused and affected regression evidence |
| Identified Issues/Anomalies | The aggregate mixed-step scheduler treats fixed operation names `context_attention` and `generation_attention` as its semantic interface, while Step4 emits separate Full/SWA MLA approximation operations under `context_full_mla_approx_*`, `context_swa_mla_approx_*`, `generation_full_mla_approx_*`, and `generation_swa_mla_approx_*`. Context Full/SWA attention is therefore not split through the intended attention path; mixed steps omit generation MLA attention and BMM core work; missing generic keys become `0.0` through mapping default behavior; and absent operations can be labeled `sol` by the source default. All `120` Step4 aggregate mode runs are affected. Existing Step4 aggregate H200 primary/decode artifacts are semantic-invalid and stale, but remain preserved as evidence. Additional review findings require exact 21-config completeness, a public Step4 SOL-only Task contract, stronger normalized-row checkpoint validation, actual collective-query bandwidth provenance, TPOT comparison deltas, and fingerprinting of `engine_step_backend="python"`. |
| Remediation/Verification Code Actions Taken | No code or search-space change was made in this checkpoint. Opened ISSUE-048, retained the independently approved `21`-row search space unchanged, blocked the full `480` mode-run matrix, and required an explicit aggregate-attention semantic contract plus RED/GREEN tests before any aggregate artifact is regenerated or treated as valid. |

## Checkpoint 47: ISSUE-048 user APPROVE adjudication

| Field | Record |
|---|---|
| Target Component/Phase | User adjudication of the aggregate mixed-step semantic BLOCK and associated review findings |
| Reviewer Agent Identity | User/human adjudicator; answer=`确认，执行上述plan` |
| Inspected Artifacts | Checkpoint 46; ISSUE-048; accepted `21`-row search contract; stale Step4 aggregate artifact declaration; required RED/GREEN, H200 rerun, independent review, and full-regression gates |
| Identified Issues/Anomalies | No approval ambiguity remains. Existing aggregate artifacts remain invalid evidence until regeneration, and the full matrix remains gated by tests and independent review rather than by further design approval. |
| Remediation/Verification Code Actions Taken | Authorized implementation of the explicit aggregate attention semantic contract and the attached HIGH/MEDIUM review findings. Required execution order remains RED tests, minimal root-cause fix, focused regression, representative H200 reruns, independent review, full regression, and only then the complete matrix. |

## Checkpoint 48: Post-ISSUE-046 independent code-review BLOCK

| Field | Record |
|---|---|
| Target Component/Phase | ISSUE-046 W1-W6 implementation, durable normalized evidence, SOL provenance, SQLite initialization, representative H200 resume/shard artifacts, and readiness for post-change full unit regression |
| Reviewer Agent Identity | Independent native code-reviewer `Bohr` (`019f5c74-5313-7f32-bf4c-7d735814e046`), READ-ONLY; verdict=`BLOCK` |
| Inspected Artifacts | All modified SDK sources; Step4 model/config; `tests/performance/aic_roofline_pareto/run_step4_comparison.py`; Step4 unit/integration/performance tests; ISSUE-046/047 docs; H200 primary v2, H200 decode, simulated H800, Step4 nextn=3, and real `2+2` shard-merge artifacts; SQLite checkpoints and resume evidence |
| Identified Issues/Anomalies | HIGH: a legal experiment payload can coexist with mutated normalized ranking/config/per-op fields and finalization publishes the mutation. HIGH: missing operation-level provenance for executed context/generation attention falls back to `sol`. MEDIUM: checkpoint header accepts extra fields and malformed initialization can leave a header-less schema. LOW: `batch_sweep_step` does not require an exact positive `int`. Reviewer fresh regression passed `453/453` in `61.51s`, but this normal-path evidence does not close the semantic mutation paths. |
| Remediation/Verification Code Actions Taken | No production or test code was changed during review. Opened ISSUE-049 through ISSUE-052, retained the full-unit and `480`-matrix prohibition, and required strict RED/GREEN tests, fresh representative execution/resume/shard evidence under the changed fingerprint, and a new independent review. |

## Checkpoint 49: User adjudication of the post-ISSUE-046 review BLOCK

| Field | Record |
|---|---|
| Target Component/Phase | Authorization to remediate ISSUE-049 through ISSUE-052 before the post-change full unit and full matrix gates |
| Reviewer Agent Identity | User/human adjudicator; answer=`1` to the immediately pending review-remediation choice |
| Inspected Artifacts | Checkpoint 48; independent `BLOCK`; exact required remediation for normalized-row binding, attention provenance, SQLite header/init atomicity, and batch-step validation |
| Identified Issues/Anomalies | No approval ambiguity remains. Prior successful representative artifacts are preserved as historical evidence but become non-resumable after source/runner fingerprint changes and cannot authorize the matrix. |
| Remediation/Verification Code Actions Taken | Authorized strict TDD remediation of ISSUE-049 through ISSUE-052. Required order: verify each root cause, observe focused RED tests, implement minimal fixes, run affected/broader regression, regenerate H200 and real shard evidence in new directories, obtain a new independent READ-ONLY review, then run full unit and only afterward the `480` matrix. |

## Checkpoint 50: ISSUE-048 independent semantic-contract adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Explicit aggregate attention semantic interface for Step4 decomposed Full/SWA MLA approximation operations |
| Reviewer Agent Identity | Independent StepCode Claude, `claude-opus-4-6[1m]`, effort=`max`; verdict=`APPROVE` |
| Inspected Artifacts | `BaseBackend._get_mix_step_latency()`; `BaseModel`; `Step4Model`; ISSUE-048; relevant mixed-step tests; `.omx/artifacts/claude-you-are-the-independent-cross-model-architecture-reviewer-fo-2026-07-13T17-33-00-897Z.md` |
| Identified Issues/Anomalies | Literal `context_attention`/`generation_attention` keys are an implicit interface that cannot represent Step4's decomposed norm, projection, MLA-core, BMM, output, and all-reduce operations. Backend substring matching and operation-class markers were rejected as incomplete or fragile. Claude recommended explicit model-level context/generation attention-name sets with legacy defaults, exact membership, source truthfulness, and latency conservation. |
| Remediation/Verification Code Actions Taken | No code changed during review. Accepted the model-level semantic category direction for RED/GREEN implementation. The implementation must preserve real Step4 component names and per-operation sources rather than replacing them with fabricated generic zero placeholders. |

## Checkpoint 51: Remaining search-execution and artifact-contract audit

| Field | Record |
|---|---|
| Target Component/Phase | Six non-primary findings attached to Checkpoint 46: search evaluation completeness, Step4 SOL admission, normalized-row integrity, collective provenance, TPOT comparisons, and Python engine fingerprinting |
| Reviewer Agent Identity | Independent native `test-engineer` agent `019f5c86-90a6-7b43-aa12-78aa24710b57`; read-only audit |
| Inspected Artifacts | Generic aggregate/disaggregate sweep exception paths; Step4 public Task construction/validation; checkpoint validator and finalization; communication query path and `PerformanceResult`; model-comparison metrics; `MatrixPoint`, Task propagation, mode identity, and execution contract |
| Identified Issues/Anomalies | Exact parallel materialization is correct, but unknown per-config exceptions can be swallowed after another config succeeds. Step4 admits non-formula database modes. Normalized ranking/config/per-op fields remain independently mutable. Reported bandwidth is topology-derived rather than query provenance. TPOT is supported by the helper but omitted by finalization. `engine_step_backend` remains `None` and outside identity/fingerprint. Audit status: fully fixed=`0/6`, partially fixed=`2/6`, unfixed=`4/6`. |
| Remediation/Verification Code Actions Taken | No code changed during audit. Preserved the authoritative 21-row set unchanged and expanded the execution gate to require fail-fast evaluation completeness plus the five missing admission/provenance/reporting/fingerprint contracts. Existing related tests passed `8/8` in `0.38s`, proving only that the current tests do not cover these gaps; proposed focused RED cases are required before implementation. |

## Checkpoint 52: ISSUE-048 native architecture refinement

| Field | Record |
|---|---|
| Target Component/Phase | Exact model-owned semantic contract for Step4 aggregate mixed-step context/generation attention accounting |
| Reviewer Agent Identity | Independent native `architect` agent `019f5c86-8e43-7f80-a053-b4c65ec29335`; read-only verdict: current implementation=`BLOCK`, proposed minimal contract=`APPROVE` |
| Inspected Artifacts | `BaseModel`; `Step4Model`; `BaseBackend._get_mix_step_latency()`; phase-summary aggregation; Gemma 4 same-name attention behavior; current mixed-step and Step4 tests; ISSUE-048; downstream comparison-runner provenance checks |
| Identified Issues/Anomalies | The backend's fixed generic attention keys are the root cause of omitted or double-classified Step4 Full/SWA MLA components and fabricated default source evidence. The semantic identity must be `(phase, top-level summary key)`, not operation-object identity, operation class, prefix, or substring. A Python `set` is unsuitable because it silently removes duplicate declarations and does not provide a stable output order. Runtime subset validation alone cannot detect an attention builder adding a new component that the contract forgot to declare. |
| Remediation/Verification Code Actions Taken | No code or search-space change was made. Refined the approved design to two legacy-default `ClassVar[tuple[str, ...]]` contracts on `BaseModel`, exact Step4 overrides for all `14` context and `16` generation component keys, strict graph/latency/energy/source validation, truthful component-level sources, and a Step4 builder-versus-contract exact-set drift test. Existing aggregate artifacts remain stale and the full matrix remains blocked until RED/GREEN implementation, representative regeneration, independent review, and full regression pass. |

## Checkpoint 53: User-authorized ISSUE-048 through ISSUE-052 focused remediation

| Field | Record |
|---|---|
| Target Component/Phase | Focused strict-TDD implementation for aggregate attention semantics, executed provenance, durable normalized evidence, checkpoint-header initialization, and exact batch-step typing |
| Reviewer Agent Identity | Primary Codex implementation lane under user option `1`; this checkpoint is evidence capture, not the required independent post-change approval |
| Inspected Artifacts | `BaseModel`; `Step4Model`; `BaseBackend._get_mix_step_latency()`; comparison runner validation/SQLite initialization; `Task`; sweep helper; focused unit/integration tests; RED/GREEN logs under `/tmp/issue048_050_*`, `/tmp/issue049_*`, `/tmp/issue051_*`, and `/tmp/issue052_*` |
| Identified Issues/Anomalies | Confirmed all five root causes. Durable normalized rows had a second mutable truth source; Step4 granular attention lacked an explicit backend contract; executed provenance could be guessed from database mode; checkpoint creation preceded complete semantic validation; and Python `bool`/non-int values crossed public batch-step boundaries. One ISSUE-049 test fixture also compared tuple/list representations before durable JSON normalization; that test-only mismatch was corrected without weakening production equality. Active WATCH: durable reconstruction loads the system spec through active `perf_database` systems paths, so H800 load/resume/finalize must prove the CLI systems-path contract. |
| Remediation/Verification Code Actions Taken | ISSUE-049 GREEN=`32/32`; ISSUE-048/050 focused GREEN=`15/15`; real Step4 integration=`16/16`; ISSUE-051 GREEN=`5/5`; ISSUE-052 GREEN=`8/8`; all exit codes=`0`. Added exact reconstruction, ordered `14`/`16` Step4 semantic contracts, direct executed-source checks, unique exact no-op placeholder, prevalidated in-memory SQLite serialization before exclusive target creation, and exact positive-int checks. No fallback, tolerance, auto-recovery, overwrite, correction-factor change, search-space change, `rm`, or `mv` was introduced. Broader regression, gate 6–10 audit, regenerated artifacts, and new independent READ-ONLY review remain mandatory. |

## Checkpoint 54: Gate 6/7 fail-fast execution and Step4 formula-only admission

| Field | Record |
|---|---|
| Target Component/Phase | Unknown sweep-exception completeness and public Step4 database-mode admission before perfdb loading |
| Reviewer Agent Identity | Primary Codex strict-TDD implementation lane; independent post-change approval still pending |
| Inspected Artifacts | `src/aiconfigurator/sdk/sweep.py`; `src/aiconfigurator/sdk/task_v2.py`; `tests/unit/sdk/sweep/test_sweep.py`; `tests/unit/sdk/database/test_step4_roofline.py`; `/tmp/gate06_07_red.log`; `/tmp/gate06_07_green_focused.log`; `/tmp/gate06_07_green_regression.log` |
| Identified Issues/Anomalies | Aggregate and disaggregate search paths could publish partial results after arbitrary exceptions if another row succeeded. Step4 lacked a public allowlist and could enter `None`, `SILICON`, `HYBRID`, or `EMPIRICAL`, including through a mutated Task with `run(validate=False)`. The `sweep_agg()` `Raises` docstring still describes the removed synthetic `RuntimeError` behavior and requires wording cleanup. |
| Remediation/Verification Code Actions Taken | Observed `28` intended RED failures (`4` exception-order cases plus `24` Step4 mode/boundary cases), all as `DID NOT RAISE`. Unknown exceptions now log and re-raise the original object/traceback; Step4 accepts only `SOL`/`SOL_FULL` in construction, validation, and unconditional run preflight. Focused GREEN=`28/28` in `0.11s`; related regression=`109/109` in `7.66s`; all GREEN exit codes=`0`. Typed OOM/KV/SLA terminal behavior and the 21-row search space were not changed. |

## Checkpoint 55: Gate 8/9/10 provenance, TPOT, and engine-identity audit

| Field | Record |
|---|---|
| Target Component/Phase | Actual collective-query provenance, observed/non-binding TPOT final artifacts, and Python engine-backend reproducibility |
| Reviewer Agent Identity | Independent native test-engineer `Harvey` (`019f5c9b-307c-7101-8b38-b87e13e149ee`), Gate 8 verdict=`OPEN`; independent native verifier `Planck` (`019f5c9b-3328-7c40-8e01-faa2a2135b6a`), Gate 9 verdict=`PARTIAL`; primary Codex read-only trace for Gate 10, verdict=`OPEN` |
| Inspected Artifacts | `SystemSpec` bandwidth selection; `PerformanceResult`; communication and `MoEDispatch` operations; backend per-operation summaries; `SinglePointEvaluation`; comparison-runner `_bandwidth_evidence()`, finalization, Markdown rendering, `MatrixPoint`, Task factory, mode identity, matrix hash, execution contract, checkpoint/resume tests; historical H200 decode artifacts |
| Identified Issues/Anomalies | Gate 8: runner-side topology reconstruction is not execution provenance and can report GB300 group `2` while `MoEDispatch` actually queries group `8`; the full evidence propagation chain is absent. Gate 9: TPOT helper/polarity support exists, but final JSON/CSV/Markdown omit TPOT comparisons even though eligibility correctly remains TTFT-only. Gate 10: `engine_step_backend` defaults/environment can change execution without changing mode identity, matrix hash, execution contract, or resume compatibility. |
| Remediation/Verification Code Actions Taken | No production or test code changed during the audits. Opened ISSUE-055 through ISSUE-057 with exact RED matrices. Gate 8 is classified as an important shared-data-model decision and must receive independent StepCode Claude architecture adjudication before implementation. Historical H200 TPOT evidence was preserved: aggregate Step4/DeepSeek=`89.71228254507545/122.49266142144401 ms`, delta=`-32.78037887636856 ms`, relative=`-0.26761096130963735`; disaggregate=`37.305/33.362 ms`, delta=`3.942999999999998 ms`, relative=`0.11818835801210952`. Full unit, representative regeneration, and `480` matrix remain blocked. |

## Checkpoint 56: Gate 8 independent query-provenance architecture approval

| Field | Record |
|---|---|
| Target Component/Phase | Root-cause-complete communication query provenance from topology selection through operation arithmetic, backend ownership, durable checkpoints, and final artifacts |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; READ-ONLY verdict=`APPROVE` |
| Inspected Artifacts | `system_spec.py`; `performance_result.py`; communication and MoE operations; backend aggregation and inference summary; Task/single-point evaluation path; comparison runner bandwidth reconstruction, normalization, checkpoint schema, artifacts, and tests; ISSUE-055; `.omx/artifacts/claude-you-are-the-independent-stepcode-claude-architecture-adjudic-2026-07-13T18-12-08-658Z.md` (SHA256 `86dab8ced4ed820f0072b7ba42ec4e035eca025260eb5b209a087f31b9d8a7d0`) |
| Identified Issues/Anomalies | Reviewer confirmed the stated root cause and GB300 adversarial mismatch. `PerformanceResult` is the narrowest correct carrier. Evidence must be immutable and ordered; addition concatenates without deduplication; scalar scaling preserves evidence; no-op evidence is empty; multi-query duplicates are valid; positive-latency communication without evidence fails. Old checkpoint compatibility is intentionally rejected. One prose line called GB300 node size `8`; the task/system contract is `4`, and the review's actual group-2/group-8 tier conclusion remains correct. |
| Remediation/Verification Code Actions Taken | No code changed during review. Verdict=`APPROVE`; material scope expansion=`no`; renewed human approval=`not required`. Bound implementation to leaf-created query evidence, per-operation backend ownership, exact runner normalization/artifact propagation, `CHECKPOINT_SCHEMA_VERSION` bump, execution-fingerprint drift, and removal of `_bandwidth_evidence()` as a production evidence source. Gate 8 may proceed only through strict RED→GREEN TDD. |

## Checkpoint 57: Execution-worktree search-space parity repair

| Field | Record |
|---|---|
| Target Component/Phase | Active implementation plan parity against the authoritative DeepSeek-V4-Pro search TODO |
| Reviewer Agent Identity | Primary Codex docs-only parity audit |
| Inspected Artifacts | Original reference TODO; original-checkout `plan.md`; active-worktree `plan.md`; exact 25-row and 21-row Markdown tables; aggregate/disaggregate role maps; worker/replica bounds; cap policy; matrix arithmetic |
| Identified Issues/Anomalies | The active row set was already exact, but three execution clarifications existed only in the original checkout: complete decode-smoke expansion, Pattern A/B invariant enforcement by construction, and the complete common Task-level field contract. The first disposable parser also misordered `cp` and `worker_gpus`, causing a false tuple mismatch before correction. |
| Remediation/Verification Code Actions Taken | Corrected the parser field order and observed `SEARCH_SPACE_PARITY=PASS`: reference=`25`, approved exclusions=`4`, current=`21`, missing/unexpected=`0/0`, Pattern A/B=`17/4`, disaggregate pairs=`441`, and mode runs=`480`. Added only the three missing plan statements and ISSUE-058/progress evidence. No production code, tests, or search rows changed. |

## Checkpoint 58: Final Gate 8 carrier-architecture adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Resolve the conflicting `PerformanceResult` carrier and `contextvars` capture approvals for ISSUE-055 before RED implementation |
| Reviewer Agent Identity | Independent final StepCode Claude adjudicator, model `claude-opus-4-6[1m]`, effort=`max`; READ-ONLY verdict=`APPROVE CONTEXT_CAPTURE` |
| Inspected Artifacts | Both complete conflicting review artifacts; `performance_result.py`; `perf_database.py` public query wrappers; communication and MoE operations; `base_backend.py`; `task_v2.py`; Step4 comparison runner; final artifact `.omx/artifacts/claude-you-are-the-independent-final-architecture-adjudicator-for-a-2026-07-13T18-19-30-395Z.md` (SHA256 `e0da5217b6f442da72a3c2de154fab0dad036a545de79ff22772fc2dafbd1dad`) |
| Identified Issues/Anomalies | The earlier carrier approval conflicts with the simultaneous context-capture approval. Communication operations routinely re-wrap or cast `PerformanceResult`, making metadata propagation fragile and always-on. Capture must be outside `lru_cache`, operation identity must come from `Operation._name`, nested/concurrent evaluations must remain isolated, and requested-empty evidence must be distinct from capture-not-requested. |
| Remediation/Verification Code Actions Taken | Final verdict selected `CONTEXT_CAPTURE` and rejected the carrier architecture. Bound implementation to token-scoped operation identity, outer public query capture with cached inner queries, immutable ordered evidence without deduplication, `None` versus empty-tuple semantics, exact durable round trips, fail-fast completeness, checkpoint-schema rejection, and deletion of runner reconstruction. No production code changed during adjudication; renewed human approval is not required. |

## Checkpoint 59: Gate 8-10 implementation and focused verification

| Field | Record |
|---|---|
| Target Component/Phase | ISSUE-055 communication-query provenance, ISSUE-056 TPOT publication, ISSUE-057 Python engine identity, and post-format focused verification before representative artifacts |
| Reviewer Agent Identity | Primary Codex implementation/verifier lane; duplicate pane `%95` performed an additional read-only `430/430` focused run, then was explicitly returned to READ-ONLY. This checkpoint is not the required independent post-change approval. |
| Inspected Artifacts | `communication_evidence.py`; public `PerfDatabase` wrappers and cached inner queries; operation binding in backend execution; `SinglePointEvaluation`; comparison runner exact rerun, schema-3 serialization/rebuild, TPOT finalization/Markdown, `MatrixPoint`, Task factory, mode identity, execution contract, final artifacts; Gate 8-10 unit suites; Ruff and diff logs under `/tmp/gate08_10_*` and `/tmp/gate06_10_*` |
| Identified Issues/Anomalies | The earlier static check covered only a partial file list; the full changed-file scan found five lint issues and seven formatter drifts. A stale `sweep_agg()` `Raises` docstring would have changed the SDK source fingerprint after representative completion, so the first new H200 primary v1 run was interrupted after one committed row rather than producing intentionally stale resume evidence. The pre-existing duplicated historical Checkpoint `39` and the missing untracked `tests/performance/__init__.py` observation remain unrelated and were not repaired in this checkpoint. |
| Remediation/Verification Code Actions Taken | Final public names are `CommunicationQueryEvidence` and `communication_evidence`, with no compatibility aliases. Context capture preserves actual ordered query evidence, cache hits, duplicates, nested/concurrent isolation, no-op semantics, and exact durable round trips; runner reconstruction and metadata carriers are absent. TPOT is published with lower-is-better deltas but remains non-binding. Python engine identity is fixed and durable. Gate 10 targeted=`12/12`; performance=`156/156`; SDK/capture=`257/257`; post-format combined=`413/413`; corrected Gate 6/7=`109/109`; Ruff lint/format and `git diff --check`=`PASS`. Representative v2, independent READ-ONLY review, post-review full unit, and the `480` matrix remain gated. |

## Checkpoint 60: ISSUE-059 TPOT zero-baseline semantic adjudication

| Field | Record |
|---|---|
| Target Component/Phase | Real H200 primary v2 finalization failure and auditable zero-baseline contract for observational disaggregate TPOT at `OSL=1` |
| Reviewer Agent Identity | Independent StepCode Claude semantic adjudicator, `claude-opus-4-6[1m]`, effort=`max`; READ-ONLY verdict=`APPROVE` |
| Inspected Artifacts | `run_step4_comparison.py`; comparison/matrix and CLI tests; active `plan.md`; H200 v2 SQLite with header rows=`1` and mode-run rows=`4`; `/tmp/gate06_10_h200_primary_v2_fresh.log`; `.omx/artifacts/claude-you-are-the-independent-semantic-adjudicator-for-a-step4-vs--2026-07-13T19-16-53-747Z.md` (SHA256 `6f2c530e32a3b9fe309d5ff8ac5a15b0b7cc590ea64fa979f5082e93f95c01bc`) |
| Identified Issues/Anomalies | Primary disaggregate `OSL=1` has no inter-token interval, so both models correctly report TPOT=`0.0`; the generic zero-baseline rejection treats this physical boundary as malformed. H200 v2 completed all four records but generated no final JSON/CSV/Markdown. Two disposable read-only validators initially assumed wrong SQLite/row field names; corrected schema-driven inspection proved the checkpoint itself is intact. |
| Remediation/Verification Code Actions Taken | No production or test code changed during review. Accepted Option A with a stricter metric allowlist: only TPOT with both values exactly zero may publish `absolute_delta=0.0`, `relative_delta=null`, and `status="zero_baseline_both_zero"`; nonzero baselines publish `status="computed"`; throughput/ranking zero baselines and unequal zero baselines remain fail-fast. Verdict says this is not a material scope expansion and needs no renewed human approval. Required next gate is strict RED→GREEN plus fresh v3 representative evidence; v2 remains immutable failure evidence. |

## Checkpoint 61: ISSUE-059 strict TDD and focused verification

| Field | Record |
|---|---|
| Target Component/Phase | Metric-delta schema, zero-baseline TPOT arithmetic, primary-like finalization, JSON/CSV/Markdown publication, and affected Gate 6–10 regression |
| Reviewer Agent Identity | Primary Codex implementation/verifier lane under the independent Checkpoint 60 `APPROVE`; this is not the required final independent post-change review |
| Inspected Artifacts | `run_step4_comparison.py`; `test_step4_roofline_matrix.py`; `test_step4_comparison_cli.py`; `test_step4_comparison_checkpoint.py`; Gate 6–10 SDK tests; `/tmp/issue059_red_valid.log`; `/tmp/issue059_green_targeted.log`; `/tmp/issue059_green_performance.log`; `/tmp/issue059_green_combined_post_format.log`; Ruff/diff logs |
| Identified Issues/Anomalies | Initial RED included one invalid finalization fixture key before reaching the intended comparison branch; corrected only the test key and re-observed the same six intended contract failures. Initial static pass found two formatter drifts after lint had passed. No production fallback, workload change, ranking change, or broad refactor was needed. |
| Remediation/Verification Code Actions Taken | Added required `MetricDelta.status`; changed `relative_delta` to optional; permitted `null` only for exact TPOT `0/0`; retained fail-fast for unequal and non-TPOT zero baselines; added explicit delta contract and Markdown status/N/A rendering. Valid RED=`6 failed, 119 passed`; targeted GREEN=`125/125`; performance=`162/162`; pre-format combined=`419/419`; formatted exactly two files; post-format combined=`419/419` in `52.69s`; Ruff lint/format and `git diff --check`=`PASS`. Fresh H200 v3, strict resume, decode smoke, real shard merge, and independent review remain mandatory. |

## Checkpoint 62: Fresh H200 representative and shard verification

| Field | Record |
|---|---|
| Target Component/Phase | ISSUE-059 real primary boundary, decode-smoke numeric regression, strict checkpoint resume, actual communication evidence, Python/SOL identity, and real `2+2` shard composition |
| Reviewer Agent Identity | Primary Codex verifier lane; this checkpoint supplies evidence to the still-pending independent post-change reviewer |
| Inspected Artifacts | H200 primary v3 and decode v2 SQLite/JSON/CSV/Markdown directories; primary resume-before/after hash manifests; real-shard merge v2 directory; `/tmp/gate06_10_h200_primary_v3_*`; `/tmp/gate06_10_h200_decode_v2_*` |
| Identified Issues/Anomalies | Primary v2 remains a finalization failure and is excluded. The first v3 content validator used the TTFT-SLA slot of `aligned_key` as serving mode; corrected the disposable validator from index `5` to the source-declared index `6` and obtained PASS without changing artifacts. Resume emits expected model `nextn=0` override notices while performing zero mode-run executions. |
| Remediation/Verification Code Actions Taken | Primary v3 completed mode runs=`4`, experiments=`12`, rows=`12`, comparisons=`2`; disaggregate TPOT serialized `0.0/0.0`, absolute `0.0`, relative `null`, explicit status; aggregate TPOT remained numeric. Primary strict resume execution=`0`, rows=`4`, hashes unchanged=`5/5`. Decode v2 completed the same counts with aggregate/disaggregate TPOT relative deltas `-0.2566943741127054` and `0.11818835801210952`, all status=`computed`. Real shards=`2+2`, shard and merged resume loader/executor calls=`0`, merged rows=`4`, checkpoint hash unchanged, artifact equality=`4/4`. Gate 11 is complete; independent READ-ONLY review is now required before full unit. |

## Checkpoint 63: Final independent post-change code and architecture review

| Field | Record |
|---|---|
| Target Component/Phase | Gate 12 approval of the complete Step4 formula-only implementation, communication-query provenance, Python engine identity, TPOT publication, checkpoint/resume behavior, representative artifacts, and test readiness |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; READ-ONLY leading verdict=`APPROVE` |
| Inspected Artifacts | Full uncommitted diff; task requirements/plan/issues/progress/review; Step4 SDK model and formula-only database paths; communication capture and operation binding; sweep/Task/system contracts; comparison runner and changed tests; H200 primary v3, H200 decode v2, and real `2+2` shard artifacts; `.omx/artifacts/claude-you-are-the-independent-final-post-change-code-and-architect-2026-07-13T19-56-13-078Z.md` (SHA256 `9d7eb4468e1e76be3dfc051639c53fc58d7c97e770d55216d2e435e0df23d5d3`) |
| Identified Issues/Anomalies | No blocking defect. Reviewer marked all seven contracts satisfied and found no required test missing before full unit. Non-blocking WATCH observations: P2P formula/evidence currently use a fixed `inter_node_bw` tier; generic mode-run identity has partially null descriptive fields; disaggregate cluster identity hardcodes decode `cp=1` behind a fail-fast guard; resume proof is external logs/hash evidence rather than top-level JSON metadata. Reviewer considered matrix/full-unit parallelism safe. |
| Remediation/Verification Code Actions Taken | No production or test code changed. Recorded the four observations as ISSUE-060. Accepted the `APPROVE` gate for post-change full unit. Rejected only the reviewer's scheduling relaxation: the task's pre-existing stricter Gate 14 remains authoritative, so the `480` matrix cannot start until full unit passes. |

## Checkpoint 64: Post-change full-unit regression and Gate 14 release

| Field | Record |
|---|---|
| Target Component/Phase | Gate 13 repository-wide unit regression after final independent approval and before the frozen `480` mode-run matrix |
| Reviewer Agent Identity | Primary Codex verifier lane; Gate 12 independent StepCode Claude approval was already complete before this execution |
| Inspected Artifacts | Explicit-PYTHONPATH pytest command; complete `/tmp/step4_post_change_full_unit.log` (`287850` bytes, SHA256 `068918e1c6d87b80c5f6187ba1725adee905e16e203b025cc666cc343b88b04e`); ANSI-normalized collection and terminal summaries; current isolated-worktree source and tests |
| Identified Issues/Anomalies | No test failure or blocking regression. Pytest reported `12` skipped tests and `4` warnings; the suite still completed with zero failures and exit code `0`. No matrix shard was started before this gate completed. |
| Remediation/Verification Code Actions Taken | Ran `/home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python -m pytest -p no:cacheprovider -m unit` from the isolated worktree with `PYTHONPATH="$PWD/src:$PWD"`, `tee`, and `set -o pipefail`. Collected=`3111`, selected=`1992`, passed=`1986`, skipped=`12`, deselected=`1119`, warnings=`4`, elapsed=`829.31s`, exit code=`0`. Gate 13 is complete; Gate 14 is released without changing the frozen search space or any production/test code. |

## Checkpoint 65: Shared full-contract system-shard execution review

| Field | Record |
|---|---|
| Target Component/Phase | Gate 14 system partitioning, execution-contract identity, strict resume, and final `480`-mode checkpoint composition |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; READ-ONLY leading verdict=`APPROVE` |
| Inspected Artifacts | `build_execution_contract()`; CLI `main()` filtering; `build_checkpoint_header()`; `execute_matrix_runs()`; `merge_completed_checkpoints()`; merge/resume/transaction tests; reproduced full and per-system hashes; `.omx/artifacts/claude-you-are-the-independent-read-only-reviewer-for-a-long-step4--2026-07-13T20-24-37-729Z.md` (SHA256 `cd9a9710d0f46a2aa2d15c5525ebdbb96175dbd9db3eeefd37bfbde2c334e223`) |
| Identified Issues/Anomalies | Direct `--system` CLI shards fingerprint only their selected system and therefore cannot satisfy one full-matrix merge contract. Independent WATCH risks are operational: git HEAD divergence, source or system-spec drift after contract construction, and attempting merge before every shard reaches `120/120`. |
| Remediation/Verification Code Actions Taken | No production/test code changed and no custom merger was added. Bound all shards to one complete execution contract: mode specs=`480`; models=`2`; systems=`4`; each system shard=`120`; full contract SHA256=`63aa70620a6fa8908bf0747390d3833756af6b88c0a11aecb21e63cd81da8297`; full matrix-spec SHA256=`78b4970381ca7d0fb7bdfb53619a3280b72f936703c3c4d813bd23b96398edd4`; pinned HEAD=`9ce84ebbe3a0d7f785c91d055bdbdf4fdaabcbf1`. Launchers must assert these values before creating output; source/config/system edits are prohibited until all shards complete and merge. |

## Checkpoint 66: Full 480-run merge and strict resume

| Field | Record |
|---|---|
| Target Component/Phase | Gate 14 strict composition of four complete system shards, merged artifact finalization, and fail-on-call resume immutability |
| Reviewer Agent Identity | Primary Codex verifier lane using the independently approved shared-contract design from Checkpoint 65 |
| Inspected Artifacts | Four `120`-record system checkpoints; merged `mode_runs.sqlite3`, `results.json`, `ranked_rows.csv`, `model_comparisons.csv`, and `report.md`; `/tmp/step4_full_matrix_merge_20260714_v1.log`; `/tmp/step4_full_matrix_merged_20260714_v1_resume.log` |
| Identified Issues/Anomalies | No merge or resume failure. Finalization and resume print repeated expected `nextn=0 overrides checkpoint` notices for source checkpoints carrying MTP metadata; normalized matrix identity remains explicitly `nextn=0`. No custom merger, fallback, source edit, or artifact rewrite was introduced. |
| Remediation/Verification Code Actions Taken | Used only `merge_completed_checkpoints()` under the frozen full contract. Merge produced records=`480`, normalized/ranked=`545/545`, paired/unpaired=`89/28`, H800 rows=`88`, exit=`0`. Recomputed and asserted the full contract/matrix/HEAD triple, then strict-resumed with fail-on-call hooks: records=`480`, loader/executor=`0/0`, hashes unchanged=`5/5`, exit=`0`. Final hashes are checkpoint=`d5fc2781...f39c4`, JSON=`40b3dab1...d4e`, ranked CSV=`b4eede4c...a3fd`, comparisons CSV=`ca7fd0a7...eb65`, report=`46c1681b...1528`. |

## Checkpoint 67: Merged semantic contract and comparison-summary verification

| Field | Record |
|---|---|
| Target Component/Phase | Final merged matrix cardinality, search-space parity, SOL/Python identity, no-op provenance, H800 simulation label, Step4 approximation labels, terminal taxonomy, TPOT publication, and grouped model comparison evidence |
| Reviewer Agent Identity | Primary Codex verifier lane; independent final completion review remains a separate pending lane |
| Inspected Artifacts | Merged SQLite/JSON/CSV/Markdown artifacts; source `build_common_vllm_parallel_rows()` and `pair_disagg_parallel_rows()`; `/tmp/step4_full_matrix_semantic_validation_20260714_v1.log`; `comparison_summary.md` |
| Identified Issues/Anomalies | The disposable validator initially guessed four schema details incorrectly: `num_gpu` instead of the `worker_gpus` property, DeepSeek `null` instead of `{}`, all sources=`sol` instead of six explicit zero-latency `not_executed` placeholders, and identical top-level source/data phases despite aggregate scheduling metadata. These were validator defects only and are recorded as ISSUE-062. The final artifacts contain `28` unpaired keys due one-model feasibility, so grouped numeric means explicitly use paired points only. |
| Remediation/Verification Code Actions Taken | Corrected only the disposable validator and reran all ten stages to exit=`0`. Verified mode runs=`480`, normalized/ranked=`545/545`, common rows=`21`, disaggregate pairs=`441`, SOL/no-op source entries=`20280/6`, typed success/memory/SLA terminals=`545/655/240`, Step4/DeepSeek rows=`348/197`, H800 simulated rows=`88`, paired/unpaired=`89/28`, and TPOT computed/zero-zero=`59/30`. Generated `comparison_summary.md` with exact raw-source links and grouped actual values/deltas; SHA256=`b71016083cc5b99906b60dbe471f994bf6ad26d48b78374c8e8222f94d282dbf`. |

## Checkpoint 68: Corrected final static verification

| Field | Record |
|---|---|
| Target Component/Phase | Full-repository Ruff lint/format, tracked-diff whitespace, and explicit final task-document whitespace validation before independent completion review |
| Reviewer Agent Identity | Primary Codex verifier lane |
| Inspected Artifacts | All repository Python/formattable files; tracked diff; `plan.md`, `requirements.md`, `notes.md`, `progress.md`, `issues.md`, `review.md`, `summary.md`, `lessons.md`, `comparison_summary.md`, and the final test report; `/tmp/step4_final_static_checks_20260714_v1.log` |
| Identified Issues/Anomalies | The first pass found six trailing-space lines in the new test report, all caused by intentional Markdown hard-break syntax. Ruff lint, Ruff format, and tracked diff had already passed, but the composite gate correctly remained failed. |
| Remediation/Verification Code Actions Taken | Removed the six hard-break spaces and reran all four checks. Corrected results: Ruff lint exit=`0`; Ruff format exit=`0`, `426` files already formatted; `git diff --check` exit=`0`; task docs scanned=`10`; trailing-whitespace lines=`0`. Log SHA256=`8bbeca1fec3b073e60aab6c1f50ed7475f91124bcd87ea95e7025f50a4f13ffa`. Independent completion review is released. |

## Checkpoint 69: Final independent completion review and evidence reconciliation

| Field | Record |
|---|---|
| Target Component/Phase | Final author-independent delivery approval for Step4 predefined ops, the formula-only four-system matrix, strict checkpoint composition/resume, semantic outputs, comparison evidence, and task documentation |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`; READ-ONLY leading verdict=`APPROVE`; required actions=`none` |
| Inspected Artifacts | Full tracked and untracked implementation/test diff; Step4 config/model and SDK paths; comparison runner and changed tests; all core task docs; merged SQLite/JSON/CSV/Markdown artifacts; unit, shard, merge, resume, semantic, and static evidence; `.omx/artifacts/claude-you-are-the-final-independent-read-only-completion-reviewer--2026-07-14T02-29-46-655Z.md` (SHA256 `77bbe32d1d128c5457a8440915d1777b27176e0c1a2ec54129f2ed9436187d86`) |
| Identified Issues/Anomalies | No blocking defect. Reviewer accepted every delivery requirement. It retained WATCH observations for no successful ranked rows at ISL 256K/1M and narrow single-point CI integration coverage. It also stated that no dedicated 21-row test exists; primary reconciliation found the existing exact test at `tests/unit/performance/test_step4_roofline_matrix.py:81-113`, so that observation is factually closed. The first disposable reconciliation query guessed wrong SQLite columns and was corrected after reading `PRAGMA table_info`. |
| Remediation/Verification Code Actions Taken | No production code, tests, checkpoint, or result artifact changed. Read-only reconciliation proved `80` mode runs per long ISL (`160` combined), zero normalized rows at both lengths, and typed memory/SLA experiment terminals=`160/80` at 256K and `225/15` at 1M. Confirmed the exact 21-row unit assertion and the one-function H200/4K integration shape. Accepted the remaining two observations as non-blocking future extensions; final delivery verdict remains `APPROVE`. |

## Checkpoint 70: Corrected final completion verification

| Field | Record |
|---|---|
| Target Component/Phase | Final evidence gate over merged artifacts, checkpoint identity, semantic counts, task inventory, review verdict, full-unit log, static log, and document whitespace |
| Reviewer Agent Identity | Primary Codex verifier lane following the independent Checkpoint 69 `APPROVE`; verification-before-completion and systematic-debugging workflows applied |
| Inspected Artifacts | Five merged outputs; SQLite checkpoint header/records; `results.json`; all 51 path/size/hash inventory rows in `summary.md`; final Claude artifact; full-unit log; final static log; ten task docs; `/tmp/step4_final_completion_verification_20260714_v1.log`; `/tmp/step4_final_completion_verification_20260714_v2.log` |
| Identified Issues/Anomalies | The first full verifier failed after all merged/semantic/review checks because its full-unit substring assertion read ANSI-colored raw pytest output as plain contiguous text. The raw log hash and exit marker were already correct. This was a disposable validator parsing defect, not a test or implementation failure. |
| Remediation/Verification Code Actions Taken | Normalized ANSI escapes only for the semantic summary match while retaining exact raw-log hash and exit-marker checks, then reran the complete validator. Corrected exit=`0`; merged hashes=`5/5`; SQLite=`1` header + `480` rows; SOL/no-op=`20280/6`; paired/unpaired=`89/28`; TPOT statuses=`59/30`; H800 simulated=`88`; inventory=`51/51`; final review=`APPROVE` with no required actions; full unit=`1986 passed, 12 skipped, 0 failed`; ten-doc whitespace=`0`. No source, test, checkpoint, or result artifact changed. |

## Checkpoint 71: Pre-commit corrected-v2 blocker review and remediation

| Field | Record |
|---|---|
| Target Component/Phase | Phase 6 execution-contract source identity and nested collective communication provenance before commit/push |
| Reviewer Agent Identity | Independent pre-commit reviewer lane; initial result=`REQUEST CHANGES`; authoring/remediation remained in the primary Codex lane |
| Inspected Artifacts | `run_step4_comparison.py`; `run_dsv4pro_vs_step4_throughput.py`; execution-contract tests; `OverlapOp`; backend operation binding; communication-evidence collector; collective-capture tests; RED/GREEN/focused logs under `/tmp/step4_phase6_high_blockers_*_20260715.log` |
| Identified Issues/Anomalies | HIGH-1: throughput checkpoints did not fingerprint wrapper source changes. HIGH-2: collectives nested inside `OverlapOp` inherited the outer composite operation name. Both findings invalidate old artifact provenance even though numeric execution can remain unchanged. The reported MEDIUM maps to the previously resolved ISSUE-051 checkpoint-header exact-schema and transactional initialization boundary rather than a new unhandled defect. |
| Remediation/Verification Code Actions Taken | Added contract-mutation and inner-operation evidence RED tests, observed `3 failed`, then extended the wrapper contract source set and bound each inner operation using the existing token-scoped context manager. Focused GREEN=`3 passed in 0.10s`; affected regression=`299 passed in 10.61s`. Confirmed ISSUE-051 remains covered by missing/extra-header, injected-insert-failure, exactly-one-header, and stored-extra-field rejection tests; its original RED/GREEN=`4 failed, 1 passed` then `5/5 passed in 0.08s`. Regenerated all authoritative throughput and primary evidence in new v2 directories; did not rewrite old checkpoints or add compatibility fallback. |

## Checkpoint 72: Corrected-v2 throughput menu and completed primary-shard audit

| Field | Record |
|---|---|
| Target Component/Phase | Non-GB300 and four-GPU throughput menus; three completed primary-v2 shard identities and immutable artifacts |
| Reviewer Agent Identity | Primary Codex verifier lane; final native code reviewer remained separate and pending during artifact audit |
| Inspected Artifacts | Four corrected-v2 throughput shards; `figures_completed_v2`; `figures_final_v2`; three completed primary-v2 SQLite/JSON/CSV/Markdown shard sets; annotation-spacing regression; `/tmp/step4_throughput_v2_final_audit_20260715.log`; `/tmp/step4_primary_v2_completed_shards_audit_20260715.log` |
| Identified Issues/Anomalies | The annotation regression used strict zip over unequal-length adjacent-pair inputs and failed before checking spacing. Existing output directories correctly raised `FileExistsError`, requiring new v2 paths instead of overwriting v1 evidence. The primary GB300 `120`-run shard remained active and was not inspected as complete. |
| Remediation/Verification Code Actions Taken | Replaced the test-only adjacent-pair expression with `itertools.pairwise()` and reran the affected suite to `177/177` PASS. Verified final figures=`64/64` mode identities, `50` rank-one rows, `23` paired points, `9` missing points, ratio recomputation delta=`0.0`, and fully opaque `2160 x 1260` PNGs. Verified H200/H100/H800 primary shards=`120` rows each under contract `0aec9361...5d4` and pinned HEAD, with H800 labels exclusively `simulated`. |

## Checkpoint 73: Final independent native code-review approval

| Field | Record |
|---|---|
| Target Component/Phase | Full feature diff after HIGH-1/HIGH-2 remediation, including throughput contract identity, nested collective names, annotation adjacency, and `MoEDispatch.reduce_results` behavior |
| Reviewer Agent Identity | Independent native `/root/code_reviewer`; READ-ONLY final verdict=`APPROVE`; CRITICAL/HIGH/MEDIUM=`0/0/0` |
| Inspected Artifacts | Full tracked/untracked implementation and tests; `run_step4_comparison.py`; throughput wrapper/plotter; `OverlapOp`; communication evidence; Step4 MoE graph; four focused unit-test files; focused Ruff output; `git diff --check` |
| Identified Issues/Anomalies | No CRITICAL, HIGH, or MEDIUM finding. The reviewer confirmed both runner sources participate in the throughput contract, each nested collective binds its own operation name, `pairwise()` correctly represents adjacent annotations, and `reduce_results=False` changes only the Step4 pre-dispatch path while preserving default and attention-DP behavior. |
| Remediation/Verification Code Actions Taken | No additional production/test modification was requested. Reviewer ran `114 passed in 14.22s`, focused Ruff=`All checks passed!`, and `git diff --check` exit=`0`. The feature diff is approved to proceed to primary GB300 completion, merged-v2 validation, final full regression, and delivery gates. |

## Checkpoint 74: Cross-model pre-commit and target-integration WATCH reconciliation

| Field | Record |
|---|---|
| Target Component/Phase | Independent cross-model pre-commit risk review and target `step-design` preservation/ff-only integration design |
| Reviewer Agent Identity | StepCode Claude `claude-opus-4-6[1m]`, effort=`max`, READ-ONLY verdict=`WATCH`; independent native architect verdict=`WATCH` with temporary MLA=`CLEAR`, target tracked semantics=`CLEAR`, integration procedure=`WATCH` |
| Inspected Artifacts | Complete feature diff; task requirements/plan; current Step4 temporary MLA geometry; P2P evidence boundary; `OverlapOp`; target four tracked diffs; twelve exact untracked overlaps; local-only `.omx/artifacts/claude-you-are-the-independent-cross-model-final-pre-commit-and-pre-2026-07-14T18-20-53-245Z.md` (8730 bytes, SHA256 `f244559a7af91c31ed36db93e700ef4fe8ee0be45bbb4de7262e75f90db31e84`) |
| Identified Issues/Anomalies | Claude flagged fixed inter-node P2P evidence, informational Full/SWA head fields, literal temporary projection width `2112`, graph-level `reduce_results` coverage, nested overlap evidence, an unused helper argument, and intentional fail-fast sweep behavior. The target contains four superseded tracked changes plus twelve exact untracked overlaps amid roughly 23k unrelated untracked files. |
| Remediation/Verification Code Actions Taken | Reconciled rather than silently overriding the review. P2P is already ISSUE-060 and the frozen matrix fixes PP=1. Requirements explicitly retain Full/SWA counts as audit labels while all 92 temporary MLA layers use the approved 128-head geometry. `2112` is the exact approved `1536+512+64` projection and changing its spelling after v2 execution would invalidate source identity without correcting behavior. ISSUE-068 fixes nested operation evidence. The final native reviewer independently verified `reduce_results` default/pre-dispatch/attention-DP coverage and returned `APPROVE 0/0/0`. Intentional unknown-exception re-raise remains the user's fail-fast contract. For integration, require an exact 16-path `--include-untracked` stash, verify exact membership, retain the stash without pop/drop/apply, then allow only `git merge --ff-only`; any mismatch stops the merge. The `.omx` artifact remains local-only and is not staged. |

## Checkpoint 75: Corrected-v2 GB300 completion, merge, and semantic verification

| Field | Record |
|---|---|
| Target Component/Phase | Final primary GB300 shard; four-system corrected-v2 checkpoint merge; strict resume; semantic and comparison-summary identity gates |
| Reviewer Agent Identity | Primary Codex verifier lane executing previously reviewed fail-fast merge/audit scripts; independent native review remained Checkpoint 73 `APPROVE` |
| Inspected Artifacts | Four `result-full-matrix-v2-*` shard checkpoints; merged-v2 five-artifact set; `/tmp/step4_merge_primary_v2_20260715.log`; `/tmp/step4_audit_merged_primary_v2_20260715.log`; `/tmp/step4_rebind_comparison_summary_v2_20260715.log` |
| Identified Issues/Anomalies | No data drift or merge defect. Output contained expected repeated `nextn=0` override notices. The corrected-v2 comparison table was numerically identical to v1, permitting metadata-only source rebinding; any difference would have stopped the rebind path. |
| Remediation/Verification Code Actions Taken | Verified GB300 `120/120`, contract/head identity, and five artifacts. Strict-merged four exact `120`-row shards to `480`, strict-resumed with loader/executor=`0/0` and hashes unchanged=`5/5`, then verified normalized/ranked=`545/545`, paired/unpaired=`89/28`, terminals=`545/655/240`, SOL/no-op=`20280/6`, inner collective evidence=`2935/4338/1403`, forbidden outer-overlap evidence=`0`, common rows=`21`, and AA/AB/BA/BB=`289/68/68/16`. Rebound `comparison_summary.md` to merged JSON SHA256 `d18e0580...0ed8` only after exact table identity passed. |

## Checkpoint 76: Formatter-consistent corrected-v3 completion and semantic reconciliation

| Field | Record |
|---|---|
| Target Component/Phase | Primary GB300 v3 completion; four-system merged-v3 composition; strict resume; semantic identity; comparison-summary rebinding |
| Reviewer Agent Identity | Primary Codex verifier lane executing reviewed fail-fast merge/rebind logic; a fresh author-independent pre-commit review remains a later gate |
| Inspected Artifacts | Four `result-full-matrix-v3-*` shard checkpoints; merged-v3 five-artifact set; corrected-v2 counterparts for scientific identity; `/tmp/step4_primary_v3_gb300_audit_20260715.log`; `/tmp/step4_merge_primary_v3_20260715.log`; `/tmp/step4_audit_merged_primary_v3_20260715.log`; `/tmp/step4_rebind_comparison_summary_v3_20260715.log` |
| Identified Issues/Anomalies | GB300 completed without runtime defect. Two disposable audit assertions used the wrong population: full-matrix hash/status assumptions for a system shard, and complete communication-evidence Counter equality against a three-name nested subset. Direct v2/v3 comparison showed identical scientific payloads and identical complete evidence Counters. |
| Remediation/Verification Code Actions Taken | Changed no repository source, test, checkpoint, or result artifact. Corrected only disposable validator expectations from declared evidence and reran the complete audits. Verified GB300=`120/120`, five hashes exact, v2/v3 identity after contract normalization, merged=`480`, strict resume loader/executor=`0/0`, hashes unchanged=`5/5`, normalized/ranked=`545/545`, paired/unpaired=`89/28`, terminals=`545/655/240`, SOL/no-op=`20280/6`, nested generation evidence=`2935/4338/1403`, outer overlap=`0`, common rows=`21`, and AA/AB/BA/BB=`289/68/68/16`. Rebound comparison metadata only after exact v2/v3 comparison identity. |

## Checkpoint 77: Minimum-cost focused and static verification

| Field | Record |
|---|---|
| Target Component/Phase | Final focused unit/integration regression and repository static checks before inventory/review/staging |
| Reviewer Agent Identity | Primary Codex verifier lane following the user's explicit minimum-cost test instruction |
| Inspected Artifacts | Four affected performance unit files; one Step4 integration file; full-unit v3 log; Ruff check/format output; `git diff --check`; `tests/.tmp` logs; `task_memory/env_handbook.md` |
| Identified Issues/Anomalies | `/tmp` had zero free inodes despite about `14G` free byte capacity, preventing tee-log creation. After redirecting pytest temporary files to `tests/.tmp`, the first repository-wide format check included four copied fixture runners. Neither event implicated repository source behavior. |
| Remediation/Verification Code Actions Taken | Deleted no files. Redirected `TMPDIR` and logs to the data filesystem, reran focused/integration tests to `178 passed in 8.27s`, and reran the complete static sequence with Ruff excluding only `tests/.tmp`. Verified Ruff lint=PASS, format=`429 files already formatted`, and `git diff --check` exit=`0`. Preserved the fresh formatter-consistent full-unit result `2002 passed, 12 skipped, 0 failed` without an unnecessary rerun. |

## Checkpoint 78: Final independent corrected-v3 pre-commit review

| Field | Record |
|---|---|
| Target Component/Phase | Complete feature worktree after formatter-consistent primary/throughput v3 regeneration, final focused/static/inventory verification, and before exact staging |
| Reviewer Agent Identity | Independent StepCode Claude, model `claude-opus-4-6[1m]`, effort=`max`, READ-ONLY leading verdict=`APPROVE` |
| Inspected Artifacts | Current Git diff and status; primary merged-v3 contract/header/five hashes; throughput-v3 menu and counts; formatter-consistent full-unit log; focused/integration/static logs; primary `49+1` and figure `41` inventories; exact 16-path target preservation and `ff-only` procedure; `.omx/artifacts/ask-claude-step4-final-v3-precommit-review-20260715.md` (SHA256 `f1beae4f04a09331f8ca6692172bf7ea3be1f96ac2e17440c02072c71e82770c`) |
| Identified Issues/Anomalies | Previous stale-contract HIGH/BLOCK resolved=`YES`. CRITICAL/HIGH/MEDIUM=`0/0/0`; required actions=`none`. WATCH-only: existing broad per-config sweep exception boundary and retained local v1/v2 historical directories. Reviewer found no authoritative v1/v2 staging reference, no file above 100 MiB, and no provenance or regression blocker. |
| Remediation/Verification Code Actions Taken | No code, test, checkpoint, result, or figure modification was requested. Recorded the WATCH observations in ISSUE-076. Kept v1/v2 immutable local evidence excluded from staging and retained the exact allowlist/membership audit as the final pre-commit gate. Independent conclusion: commit, push, and `ff-only` integration may proceed after the exact staging audit. |
