## Modification History

| Date       | Summary of Changes                          |
|------------|---------------------------------------------|
| 2026-08-17 | Completed Phase 14 runtime lifecycle/evidence hardening locally: `26/26` contracts, `6/6` shell syntax, strict cleanup, same-argument predict-only, quota gate, timeout margin, and synchronized forward evidence. |
| 2026-08-17 | Completed the current code/docs publication review, normalized stale reports, and recorded fresh `347/347` focused plus `401/401` Collector evidence before commit/push. |
| 2026-08-17 | Revalidated the stable post-concurrency Phase 12 files and corrected stale runtime-inventory hashes and the inventory count. |
| 2026-08-17 | Completed the local two-node coordinator and global-marker-scope fixes, recorded the post-fix live evidence, and stopped on the 16-GPU request versus 6-GPU quota blocker. |
| 2026-08-17 | Replaced active DeepEP runtime settings with explicit AgRs/NCCL communication and recorded TDD/static verification. |
| 2026-08-17 | Recorded B300 live evidence: single-node PASS, two-node lifecycle blocker, and zero-resource cleanup after controlled stop. |
| 2026-08-17 | Started the owner-authorized two-node lifecycle root fix with a focused RED contract. |
| 2026-08-13 | Created the task log and recorded the initial review and blocking scope discrepancy. |
| 2026-08-13 | Recorded the access-probe session: measured B300 quota RBAC denial, vLLM credential denial, cached-image implementation gap, and the AIC Step4-Pro-V4 shape conflict. Produced the platform ticket packet. |
| 2026-08-13 | Revalidated latest branch/image identity and B300 access; corrected stale blocker conclusions and stopped at the semantic scope gate. |
| 2026-08-13 | Confirmed the missing manifest by repository-wide filename search and recorded the dirty linked-worktree implementation gate. |
| 2026-08-13 | Recorded user choice A to reconstruct the missing manifest with explicit provenance and a new SHA256. |
| 2026-08-13 | Completed the pinned-vLLM versus AIC fidelity-gap audit and opened the operation-boundary decision. |
| 2026-08-13 | Clarified that the current Full-MFA inventory is source-derived and added a required B300 runtime-trace validation gate. |
| 2026-08-13 | Recorded user confirmation of operation-boundary option A and advanced to the dirty-worktree checkpoint gate. |
| 2026-08-13 | Created the approved task branch and resolved the missing Git LFS dependency that caused its post-checkout hook to fail. |
| 2026-08-13 | Recorded user confirmation of branch/checkpoint option A and staged the audited baseline file set. |
| 2026-08-13 | Created baseline commit `4f2b0c31`, then recorded the focused baseline result: 859 passed and 128 failed. |
| 2026-08-13 | Recorded the owner's decision to retain the temporary security-review file without any security action. |
| 2026-08-14 | Stopped the local pinned-vLLM smoke lane, created an external execution handoff, and restricted this session to AIC-side work. |
| 2026-08-14 | Strengthened the Latest MTP-off RED contract and verified the expected missing-implementation failure. |
| 2026-08-14 | External execution session resumed the pinned-vLLM B300 handoff, completed Stage 0, and began a bounded Stage 1 source-identity probe. |
| 2026-08-14 | Reduced all B300 controller scopes to 3 GiB, made launcher cleanup deterministic, and verified the static contracts. |
| 2026-08-15 | Recorded the complete B300 Attention dataset/consumer pass and the Optimus MoE static preflight. |
| 2026-08-15 | Recorded two Optimus smoke root fixes and the final 3-case B300 smoke pass. |
| 2026-08-15 | Rejected the fallback-contaminated 174-row Optimus dataset and completed the true pinned-vLLM masked/contiguous op-path local fix. |
| 2026-08-15 | Diagnosed the first replacement smoke's workspace unit mismatch and completed its RED-to-GREEN byte-capacity fix. |
| 2026-08-16 | Opened Phase 10, fixed the simulation-only DeepEP proxy boundary, and prepared its RED test plan. |
| 2026-08-15 | Completed the streamed Stage 1 source-identity probe on B300 and removed large Git-pack work from the one-GPU smoke path. |
| 2026-08-15 | Recorded the qy1-pt mount authorization blocker that prevents the one-GPU model smoke from being created. |
| 2026-08-15 | Replaced the checkpoint path with synthetic dummy weights and isolated an Optimus native SM103 runtime blocker. |
| 2026-08-15 | Completed and verified the MTP-off Latest model/op graph, including the hybrid KV-capacity inversion fix. |
| 2026-08-15 | Recorded two AIC DeepEP HT EP16 live attempts, the CUDA library-path repair, and the remaining full-RDMA/NCCL preflight gate. |
| 2026-08-15 | Restored the interrupted Collector phase and established the preliminary provider-data consumer boundary before RED tests. |
| 2026-08-15 | Completed the pinned runtime/model-plan and grouped `wo_a` provider-data vertical slice. |
| 2026-08-15 | Verified the FP32 router RED contract against the pinned Optimus custom-op path. |
| 2026-08-15 | Completed the FP32 router provider-data vertical slice with exact pinned-vLLM invocation. |
| 2026-08-15 | Verified separate Full-MFA and SWA QKV norm/RoPE RED contracts. |
| 2026-08-15 | Corrected the resumed-session Python invocation to the recorded `aic-step-design` interpreter path. |
| 2026-08-15 | Ran the authorized full-RDMA EP16 NCCL preflight; the transport and numeric all-reduce passed, while line-based PASS counting falsely rejected interleaved rank output. |
| 2026-08-15 | Revalidated the QKV vLLM-config-context fix and provider-core runner before the replacement B300 smoke. |
| 2026-08-15 | Repaired concurrent marker counting and completed the EP16 full-RDMA NCCL preflight with 16/16 ranks and exact 136/16 numeric results. |
| 2026-08-15 | Ran EP16 DeepEP HT smoke; pinned source and NCCL passed, then NVSHMEM failed because the direct Collector runtime skipped the pinned vLLM worker environment harmonization lifecycle. |
| 2026-08-15 | Completed the QKV norm/RoPE provider-data slice and corrected the Full-MFA Q tail-RoPE omission found during review. |
| 2026-08-15 | Corrected simulator OOM/capacity accounting to use peak physical KV allocation rather than logical bytes or point-in-time SWA residency. |
| 2026-08-15 | Resumed at the B300 Attention runtime gate and revalidated the controller, runtime image, Collector contract, and reusable worker-transfer path. |
| 2026-08-15 | Root-fixed the full Optimus MoE collection's post-CUDA-graph default RNG failure and completed local RED/GREEN verification. |
| 2026-08-15 | Re-evaluated and rewrote the necessity/design/training impact of all pinned B300 runtime deviations. |
| 2026-08-15 | Completed the two-node distributed-environment and RDMA-topology RED→GREEN wrapper repair. |
| 2026-08-15 | Live-verified environment restoration, root-fixed the vLLM headless/API role split, and stopped before a fourth launch per the three-attempt gate. |
| 2026-08-15 | Implemented and locally verified the pinned-vLLM DeepEP HT dispatch/combine runtime call boundary. |
| 2026-08-15 | Executed the owner-authorized fourth two-node run; role and Gloo setup passed, but NCCL failed because no usable HCA was injected. |
| 2026-08-15 | Completed the external 16-rank full-RDMA NCCL preflight, then isolated the full-model failure to DeepEP Buffer/NVSHMEM shared-host-SHM bootstrap. |
| 2026-08-15 | Recovered the interrupted one-node DeepEP discriminator and confirmed that its TTY-stopped remote command never executed. |
| 2026-08-15 | Completed the one-node DeepEP discriminator: Buffer construction passed 8/8 ranks, explicit destroy failed 8/8, and cross-node collection was formally blocked. |
| 2026-08-15 | Retried with the supplied legacy launcher guidance: NCCL and explicit NVSHMEM passed 16/16, while DeepEP Buffer failed the NVSHMEM PE-count assertion. |
| 2026-08-15 | Applied the owner-directed DeepEP no-retry gate, confirmed the SWA QKV image-runtime root cause, and verified exact cleanup of the diagnostic B300 job. |
| 2026-08-15 | Inventoried DeepEP launch examples and cases in the pinned image and vLLM checkout without launching a container or GPU job. |
| 2026-08-15 | Canonicalized the accepted 199-row B300 Attention dataset and reproduced all 199 exact consumer queries from the canonical Parquet files. |
| 2026-08-15 | Reopened the exact later DeepEP launch-contract lane under owner authorization and audited commit `9bfd9a610e` before application. |
| 2026-08-15 | Split the non-QKV provider slice, completed 74+74 B300 grouped/router measurements, canonicalized both datasets, and passed 148/148 canonical exact-consumer queries. |
| 2026-08-15 | Resumed after the DeepEP skip direction, revalidated the focused AIC suite, and retained SWA QKV overlay approval as the next-op gate. |
| 2026-08-15 | Reconfirmed DeepEP `0/116` as deferred and advanced to the missing 65K provider workload point. |
| 2026-08-15 | Measured and canonicalized the grouped/router 65K extension: 75+75 rows, 150/150 exact silicon queries, and zero duplicate keys. |
| 2026-08-15 | Completed corrected Attention 225/225 canonical validation, coverage recheck, and the full blocked MTP-off requirements matrix. |
| 2026-08-15 | Completed Full-MFA QKV 75/75 B300 collection, canonical validation, updated coverage/simulation, and post-change regression. |
| 2026-08-15 | Repaired the final Ruff-only formatting findings and completed a fresh fail-fast verification checkpoint without retrying DeepEP. |
| 2026-08-15 | Added and verified two expected RED contracts for the bounded SWA QKV annotation overlay and exact `qkv_swa` collection slice. |
| 2026-08-15 | Stopped at the repeated explicit-approval gate and marked the active goal blocked after the third consecutive continuation turn. |
| 2026-08-16 | Recorded explicit approval of the bounded SWA QKV annotation overlay and resumed Phase 9 GREEN implementation. |
| 2026-08-16 | Completed SWA QKV `75/75`, canonical QKV `150/150`, exact consumer validation, coverage refresh, and the full DeepEP-only-blocked MTP-off matrix. |
| 2026-08-16 | Closed three external-review acceptance gaps with explicit provider slices, SHA256-bound smoke evidence, and complete QKV expected-key validation. |
| 2026-08-16 | Repeated final local verification after the archive wording repair and revalidated all 43 summary inventory hashes. |
| 2026-08-16 | Re-ran the user-requested final requirement, artifact, regression, static, and publication audits before staging. |
| 2026-08-16 | Synchronized the Phase 10 proxy results, 235-row Attention closure, local audit, and refreshed deliverable inventory. |
| 2026-08-16 | Added the second scheduler-derived Attention gap slice with Collector and targeted B300 runner RED→GREEN evidence. |
| 2026-08-16 | Rejected a completed Attention timing run whose GPU was already 90% occupied and reopened only that exact workload on a clean worker. |
| 2026-08-16 | Completed the full proxy matrix checkpoint and exhaustively queried all 197 scheduler Attention coordinates to expose the complete remaining coverage gap. |
| 2026-08-16 | Rejected the first minimal endpoint measurement because the assigned B300 began with 255606 MiB occupied; preserved the evidence and reopened a clean rerun. |
| 2026-08-16 | Completed the clean endpoint rerun, canonical 68+167 Attention merge, 235/235 exact row validation, and 394/394 scheduler Attention closure. |
| 2026-08-16 | Completed fresh final archive verification, removed one duplicated local-review entry, and revalidated tests, inventory hashes, artifacts, and DeepEP isolation. |
| 2026-08-16 | Closed the Phase 11 simulation-reporting gap, reran the full matrix three times, and produced a normalized 156-row manual-review table. |
| 2026-08-16 | Completed the final requirement audit and publication verification with fresh tests, a byte-identical full simulation rerun, scoped static checks, and refreshed review records. |
| 2026-08-16 | Root-fixed CRLF output in the manual-review CSV and regenerated the 156-row publication artifact with LF-only serialization. |

# Progress Log

## 2026-08-16 — Manual-review CSV publication-format fix

**Status:** PASS; final staging audit, commit, push, and remote verification
remain.

### Motivation

`git diff --cached --check` reported every row of the normalized review CSV
as trailing whitespace. Manual inspection showed `157` CRLF line endings,
which reduced the clarity of the review packet and blocked a clean commit.

### Expectation

Keep all `156 × 87` values unchanged while making CSV serialization stable
and repository-native: exactly `157` LF endings and no carriage returns.

### Method

1. Added a unit contract that writes the real normalized row structure and
   rejects CRLF output.
2. Verified RED: `1` failed because `csv.DictWriter` emitted `\r\n`.
3. Set the writer's `lineterminator` to `"\n"` and verified GREEN:
   `1/1` passed.
4. Regenerated the CSV from the same three independently completed full-run
   JSON files. The first shell command tried `conda` by name and exited `127`;
   the existing environment handbook already requires the direct interpreter
   `/home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python`, which
   was then used successfully.

### Result

- Review matrix remains `156 × 87`.
- CSV endings: LF `157`, CRLF `0`, bare CR `0`.
- Repeat JSON SHA256 is unchanged:
  `9d5c296c7c4e95859982cbb986cbda21ef65f9a56d286ebe37cc5a780f7208a1`.
- New CSV SHA256:
  `4c1afd34a37877a6cb59cf79c37a326b537261ed9640ee4d657ce527ff49ac62`.
- Focused regression: `344/344` passed in `8.14s`.
- Full Collector regression: `401/401` passed in `31.23s`.
- Fresh full simulation: `72/72` prefill and `84/84` decode; byte-identical
  SHA256
  `b68008b8a86c0a67b90a51fc9b57c301c7561101a9e3613b405e75c6482dd61a`.
- Static checks: Ruff `45/45`, shell `14/14`, YAML `2/2`, JSON `3/3`, and
  working-tree whitespace PASS.
- Final requirement/inventory audit: `52/52` hashes, six Parquet files and
  `709` rows, `72+84` simulation conditions, `468` repeated executions,
  `156 × 87` review records, component error
  `6.51925802230835e-09 ms`, and KV error `0.0 GiB`.
- RED log:
  `/data/ycfeng/tmp/step4_phase11_csv_lf_red_20260816.log`.
- GREEN log:
  `/data/ycfeng/tmp/step4_phase11_csv_lf_green_20260816.log`.
- Regeneration log:
  `/data/ycfeng/tmp/step4_phase11_csv_lf_regeneration_20260816.log`.
- Final requirement audit log:
  `/data/ycfeng/tmp/step4_phase11_final_requirement_audit_lf_20260816.log`.

## 2026-08-16 — Final requirement audit and publication verification

**Status:** PASS; the single task-scoped commit and remote push are the
remaining publication commands.

### Motivation

The owner requested a second review against
`step4pro_v4_external_simulator_requirements.md`, normalized evidence for
manual inspection, and one code/documentation commit pushed to the remote
branch.

### Expectation

Prove from current artifacts and a fresh run that the approved AIC scope is
complete: MTP-off, all three required topologies, the explicit B300 NCCL
DeepEP proxy, all prefill/decode conditions, and three-repeat evidence. Keep
real DeepEP, native Step4Pro MTP1, and whole-model vLLM comparisons visibly
deferred rather than filling them with inferred values.

### Method

1. Re-read the full source requirements and the current task requirements,
   plan, driver, tests, JSON, CSV, issues, review, and summary.
2. Ran a fresh assertion audit across the archived JSON, repeat JSON, flat CSV,
   six canonical B300 Parquet files, and DeepEP-Parquet absence.
3. Re-ran the complete proxy matrix in a `MemoryMax=2G` scope and compared it
   byte-for-byte with the archived full result.
4. Re-ran the focused task suite and all Collector unit tests.
5. Ran Ruff only on the `45` Python inputs. The earlier YAML-as-Python command
   was a command-construction error, not a source failure; YAML and JSON were
   parsed with their native parsers.
6. Revalidated shell syntax for the `14` task scripts.

### Result

- Requirement artifact audit: PASS.
  - prefill `72`, decode `84`;
  - prefill status `24 PASS_WITH_PROXY + 48 OOM`;
  - decode status `84 PASS_WITH_PROXY`;
  - repeat executions `468`, identical cases `156/156`, maximum spread `0.0`;
  - coverage `36,420` records:
    `18,220` silicon, `15,160` analytic/non-silicon, `3,040` proxy,
    missing/error `0/0`;
  - manual-review CSV `156` rows and `87` columns;
  - component maximum absolute error
    `6.51925802230835e-09 ms`;
  - KV maximum absolute error `0.0 GiB`;
  - canonical B300 rows `709`;
  - `step4_deepep_ht_perf.parquet` absent.
- Fresh full run: `72 + 84`, `PASS_WITH_PROXY`, SHA256
  `b68008b8a86c0a67b90a51fc9b57c301c7561101a9e3613b405e75c6482dd61a`,
  byte-identical to the archived result.
- Focused suite: `343/343` passed in `8.09s`.
- Collector suite: `401/401` passed in `31.57s`.
- Ruff check and format: `45/45` Python files passed.
- Shell syntax: `14/14` passed.
- Structured inputs: YAML `2/2`, JSON `3/3` parsed.
- No new simulation-code gap was found. The remaining gaps are intentionally
  outside the approved scope: real DeepEP, native Step4Pro MTP1, live vLLM
  request metrics/router observations, and the vLLM-versus-AIC error table.

**Evidence:**

- `/data/ycfeng/tmp/step4_phase11_fresh_requirement_audit_20260816.log`,
  SHA256
  `1d249a59a03670c923fe6025047d0dfa7b565542867bdbbd805e3fbf6473db63`;
- `/data/ycfeng/tmp/step4_phase11_fresh_full_sha_20260816.log`, SHA256
  `85569c0fdaa92aeea35fd8ba9f5c8aa8531a04e3a4335e1f1b083c1ca22c5e19`;
- `/data/ycfeng/tmp/step4_phase11_final_focused_20260816.log`, SHA256
  `a4889334fa760d2c457aa032b7bb1ce4671da9953bc8182ff270ee9f5dafa128`;
- `/data/ycfeng/tmp/step4_phase11_final_collector_20260816.log`, SHA256
  `d94287553341a0b48777de9a19a07db96c3470bf3a85a6571499c7f201472494`;
- `/data/ycfeng/tmp/step4_phase11_ruff_python_only_20260816.log`, SHA256
  `ef0248af871deee68a83e15c814b0255bf03aa4f1651f4f7591a8430f8bea6b5`;
- `/data/ycfeng/tmp/step4_phase11_shell_syntax_20260816.log`, SHA256
  `2c317ea048f882d1d56062bca99c048b57d99602f684110c8a36e56d8cd9e3f9`.

## 2026-08-16 — Second scheduler-derived Attention gap RED→GREEN

**Status:** Local implementation PASS; B300 measurement pending.

### Motivation

The full DeepEP-proxy matrix exposed `18/72` blocked prefill results after the
proxy allowed execution to advance beyond the former first-chunk DeepEP
blocker. The remaining errors were exact context Attention workload gaps, not
DeepEP proxy failures.

### Expectation

- Add exactly four scheduler workloads for both pinned providers.
- Preserve every existing case and provider identity.
- Keep the default square Attention smoke unchanged while allowing an explicit
  total-context value for incremental prefix measurements.
- Fail fast when total context is smaller than query length.

### Method

- Traced the errors through the full requirements JSON, the
  `ContextAttention._query_step4_provider` consumer, the Collector getter, and
  the canonical B300 Parquet.
- Added a RED assertion requiring `66` unique getter cases and the exact
  provider cross-product for:
  `(1,4096,8192)`, `(1,16384,32768)`,
  `(1,8160,1048544)`, and `(1,32736,1048544)`.
- Added only those four shared YAML workloads.
- Added a second RED contract for separate query and total-context smoke
  controls, then updated the existing host/remote B300 Attention runner.

### Result

- Case RED: expected `1 failed`, actual `58 != 66`.
- Case GREEN: `1 passed`.
- Population audit: raw getter tasks `66`, unique invocations `66`, kept `58`,
  added `8`, removed `0`, deduplicated `0`.
- Runner RED: expected missing total-context control.
- Runner GREEN: `4/4` contract tests passed; both shell scripts pass
  `bash -n`.
- No GPU job, dataset, consumer, interpolation rule, or simulation result was
  changed in this local step.
- Evidence:
  - `/data/ycfeng/tmp/step4_attention_scheduler_cases_red_20260816.log`;
  - `/data/ycfeng/tmp/step4_attention_scheduler_cases_green_20260816.log`;
  - `/data/ycfeng/tmp/step4_attention_prefix_runner_red_20260816.log`;
  - `/data/ycfeng/tmp/step4_attention_prefix_runner_green_20260816.log`.

## 2026-08-16 — Archive wording repair and final continuation verification

**Status:** PASS; no GPU, Docker, RJob, Replica, or DeepEP execution occurred.

### Motivation

Remove one duplicated word from the archived DeepEP completion condition and
prove that the final documentation-only repair did not change any inventoried
implementation, canonical dataset, or validation artifact.

### Expectation

- Preserve all 43 SHA256 entries recorded in `summary.md`.
- Pass the current 13-path focused suite and full Collector suite.
- Revalidate all 150 QKV keys through the exact consumer.
- Reassert the archived coverage and prefill/decode results without creating
  formal latency, TPOT, or `B_max`.
- Pass Ruff, shell syntax, and whitespace checks.

### Method

- Replaced the duplicated `exist;` fragment in `summary.md` and recorded the
  documentation modification in its history.
- Parsed every path and SHA256 from the three inventory tables in
  `summary.md`, recomputed all hashes, and compared them exactly.
- Ran:

  ```bash
  timeout 60s systemd-run --user --scope -p MemoryMax=2G \
    env TMPDIR=/data/ycfeng/tmp PYTHONPATH=src:. MPLBACKEND=Agg \
    /home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python \
    -m pytest -q \
    tests/performance/step4_pro_latest/test_b300_provider_core_collection_contract.py \
    tests/unit/performance/test_step4_pro_latest_mtp_off_requirements.py \
    tests/unit/performance/test_step4_pro_latest_silicon_coverage.py \
    tests/unit/collector/test_step4_pro_latest_provider_cases.py \
    tests/unit/collector/test_framework_manifest.py \
    tests/unit/collector/test_version_resolver.py \
    tests/unit/sdk/database/test_step4_pro_latest_provider_data.py \
    tests/unit/sdk/database/test_step4_pro_latest_attention_data.py \
    tests/unit/sdk/database/test_step4_pro_latest_deepep_data.py \
    tests/unit/sdk/database/test_step4_pro_latest_optimus_moe_data.py \
    tests/unit/sdk/models/test_step4_pro_latest.py \
    tests/unit/sdk/operations/test_step4_pro_latest_operations.py \
    tests/unit/sdk/backends/test_base_backend.py
  ```

  ```bash
  timeout 60s systemd-run --user --scope -p MemoryMax=2G \
    env TMPDIR=/data/ycfeng/tmp PYTHONPATH=src:. MPLBACKEND=Agg \
    /home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python \
    -m pytest -q tests/unit/collector
  ```

- Re-ran
  `tests/performance/step4_pro_latest/validate_b300_provider_core_rows.py`
  against the canonical 150-row QKV source dataset with
  `--families qkv --expected-qkv-rows 150`.
- Re-read the 2026-08-16 coverage and full requirements JSON files and applied
  fail-fast assertions to their native schemas.
- Ran Ruff check/format on the recorded 43 Python files, `bash -n` on all 14
  task shell scripts, and `git diff --check`.

### Result

- Environment: conda `aic-step-design`, Python `3.11.15`, pytest `8.4.2`.
- Summary inventory: `43/43` hashes matched, `0` missing, `0` mismatched.
- Focused suite: `259/259` passed in `7.90s`.
- Full Collector suite: `401/401` passed in `31.20s`.
- QKV validation: `150/150` exact consumer matches, provider split `75+75`,
  duplicate physical keys `0`, latency range
  `0.006319999694824219–6.715685526529948 ms`.
- Coverage assertions: `36,420` records, `18,220` exact silicon, `15,160`
  non-silicon, `3,040` missing, `0` unexpected errors; the four missing
  physical contracts are only DeepEP HT dispatch/combine at EP16 and EP32.
- Prefill: `72/72 BLOCKED`, OOM `48/72`, peak HBM
  `142.00439453125–776.97998046875 GiB`, known partial latency
  `99.18793976790863–8158.937195512976 ms`.
- Decode: `84/84 BLOCKED`, batch-1 OOM `16/84`, batch-1 HBM
  `138.58154296875–294.77685546875 GiB`, known partial latency
  `38.4266577068447–250.7935116231927 ms`.
- Formal latency, TPOT, and decode `B_max` remain null.
- Ruff: `43/43` PASS; shell syntax: `14/14` PASS; whitespace: PASS.
- The first ad hoc artifact assertion used normalized report labels
  (`total_records`) instead of the native coverage JSON key
  (`record_count`) and failed with `KeyError`. Root cause: the assertion was
  written from the human-readable summary rather than the persisted schema.
  The corrected assertion first inspected the native schema, used its exact
  field names, and passed without changing code or data.
- Fresh evidence:
  - `/data/ycfeng/tmp/step4_final_focused_repeat_20260816.log`, SHA256
    `fd95380bea1ad2e2e544df6eb5f51598f07c9f2e366db8747371f10627efaed4`;
  - `/data/ycfeng/tmp/step4_final_full_collector_repeat_20260816.log`, SHA256
    `c9bf4f07a0acf4463769978a97b114deda18fd56e548adb7f140df342e4c84d8`;
  - `/data/ycfeng/tmp/step4_final_qkv_validation_repeat_20260816.log`, SHA256
    `94c892ce4c03298b1585f68798c7147d56afa39e052182d72cacc96dc00f811c`;
  - `/data/ycfeng/tmp/step4_final_qkv_validation_repeat_20260816.json`,
    SHA256
    `afe99c85e927f607c158effc5182dfd7ff47ed8385767ead588e68731bfe3509`.

## 2026-08-16 — Final post-review verification

**Status:** PASS for code, Collector, persisted QKV keys, simulation artifacts,
lint, shell syntax, and whitespace.

### Motivation

Run a fresh, fail-fast verification after the review remediation and document
updates, rather than relying on the earlier pre-review test checkpoint.

### Expectation

- Pass the complete focused Latest suite and full Collector suite.
- Revalidate all `150` canonical QKV keys through the unchanged consumer.
- Assert the 2026-08-16 coverage and simulation artifacts contain only the
  recorded DeepEP blocker.
- Pass Ruff, shell syntax, and whitespace checks independently.

### Method

- Ran the 13-file focused model/provider/consumer/simulation suite under
  `MemoryMax=2G`.
- Ran all `tests/unit/collector` tests under a separate `MemoryMax=2G` scope.
- Reran the exact QKV expected-key validator against the canonical source
  dataset.
- Parsed and asserted the final coverage, full requirements matrix, and QKV
  canonical JSON values.
- Built a 43-file changed-task Python list, then ran Ruff check and format,
  all 14 Step4-Pro-Latest shell syntax checks, and `git diff --check`
  independently with `set -euo pipefail`.

### Result

- Focused suite: `259/259` passed in `7.88s`.
- Full Collector suite: `401/401` passed in `31.21s`.
- The first Ruff format gate failed on one reviewer-remediation file because
  four expressions did not match canonical Ruff line layout. The generated
  diff showed formatting-only changes; applying exactly that layout changed no
  logic.
- Post-format reviewer suite: `48/48` passed in `6.40s`.
- Post-format canonical QKV validation: `150` rows, provider split `75+75`,
  exact consumers `150/150`, duplicate keys `0`.
- Simulation-artifact assertions: PASS with coverage
  `36,420 = 18,220 silicon + 15,160 non-silicon + 3,040 missing`,
  `0` errors, and only four DeepEP contracts missing.
- Ruff check: PASS on `43/43`; Ruff format: `43/43` already formatted.
- Shell syntax: `14/14` PASS; `git diff --check`: PASS.
- Evidence:
  - `/data/ycfeng/tmp/step4_final_focused_after_review_20260816.log`,
    SHA256
    `4b15ac39d012ef7d9f032c7f173cc3933b09ef8fa0a785f97e904f497b5cfb4b`;
  - `/data/ycfeng/tmp/step4_final_full_collector_after_review_20260816.log`,
    SHA256
    `b0fd882e68b57189896fbb3aa554864c2ba913441f67da073b80dc566d4a6eb3`;
  - `/data/ycfeng/tmp/step4_final_reviewer_contracts_post_format_20260816.log`,
    SHA256
    `1c9707446211e3c8edb62638712581f4323befb552c6c9a193992cb94252b801`;
  - `/data/ycfeng/tmp/step4_final_qkv_expected_key_validation_post_format_20260816.log`,
    SHA256
    `b8a630a613deee73ef01c0002cf7c9254b04d55a00080ddf6ba52968dff617dc`;
  - `/data/ycfeng/tmp/step4_final_simulation_artifact_validation_20260816.log`,
    SHA256
    `21aa898ba237365fc068e21eb8384327144a44a3087b2b098bd3519f799791c2`;
  - `/data/ycfeng/tmp/step4_final_ruff_check_after_review_20260816.log`,
    SHA256
    `82b3e6a6c090a57601d22943bd23fca9218d1031dbe5a7b754092f9a156b4f18`;
  - `/data/ycfeng/tmp/step4_final_ruff_format_after_review_20260816.log`,
    SHA256
    `d6675b63a4767317d34641fb3901c01a3e6f8536bb9289fd7d0bdf7ae2bfe59b`;
  - `/data/ycfeng/tmp/step4_final_shell_syntax_after_review_20260816.log`,
    SHA256
    `ad939aebead42a2e51324a6405078e5e59146bc4c9147ffcefc4665a246f9a7a`;
  - `/data/ycfeng/tmp/step4_final_git_diff_check_after_review_20260816.log`,
    SHA256
    `531a62067629ec38e912d348148c7183f3fefa118a984525fe78804a85755a4d`.

## 2026-08-16 — External-review acceptance-contract remediation

**Status:** Local RED→GREEN, canonical-data revalidation, and independent
re-review PASS.

### Motivation

Resolve three Important review findings that could allow a provider-core run
to select the wrong scope, start a full collection without proving a matching
smoke, or accept the correct QKV row count with the wrong token/key set.

### Expectation

- Require one explicit provider-core slice:
  `grouped_router`, `qkv_full`, or `qkv_swa`.
- Require full-mode smoke JSON plus SHA256 and validate its mode, suite, slice,
  and completed-case count on both the host and B300 worker.
- Compare QKV rows with the complete Collector-generated expected-key set,
  enforce provider split `75+75`, and reject wrong shape, dtype, or key sets.
- Preserve all accepted B300 timings and do not rerun DeepEP.

### Method

- Added five RED checks covering the three findings and ran them together with
  the existing provider-case tests.
- Removed the provider-core implicit `all` route and kept every full command
  tied to exactly one slice.
- Added host and remote smoke-evidence validation and copied the validated
  evidence into the remote payload.
- Changed the row validator from count-only QKV acceptance to exact expected
  physical-key equality derived from the Collector getter.
- Added shape and dtype negative tests, then reran the canonical 150-row QKV
  validator.
- A broad read-only search across all of `/data/ycfeng/tmp` was too large and
  was interrupted; targeted evidence paths were used instead. No file or
  runtime state changed during that inspection.

### Result

- RED: `5 failed`, `43 passed`; the failures matched the three review gaps.
- GREEN: `48 passed`, `0 failed`, `6.25s`.
- Canonical validation: `150` rows, provider split `75+75`,
  `150/150` exact consumers, duplicate keys `0`, latency range
  `0.006319999694824219–6.715685526529948 ms`.
- Updated grouped, router, Full-MFA QKV, and SWA QKV full commands now include
  `PROVIDER_CORE_SMOKE_EVIDENCE` and
  `PROVIDER_CORE_SMOKE_EVIDENCE_SHA256`.
- Independent reviewer `01a008bc-413a-7672-a8b5-9768391193d4` returned
  **APPROVE** with no remaining Blocking or Important finding.
- DeepEP remains unchanged and deferred at `0/116`.
- Evidence:
  - `/data/ycfeng/tmp/step4_reviewer_remediation_red_20260816.log`,
    SHA256
    `fc2145f51c2b4a4c012b1d944ee04f411e90fa2cc5bb93e55456380126765103`;
  - `/data/ycfeng/tmp/step4_reviewer_remediation_green_20260816.log`,
    SHA256
    `8fc35d8071620ca225ead3d5018be2aa66713d2411f799be5e637735e69fa742`;
  - `/data/ycfeng/tmp/step4_qkv_150_expected_key_validation_20260816.log`,
    SHA256
    `8188da1ecef871342f86f76f8dd30dbc81b89d4dba33d133bdbd5b6de098f906`.

## 2026-08-16 — SWA QKV annotation overlay approved

**Status:** Local GREEN complete; isolated B300 smoke is the active gate.

### Motivation

Resume the only remaining measurable pinned-provider family after the owner
approved the source-hash-bounded annotation compatibility repair.

### Expectation

- Resolve only `reload_from` and `delay_w_load` to the installed
  `cutlass.Constexpr` object.
- Apply the mutation only for the isolated `qkv_swa` slice and only after the
  exact image-native QKNorm source hash passes.
- Preserve the pinned vLLM call path, QKNorm kernel body, shapes, dtypes,
  argument values, provider identity, and persisted key.
- Turn the two recorded RED contracts GREEN before any B300 execution.
- Keep DeepEP frozen at `0/116`.

### Method

- Recorded the owner's explicit approval in `requirements.md`, `plan.md`, and
  `harness.md`.
- Re-read the Collector development, auto-collection, GPU, and Docker rules.
- Resumed from the existing two-test RED evidence rather than creating a new
  implementation path.
- Strengthened the RED contract to require an explicitly imported,
  fail-fast helper module instead of relying on non-fatal `sitecustomize`
  exceptions.
- Added the exact `qkv_swa` smoke/full slice and a process-local helper that
  verifies the image-native source SHA256 before changing the two annotation
  objects.
- Re-ran the focused contracts, full provider-core contract file, QKV
  Collector unit tests, Ruff, shell syntax, and whitespace checks.
- Ran the first isolated B300 SWA case through the exact pinned provider.
- During evidence review, found that the existing QKV probe enforced shape and
  dtype but not the planned finite-value gate. Added a focused RED test,
  implemented the missing fail-fast validation, and reran the local suite
  before accepting the smoke as the full-collection gate.

### Result

- Approval gate is closed.
- Fresh RED: `2 failed`, `10 deselected`, expected exit `1`.
- Focused GREEN: `2 passed`, `10 deselected`.
- Initial combined local regression: `43 passed`, `0 failed`, `5.11s`.
- First B300 smoke compiled and executed the actual provider with source hash
  and annotation evidence. It produced one row at
  `0.00590933362642924 ms`.
- The first smoke artifact is retained as compile/provider evidence but is not
  the final correctness gate because its Collector payload did not yet reject
  non-finite outputs.
- Finite-check RED/GREEN: `1` expected failure followed by `1/1` pass.
- Post-fix combined local regression: `44 passed`, `0 failed`, `5.07s`.
- Ruff check and format passed; three shell scripts passed `bash -n`;
  `git diff --check` passed.
- The overlay is scoped to `PROVIDER_CORE_SLICE=qkv_swa`, verifies source
  SHA256
  `5c052658c210f5a24598d31fb6cf8f753df429bdd026da32fd715ad9696bc783`,
  mutates only `reload_from` and `delay_w_load`, and never writes QKNorm
  source.
- First smoke artifact:
  `/data/ycfeng/tmp/step4_aic_provider_core_b300_20260816/smoke_s4p-aic-qkvswa-0816-114415/`.
- Runtime identities from that smoke:
  B300 SM103, torch `2.10.0+cu129`, vLLM
  `0.19.0.post20.dev26+gc820e5ae1`, `optimus-jit
  0.1.10.post8+gitcfde41ba`, and `nvidia-cutlass-dsl 4.4.2`.
- Exact cleanup passed with zero matching RJobs and Replicas.
- The first post-finite-check remote execution also reached
  `B300_STEP4_PROVIDER_CORE_COLLECTION=PASS`, but the host rejected it because
  the cross-zone evidence tar stream ended early at `393,216` bytes and
  `tar xf` reported `Unexpected EOF`. Cleanup still removed the exact RJob and
  Replica. This is classified as a post-collection transport failure; no row
  from the incomplete archive is accepted.
- The bounded transport retry completed end to end. The transferred evidence
  tar is valid at `419,840` bytes, the updated finite-check payload is present,
  and the accepted SWA smoke latency is
  `0.0052480002244313555 ms`.
- Accepted smoke artifact:
  `/data/ycfeng/tmp/step4_aic_provider_core_b300_20260816/smoke_s4p-aic-qkvswa-0816-115127/`;
  evidence-tar SHA256
  `b5549c456831c59d24e674d10bfeb997b032ec72b0c4d389e2c42604361fe663`.
- Evidence:
  - `/data/ycfeng/tmp/step4_swa_qkv_overlay_contract_red_20260816.log`,
    SHA256
    `76fac6b47e468038048d39950804eb9e7fcb46fb3e5a7f0483f80c1ac3e47e68`;
  - `/data/ycfeng/tmp/step4_swa_qkv_overlay_contract_green_20260816.log`,
    SHA256
    `c50658fe34545f796501ca075e5508ebedf54b62266c12e985aa5ef9bb8b58f0`;
  - `/data/ycfeng/tmp/step4_swa_qkv_local_contracts_green_20260816.log`,
    SHA256
    `088ffe9d347a8ae502714da3123790efe10febe7a55b7507a028dc34b110c1b5`;
  - `/data/ycfeng/tmp/step4_swa_qkv_finite_probe_red_20260816.log`;
  - `/data/ycfeng/tmp/step4_swa_qkv_finite_probe_green_20260816.log`;
  - `/data/ycfeng/tmp/step4_swa_qkv_local_contracts_finite_green_20260816.log`.

## 2026-08-16 — SWA QKV full collection, canonicalization, and simulation refresh

**Status:** QKV PASS; formal MTP-off latency remains BLOCKED only by the
owner-deferred DeepEP dataset.

### Motivation

Finish the last measurable pinned-provider family after the accepted
finite-value smoke, then refresh the unchanged AIC coverage and requirements
matrix without retrying or substituting DeepEP.

### Expectation

- Measure all `75` SWA token points through the exact pinned provider.
- Merge them with the accepted `75` Full-MFA rows without changing either
  physical identity.
- Require `150` unique keys, zero duplicates, exact silicon consumer equality,
  and no remaining QKV coverage gap.
- Preserve DeepEP dispatch/combine as the only formal simulation blocker.

### Method

- Ran `PROVIDER_CORE_SLICE=qkv_swa`, `MODE=full` after the accepted smoke.
- Validated the SWA CSV independently for runtime identity, structure,
  positive finite latency, and key uniqueness.
- Proved the existing `75`-row Parquet was byte-for-byte reproducible from the
  original Full-MFA CSV before replacing it.
- Merged the two source CSVs, ran
  `validate_b300_provider_core_rows.py` for all `150` rows, wrote the canonical
  Zstd Parquet with the same Arrow method, and queried every canonical row
  through the unchanged AIC consumer.
- Reran full EP16/EP32 silicon coverage plus the smoke and full MTP-off
  requirements drivers under `MemoryMax=2G`.
- Ran a `255`-test focused regression and the complete `399`-test Collector
  suite. No DeepEP process, GPU allocation, RJob, or Replica was started.

### Result

- SWA full artifact:
  `/data/ycfeng/tmp/step4_aic_provider_core_b300_20260816/full_s4p-aic-qkvswa-full-0816-115343/`.
- SWA rows: `75/75`; unique keys `75`; token range `1–65,536`; non-finite
  latencies `0`; non-positive latencies `0`.
- SWA latency: minimum `0.006319999694824219 ms`, median
  `0.011157333850860596 ms`, maximum `1.3165173530578613 ms`.
- SWA CSV SHA256:
  `46807ec85c4cfd4bdecc546fa24a4964f289b320c3cdcb782ecd983967b9c8b3`.
- Combined QKV: `150/150` rows, `150` unique physical keys, provider split
  `75+75`, duplicate keys `0`.
- Canonical Parquet:
  `src/aiconfigurator/systems/data/b300_sxm/vllm/0.19.0/step4_qkv_norm_rope_perf.parquet`,
  `5,329` bytes, SHA256
  `cc14553d8e0d619ac5fc21b15a9dd78633e168114da270e55edec2c6e7afd579`.
- Canonical consumers: `150/150` exact silicon; maximum absolute and relative
  errors `0.0 ms` and `0.0`.
- Coverage: `36,420` records, `18,220` exact silicon, `15,160`
  analytic/non-silicon, `3,040` missing, `0` errors. QKV is
  `1,560/1,560`; only four DeepEP physical contracts remain missing.
- Smoke matrix: prefill `2/2`, decode `1/1`, expected exit `2`, missing family
  only `deepep_ht`.
- Full matrix: prefill `72/72`, decode `84/84`, expected exit `2`; formal
  latency and `B_max` remain null only because DeepEP is absent.
- Known partial latency is
  `99.18793976790863–8158.937195512976 ms` for prefill and
  `38.4266577068447–250.7935116231927 ms` for decode batch 1.
- Focused regression: `255 passed` in `13.12s`.
- Full Collector regression: `399 passed` in `32.21s`.
- Evidence:
  - `qkv_b300_consumer_validation_2026-08-16.json`;
  - `qkv_b300_canonical_consumer_validation_2026-08-16.json`;
  - `aic_silicon_coverage_2026-08-16.json`;
  - `mtp_off_requirements_smoke_2026-08-16.json`;
  - `mtp_off_requirements_full_2026-08-16.json`;
  - `/data/ycfeng/tmp/step4_final_focused_post_qkvswa_20260816.log`;
  - `/data/ycfeng/tmp/step4_full_collector_post_qkvswa_20260816.log`.

## 2026-08-15 — Repeated SWA overlay approval blocker

**Status:** BLOCKED pending explicit owner approval.

### Motivation

Respect the project rule that a runtime behavior modification must not be
implemented without explicit agreement, while avoiding repeated no-op work.

### Expectation

- Keep the verified RED contracts and all collected datasets unchanged.
- Do not implement or run the annotation overlay without approval.
- Resume directly at GREEN implementation after approval.

### Method

- Counted three consecutive goal-continuation turns reaching the same approval
  gate.
- Confirmed there is no remaining independent provider family to measure:
  DeepEP is owner-deferred, MTP1 is deferred, and all non-SWA families are
  already collected and canonicalized.
- Started no process and changed no runtime behavior.

### Result

- The active goal is blocked solely on explicit approval of the bounded SWA
  QKV annotation overlay.
- The next executable action remains deterministic: implement the two
  annotation resolutions, turn the two RED contracts GREEN, then run B300
  smoke/full collection.

## 2026-08-15 — SWA QKV overlay contract RED gate

**Status:** EXPECTED RED; runtime behavior remains unchanged pending explicit
approval.

### Motivation

Continue toward complete QKV coverage without silently modifying the pinned
runtime. The desired fix must remain limited to resolving the two postponed
`cutlass.Constexpr` annotations and must collect only the actual pinned SWA
provider path.

### Expectation

- Define a separate `qkv_swa` collection slice.
- Require the exact image-native QKNorm source SHA256 before any mutation.
- Resolve only `reload_from` and `delay_w_load` to `cutlass.Constexpr`.
- Do not rewrite the pinned vLLM source, QKNorm source, kernel body, shapes,
  dtypes, or provider identity.
- Require one smoke row and `75` full SWA rows.

### Method

- Added two static contract tests to
  `test_b300_provider_core_collection_contract.py`.
- Ran only those tests under `MemoryMax=2G`.
- Required pytest exit `1` and exactly two failures to validate the RED state.
- Did not modify the remote runner or start Docker, GPU, RJob, Replica, SWA,
  or DeepEP execution.

### Result

- RED validation: `2 failed`, `10 deselected`, `0.19s`, expected exit `1`.
- First failure proves `qkv_swa` and its source-hash/annotation boundary do
  not yet exist.
- Second failure proves an isolated `75`-case SWA collection slice does not
  yet exist.
- Evidence:
  `/data/ycfeng/tmp/step4_swa_qkv_overlay_contract_red_20260815.log`,
  SHA256
  `680272e184fe5a0e913ea31e272f0d01e8e4fc1a45f09da6b76d44d64f265665`.
- DeepEP remains frozen at `0/116`.

## 2026-08-15 — Final fail-fast formatting and verification checkpoint

**Status:** PASS for code/test hygiene; formal simulation remains blocked by
SWA QKV and the owner-deferred DeepEP dataset.

### Motivation

Close the current AIC checkpoint without allowing a previously aggregated
validation command to hide Ruff failures, and without reopening the frozen
DeepEP lane.

### Expectation

- Fix only Ruff import/format findings; do not change runtime behavior.
- Re-run the focused model, Collector, consumer, and MTP-off tests.
- Re-run the formatted e2e/provider contract tests.
- Execute Ruff, shell syntax, and whitespace checks as separate fail-fast
  commands.
- Keep DeepEP at `0/116` and SWA QKV at `0/75`.

### Method

- Inspected Ruff-generated diffs before editing.
- Applied formatting-only changes to six contract-test files.
- The first fail-fast Ruff rerun exposed one remaining extra blank line in
  `test_run_b300_source_probe_static.py`; removed that exact line.
- Re-ran each validation command independently with `set -euo pipefail`.
- Did not launch Docker, GPU, RJob, Replica, DeepEP, or SWA QKV collection.

### Result

- Root cause of the earlier false-success lint aggregation: the compound
  command lacked `set -euo pipefail`; the final successful command and `tee`
  masked earlier Ruff failures.
- Focused regression: `134 passed`, `0 failed`, `5.73s`.
- Formatted contract regression: `31 passed`, `0 failed`, `1.97s`.
- Ruff check: `43/43` changed task Python files passed.
- Ruff format: `43/43` files already formatted.
- Shell syntax: `14/14` Step4-Pro-Latest scripts passed.
- `git diff --check`: PASS, exit `0`.
- DeepEP remains frozen at `0/116`; no retry occurred.
- Evidence:
  - `/data/ycfeng/tmp/step4_final_focused_post_format_20260815.log`,
    SHA256
    `347b251905042f5d2e75f99fc7d7bf6a80b18d50f1cfa6306df3cee27398b357`;
  - `/data/ycfeng/tmp/step4_final_formatted_contract_tests_20260815.log`,
    SHA256
    `49ddabed431e511c9d109835fa38adf7f80b0fcbeb6bf3e7451f8fb560834eb4`;
  - `/data/ycfeng/tmp/step4_final_ruff_check_post_format_20260815.log`,
    SHA256
    `82b3e6a6c090a57601d22943bd23fca9218d1031dbe5a7b754092f9a156b4f18`;
  - `/data/ycfeng/tmp/step4_final_ruff_format_post_format_20260815.log`,
    SHA256
    `d6675b63a4767317d34641fb3901c01a3e6f8536bb9289fd7d0bdf7ae2bfe59b`;
  - `/data/ycfeng/tmp/step4_final_shell_syntax_20260815.log`,
    SHA256
    `2c317ea048f882d1d56062bca99c048b57d99602f684110c8a36e56d8cd9e3f9`;
  - `/data/ycfeng/tmp/step4_final_git_diff_check_20260815.log`,
    SHA256
    `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

## 2026-08-15 — Full-MFA QKV collection and post-collection matrix refresh

**Status:** Full-MFA QKV PASS; SWA QKV and DeepEP remain explicit blockers.

### Motivation

Continue the next measurable op after DeepEP was frozen, while avoiding the
unapproved SWA annotation overlay. Full MFA and SWA have separate pinned vLLM
providers, so Full-MFA data can be measured independently without changing or
replacing the SWA implementation.

### Expectation

- Run only `vllm_step4pro_k_norm_rope` through the pinned Full-MFA path.
- Measure all `75` token points through `65,536`.
- Preserve SWA as missing rather than silently accepting a substitute.
- Canonicalize the Full-MFA rows, prove exact consumer equality, then rerun
  coverage and the complete MTP-off matrix.
- Do not launch or modify DeepEP.

### Method

- Added `PROVIDER_CORE_SLICE=qkv_full` to the existing B300 provider runner.
- Added a contract test requiring exactly one Full-MFA smoke case and `75`
  full cases, with only the QKV dataset expected for this slice.
- Ran B300 smoke and full collection against pinned commit
  `607d1641ee3fec43653fca510d717725828890c2`.
- Converted the accepted `75` rows to canonical Parquet and queried every row
  through the unchanged AIC consumer.
- Re-ran the exact SILICON coverage validator and the smoke/full MTP-off
  requirements driver.
- Ran the final focused local suite under `MemoryMax=2G`.

### Result

- The first `qkv_full` smoke kernel succeeded but host validation failed
  because the expected-file map still required grouped/router files.
  Motivation for the fix: partial slices must validate only files they own.
  Expected result: the QKV-only slice passes without weakening full-suite
  validation. Method: filter the expected map by `PROVIDER_CORE_SLICE`.
  Result: RED→GREEN contract passed; the repeated smoke and full run passed.
- Full-MFA smoke: `1/1`, latency `0.028218666712443035 ms`.
- Full-MFA full collection: `75/75`, token range `1–65,536`, latency
  `0.029557332396507263–6.715685526529948 ms`, duplicate keys `0`.
- Canonical consumer: `75/75` exact silicon, maximum absolute error
  `0.0 ms`.
- Canonical Parquet SHA256:
  `7091c0da8d445025df9e98547c2bc50c7f99276bee014212c1f4fd651b6789c1`.
- SWA QKV remains `0/75`; total QKV family coverage is `75/150`.
- DeepEP remains `0/116`; no DeepEP process, Docker container, RJob, Replica,
  or GPU allocation was started for DeepEP.
- Updated coverage: `36,420` records, `17,060` exact silicon, `15,160`
  non-silicon, `4,200` missing, `0` errors, `5` missing contracts.
- Full matrix remains intentionally blocked: prefill `72/72`, decode `84/84`,
  expected exit `2`.
- Prefill known first-chunk partial latency:
  `98.29272912678272–8082.57918903562 ms`.
- Decode nested batch-1 candidate partial latency:
  `38.02761769329299–250.394471609641 ms` across `84` candidates.
- Focused regression: `134 passed` in `5.95s`; log SHA256
  `a99c3b07b413b6155d33ea4f79fb4d2222233709d662983429c767cb181926f3`.
- Evidence:
  - `/data/ycfeng/tmp/step4_aic_provider_core_b300_20260815/smoke_s4p-aic-qkvfull-0815-050738/`;
  - `/data/ycfeng/tmp/step4_aic_provider_core_b300_20260815/full_s4p-aic-qkvfull-0815-051119/`;
  - `qkv_full_b300_consumer_validation_2026-08-15.json`;
  - `aic_silicon_coverage_2026-08-15.json`;
  - `mtp_off_requirements_full_2026-08-15.json`.

## 2026-08-15 — Corrected Attention canonicalization and MTP-off matrix closure

**Status:** PASS for available data and driver execution; formal latency remains
`BLOCKED` by QKV and DeepEP.

### Motivation

Finish the current Attention recollection attempt, then continue simulation
without retrying DeepEP or inserting fallback data.

### Expectation

- Canonicalize `58` context and `167` generation Attention rows.
- Prove every row is consumed exactly from canonical Parquet.
- Re-run coverage and confirm only QKV plus DeepEP remain.
- Execute the smoke and full MTP-off matrices and preserve null formal results
  when exact silicon data is missing.

### Method

- Loaded all `225` measured CSV rows through the exact AIC consumer.
- Wrote Zstd Parquet files, verified round-trip row equality and unique
  persisted keys, then queried the canonical database.
- Ran focused Collector/consumer/driver tests under `MemoryMax=2G`.
- Ran the coverage validator, smoke matrix, and full requirements matrix under
  `MemoryMax=2G`.
- Did not launch DeepEP, Docker, RJob, Replica, or GPU work.

### Result

- Attention canonical validation: `225/225`, all `source="silicon"`,
  maximum absolute error `0.0 ms`.
- Context: `58` rows, `29+29` provider split, SHA256
  `9995b6a392076cfa481c943b4fceb9b7a0ade41c4c125d12be6b1d3930f46ce8`.
- Generation: `167` rows, `71+96` provider split, SHA256
  `bde836b884b410b21284645ab45f0a35f82d03cac5b91299b256823067cb14c0`.
- Final focused verification: `106 passed` in `3.86s`.
- Ruff check and format: `36` changed Python files passed.
- Coverage: `36,420` records, `16,660` exact silicon, `15,160`
  non-silicon, `4,600` missing, `0` errors, `6` missing contracts.
- Smoke matrix: prefill `2/2 BLOCKED`, decode `1/1 BLOCKED`, expected exit `2`.
- Full matrix: prefill `72/72 BLOCKED`, decode `84/84 BLOCKED`, expected
  exit `2`.
- Prefill peak HBM: `142.00439453125–776.97998046875 GiB`; OOM `48/72`
  at the `241.734375 GiB` utilization limit.
- Decode batch-1 HBM: `138.58154296875–294.77685546875 GiB`; OOM `16/84`.
- Prefill known first-chunk partial latency:
  `95.65944916742194–7948.26547850502 ms`.
- Decode known batch-1 partial latency:
  `37.436471045362836–249.80332496171084 ms`.
- One metrics-extraction helper initially used obsolete memory keys
  (`model_weights`, `kv_cache`) and raised `KeyError`. The corrected helper
  used the actual keys (`weights`, `kvcache`) and completed; no production
  code or result changed.

## 2026-08-15 — DeepEP skip closure and next-operation handoff

**Status:** DeepEP deferred; Latest provider 65K coverage is in progress.

### Motivation

Prevent another known-equivalent DeepEP retry from delaying the remaining AIC
Collector and simulation work after the owner explicitly directed the task to
continue with the next operation.

### Expectation

- Preserve the failed DeepEP result as `0/116` accepted rows.
- Do not substitute historical H800, generic communication, or synthetic data.
- Continue the next incomplete provider work, beginning with the requirements'
  `65536`-token workload point.

### Method

- Reconciled the current process state and confirmed no DeepEP or B300 child
  process is active.
- Retained the existing cross-node NVSHMEM/shared-host-SHM failure evidence.
- Made the Latest-only 65K provider case contract the active TDD slice; the
  global GEMM population and unapproved QKV runtime overlay remain unchanged.

### Result

- DeepEP measurement is closed for the current phase at `0/116`.

## 2026-08-15 — B300 grouped/router 65K coverage closure

**Status:** PASS; grouped `wo_a` and FP32 router now include the required
`65536`-token point.

### Motivation

Close the remaining provider-core workload gap after DeepEP was deferred,
without changing generic GEMM coverage or entering the unapproved QKV runtime
overlay.

### Expectation

- Restore the vLLM checkout from the bounded DeepEP-only commit to pinned
  commit `607d1641ee3fec43653fca510d717725828890c2`.
- Run B300 predict-only before allocation.
- Measure exactly `75` grouped and `75` router cases with zero errors.
- Add only the `65536`-token physical key to each existing 74-row canonical
  table.
- Reproduce all `150` measured values through the canonical AIC consumers
  with `source="silicon"`.

### Method

- Restored the detached vLLM checkout to the authoritative pinned commit.
- Ran the handbook B300 predict-only recipe with one GPU, 16 CPUs, 131072 MiB,
  quota `b300_train_infra`, tag `B300`, and the pinned runtime image.
- Ran `MODE=full PROVIDER_CORE_SLICE=grouped_router` through the pinned source
  identity probe and provider collector.
- Validated runtime identity, positive finite latency, exact row counts,
  unique physical keys, and exact consumer queries.
- Compared old and new canonical key sets before writing Parquet: 74 kept,
  one `65536` key added, zero removed for each family.

### Result

- Predict-only: PASS, 10 candidate B300 nodes.
- Full collection:
  - grouped `75/75`;
  - router `75/75`;
  - Collector errors `0`;
  - profiling elapsed `23.36 seconds`.
- Runtime calls:
  - grouped: `300` timed `torch.einsum` calls;
  - router: `375` calls through
    `torch.ops.vllm.optimus_matmul_fp32` and
    `torch.ops.OptimusMoe.matmul_fp32`.
- Exact source consumer validation: `150/150`, all silicon.
- Canonical consumer validation: `150/150`, all silicon.
- Duplicate physical keys: `0`.
- Maximum absolute/relative query error: `0.0 ms` / `0.0%`.
- Combined latency range:
  `0.009082666908701261–2.518085320790609 ms`.
- New `65536`-token latency:
  - grouped: `2.518085320790609 ms`;
  - router: `0.539962649345398 ms`.
- Canonical grouped Parquet:
  `4761` bytes, SHA256
  `78fdd68077e3fdaa4c4fa349ab0a72407e0421ae09ecfcfcd4a5ae22d103d760`.
- Canonical router Parquet:
  `4755` bytes, SHA256
  `3ec22771fa8577dec8b250ca1d6152552a8091b9502c1732461df65f5dc97af9`.
- Exact cleanup: RJob `0`, Replica `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_provider_65k_b300_20260815/`.
- The next required populations are grouped `75`, router `75`, and QKV `150`.
- No GPU job, Docker container, fallback dataset, or QKV overlay was started.

## 2026-08-13 — Initial audit

**Status:** blocked at requirements-confirmation gate.

### Motivation
Establish an isolated task record and verify the authoritative inputs before changing code or allocating B-card resources.

### Method
- Created this task directory and the required planning/archive documents.
- Read the supplied requirements document.
- Read the two historical task directories and their planning/profiling records.
- Read the GPU, Docker, and local environment handbooks.
- Inspected Git status, branches, local references, and Collector operation-development rules.

### Result
- Historical methodology and current repository constraints are recorded.
- The requirements document specifies B300 and fixed vLLM commit `607d1641ee3fec43653fca510d717725828890c2`.
- The user request specifies an unspecified latest B-card vLLM image and `step4-pro-latest`.
- The two linked machine-readable/reference files named by the requirements document are missing at their expected paths.
- A bounded local Docker image listing did not reveal a matching cached Step4/latest/B300 image. This does not establish registry absence.
- No code, GPU allocation, Docker launch, or collection has been performed.

### Next required action
Obtain the exact latest image/source identity and confirm whether the fixed requirements-document commit remains authoritative for this task.

## 2026-08-13 — Access probe and blocker quantification

**Status:** still blocked. Four blockers are now measured rather than suspected.

### Motivation
The initial audit left three questions inconclusive: whether B-card capacity is actually grantable, whether the pinned vLLM source is reachable, and whether the locally cached StepCast image could serve as the implementation source. Each had to be settled with evidence before either escalating to the user or starting work, because a wrong assumption on any of them would silently invalidate the whole operation set.

### Expectation
Either find at least one viable path to the target implementation and B-card hardware, so execution could begin, or produce citable evidence that no such path exists, so the escalation is actionable rather than a bare refusal.

### Method
- Probed cluster access with `rlaunch --predict-only` across B-card tag spellings and across every B300 quotagroup, with `codesign` plus H800 as the control case.
- Enumerated B300 hardware with `brainctl get nodes -l GPUType=B300 -o json` and tallied nodes, GPUs, and quotagroup ownership. Used the JSON form after observing that the plain-text listing returns a sample rather than the full set.
- Inspected a B300 node's labels with `brainctl describe node` to establish ownership.
- Tested vLLM repository authentication with `ssh -T` and `git ls-remote`, and checked `~/.ssh/` for usable keys.
- Inspected the cached StepCast post15 image's installed vLLM tree for the three implementation files named in requirements section 2.1, and read its model registry mapping.
- Probed the internal registry catalog endpoints unauthenticated.
- Compared the requirements shape against all three existing AIC Step4-Pro model configurations.
- Verified Docker daemon reachability through `sudo`.

### Result
- B300 exists but is not grantable. 20 nodes and 160 GPUs are present, all owned by `b300_*` quotagroups; every one returns HTTP 403 for this account. Every B-card tag spelling returns `no machine available`, as does pinning the grantable `codesign` quota to a B300 node. The H800 control case succeeds, which confirms the probes are valid. Recorded as ISSUE-002.
- The pinned vLLM repository is reachable over the network but rejects authentication, and no private key exists locally. Recorded as ISSUE-003.
- The cached post15 image lacks `step4pro.py` and `optimus_fa4.py`, and its registry routes `Step4ForCausalLM` to the Step3p5 implementation. It cannot serve as the implementation source. This converts the earlier inconclusive local-cache note into a measured result. Recorded as ISSUE-004.
- The requirements shape differs from the existing AIC `Step4-Pro-V4` on every load-bearing dimension, so the model-definition decision is undecidable without user confirmation. Recorded as ISSUE-005.
- Docker through `sudo` works and the mirror is configured, so containerization itself is not a blocker.
- Two delegated history-review agents returned only idle notifications and no reports. Their scope was covered directly instead, by reading the H800 coverage manifests, the prior task's notes and progress entries, and the AIC model and collector sources. The only item not recovered is the prior task's verbatim collector command lines, which are not needed until B-card access exists.
- No code was changed, no GPU was allocated, and no collection was run. One empty duplicate task directory created in error during this session was removed immediately; no pre-existing content was touched.

### Next required action
Escalate the four measured blockers to the user with the platform ticket packet at `b300_quota_ticket.md`. Execution stays halted pending B300 quotagroup membership, vLLM credentials or a correct image reference, and confirmation of the model identity and shape.

## 2026-08-13 — Latest identity and access revalidation

**Status:** paused for explicit scope confirmation; no production code or formal collection started.

### Motivation
The previous stop record contained conclusions that could have become stale after
the B300 quota and GitLab access changed. Continuing from those conclusions would
have violated the no-assumption rule.

### Expectation
Recheck the exact source/image identity and B300 availability, then either close
the access gate with evidence or stop with only the remaining semantic decisions.

### Method
- Cloned the latest `xwx/step4pro-fa3-optimus` branch read-only and recorded its
  current SHA.
- Checked ancestry against the requirements commit
  `607d1641ee3fec43653fca510d717725828890c2`.
- Read the branch's B300 scripts and resolved the referenced image tag,
  manifest digest, and config digest with read-only Docker registry inspection.
- Verified the target Step4Pro source files and model registry entry.
- Re-ran B300 predict-only for a two-replica, eight-GPU-per-replica request.

### Result
- Latest source SHA: `9bfd9a610ea4f2890010702ee7a207cf25edf8de`.
- Requirements SHA is an ancestor; the latest delta adds B300
  DeepEP/NVSHMEM bootstrap logic.
- Candidate image:
  `hub.stepfun-inc.com/stepcast/stepcast:2026-08-06-server-vllm-test-0.19.0.post20.dev26.gc820e5ae1.precompiled`,
  manifest digest
  `sha256:70492b0c79e2286b6ee56973f5f3322b53d293fc9332c4f792e56209a34d182b`,
  config digest
  `sha256:48253062026862b2921f354613d3e9a7255e9540e3a7bf9148c9c80ec2d72661`.
- B300 predict-only succeeds through `b300_train_infra`.
- The remaining blocker is not access: the user must confirm whether the
  requirements' 78-layer synthetic shape remains the experiment contract,
  what AIC model identity to register, and whether Optimus/DeepEP internal
  kernels remain provenance under existing logical op families or require new
  consumer-visible op types.

## 2026-08-14 — User supplied the pinned vLLM checkout

**Status:** paused at the semantic scope-confirmation gate.

### Motivation
The user added the local vLLM checkout path, removing the earlier source-location uncertainty.

### Expectation
Record the checkout as an auditable source input without silently resolving the remaining runtime, model-shape, or operation-boundary decisions.

### Method
- Verified the checkout exists at `/data/ycfeng/stepfun-performance-optimization/aiconfigurator-step4-pro/vllm-step4-pro`.
- Verified the checkout is detached at `607d1641ee3fec43653fca510d717725828890c2`, matching the requirements document's pinned commit.
- Compared the pinned checkout's model registry and Step4Pro implementation files with the requirements source list.
- Kept the later branch-head/image candidate as a separately recorded reference, not an implicit replacement.

### Result
- The source path and exact commit are now recorded in the task requirements and notes.
- The pinned checkout exposes `Step4ProForCausalLM` and the target implementation files.
- No AIC production code, Collector code, GPU job, dataset, or simulation was started.
- One decision remains before implementation: which source/runtime identity defines `step4-pro-latest` when the user wording says "latest" but the requirements document pins commit `607d1641ee3fec43653fca510d717725828890c2`.

## 2026-08-13 — Choice A recorded and operation inventory completed

**Status:** source/runtime gate completed; AIC identity decision pending.

### Motivation
Apply the user's choice without silently changing the pinned source or existing Step4-Pro-V4 identity.

### Expectation
Close the source/runtime gate, derive the actual prefill/decode logical graph, and identify consumer gaps before production edits.

### Method
- Recorded choice A in `requirements.md`, `plan.md`, `notes.md`, `issues.md`, and `design.md`.
- Audited pinned Step4Pro, Optimus FA4, Optimus FP8 MoE, inherited Step3p5 execution, and AIC Python consumers.
- Separated logical AIC operations from custom kernel/provider identities.
- Verified the pinned scripts declare the previously inspected B300 image tag.

### Result
- Authoritative source: pinned local commit `607d1641ee3fec43653fca510d717725828890c2`.
- Authoritative runtime image: pinned-script tag with manifest digest `sha256:70492b0c79e2286b6ee56973f5f3322b53d293fc9332c4f792e56209a34d182b`.
- Prefill/decode logical graph and provenance are recorded in `findings.md` and `op_provenance.md`.
- Optimus kernels remain provider provenance under logical ops.
- Two material gaps are explicit: vLLM DeepEP consumer/data contract and native Step4Pro MTP1 construction.
- No production code or GPU collection was started.

## 2026-08-13 — Canonical AIC identity selected

**Status:** model identity resolved; missing-manifest decision pending.

### Motivation
Prevent collision with the existing, materially different Step4-Pro-V4 model.

### Expectation
Fix one stable model path before writing config, tests, Collector cases, or coverage manifests.

### Method
Presented the separate canonical identity `stepfun-ai/Step4-Pro-Latest` as the recommended choice.

### Result
The user selected `stepfun-ai/Step4-Pro-Latest`. Existing Step4 model identities remain unchanged. No production code was modified.

## 2026-08-13 — Missing-manifest search and worktree safety audit

**Status:** paused at Q3; no production code or B300 collection started.

### Motivation
Resolve whether the referenced machine-readable shape contract exists anywhere
in the available workspace, and determine whether implementation can safely
start in the current repository state.

### Expectation
Either recover the exact original manifest and verify its stated SHA256, or
prove that a user decision is required. Separately, establish whether a clean
isolated implementation branch can preserve the uncommitted Step4 foundation.

### Method
- Delegated a recursive ordinary-file filename search under
  `/data/ycfeng/stepfun-performance-optimization`.
- Inspected linked-worktree metadata, ignore rules, current HEAD coverage, and
  the tracked/untracked state without modifying any files outside this task
  record.

### Result
- Neither `step4pro_v4_shape_manifest.json` nor
  `step4pro_v4_vllm_b300_8bit_task.md` exists in the searched tree.
- Search evidence is stored at
  `/data/ycfeng/tmp/step4_manifest_search.txt`.
- The original manifest SHA256 cannot be verified from available files.
- This checkout is already a linked worktree, but it contains 37 modified
  tracked files plus many untracked historical artifacts.
- A new worktree from HEAD would omit the untracked V3/V4 configs and tests
  needed as implementation references; `.worktrees/` is also not ignored.
- No reset, stash, commit, worktree creation, code edit, GPU allocation, or
  collection was performed.

## 2026-08-13 — Manifest reconstruction decision

**Status:** Q3 resolved; production implementation remains paused at the
worktree-safety gate.

### Motivation
Close the missing machine-readable shape contract without pretending that the
unavailable original file or SHA256 has been recovered.

### Expectation
Obtain an explicit owner decision on whether to reconstruct or wait.

### Method
Presented reconstruction as choice A and exact-original recovery as choice B.

### Result
The user selected A. The task will reconstruct the canonical manifest from the
requirements document and pinned source, compute a new SHA256, and mark the
artifact as a reconstruction. No manifest file or production code has yet been
written.

## 2026-08-13 — AIC fidelity-gap audit

**Status:** read-only audit complete; Q4 is now the active design gate.

### Motivation
Determine whether the existing Step4 schema and leaf operations can represent
the pinned Full-MFA/SWA-GQA, latent-MoE, DeepEP, shared-KV, and MTP1 execution
without silent approximation.

### Expectation
Identify the smallest consumer-visible changes required before writing RED
tests or production code.

### Method
Delegated a bounded comparison of the pinned vLLM execution path against the
current AIC Step4 config, operation graph, Attention/MoE consumers, and MTP
handling.

### Result
- Current config cannot express Full MFA and SWA GQA in one model.
- Current Full MFA graph collapses low-rank/shared-KV/grouped-output behavior
  into ordinary Q/K/V/O projections.
- Current latent-MoE decode path assumes overlap not established by the pinned
  source and does not consume vLLM DeepEP data.
- Current MTP handling is a uniform scaling approximation rather than the
  required explicit MTP1 graph.
- The smallest audited extension is a heterogeneous latest config plus
  grouped-GEMM, DeepEP dispatch/combine, KV-layout, FP32-router, and explicit
  MTP1 contracts while reusing existing operations elsewhere.
- Evidence:
  `/data/ycfeng/tmp/step4_latest_aic_gap_audit.txt`
  (`sha256:433c39c7dc1cf60b5dcef75a4053087a38a7e0b520071371d100bc3dd0860351`).
- No production file, Collector case, GPU job, dataset, or simulation was
  modified or started.

## 2026-08-13 — Full-MFA profiling provenance clarification

**Status:** Q4 remains open; measurement provenance is now explicit.

### Motivation
Answer whether the proposed profiling graph is directly produced by the
pinned Step4-Pro vLLM implementation.

### Expectation
Distinguish the completed source-level inventory from runtime profiling
evidence that has not yet been collected.

### Method
Reconciled the proposed graph with the recorded pinned-source call paths and
defined the acceptance gate for formal B300 rows.

### Result
- The current table is a logical AIC graph extracted from
  `Step4ProDecoderLayer` and the Full-MFA forward path in pinned vLLM.
- It is not an automatically exported B300 profiler graph, and no B300
  Full-MFA profiling has yet run.
- Before accepting Collector cases or data, the actual pinned-image forward
  path must be runtime-profiled and reconciled for operation order, shapes,
  dtypes, fusion, optional branches, backend choice, and kernel provider.
- Optimus FA4 and grouped `wo_a` must be measured through their actual
  vLLM/provider paths. Generic substitution or multiplied estimates are
  prohibited.

## 2026-08-13 — Operation-boundary option A confirmed

**Status:** design direction confirmed; production implementation remains
paused at Q5.

### Motivation
Close the critical architecture question raised by the fidelity audit.

### Expectation
Choose between minimum fidelity-preserving extensions, known approximation, or
a broad framework refactor.

### Method
Explained the Full-MFA composite graph, direct pinned-vLLM provenance,
runtime-trace gate, shared-KV contract, and grouped `wo_a` measurement path.

### Result
The user confirmed option A. The implementation scope is now the minimum
explicit extension set recorded in `requirements.md` and `design.md`. No
production code, branch, commit, Collector case, GPU job, or dataset was
created.

## 2026-08-13 — Approved branch creation and Git LFS recovery

**Status:** branch created; baseline checkpoint selection is in progress.

### Motivation
Implement the approved isolation strategy without altering or losing the
existing Step4 working state.

### Expectation
Create `task/step4-pro-latest-b300`, then checkpoint the relevant baseline with
all repository hooks passing.

### Method
- Ran `git switch -c task/step4-pro-latest-b300`.
- Investigated the nonzero result from the repository `post-checkout` hook.
- Verified the hook itself was intact and the host lacked the `git-lfs`
  executable.
- Installed Ubuntu package `git-lfs=3.4.1-1ubuntu0.4` from the company
  Artifactory mirror.
- Re-ran the existing hook against the current revision.

### Result
- Branch `task/step4-pro-latest-b300` exists at
  `56c606a171b5ed5e132301490aba0fd33fbf458d`.
- The first branch-switch command returned exit code 1 only because the
  post-checkout hook could not find `git-lfs`; the branch switch itself had
  completed.
- `git lfs version` now reports `git-lfs/3.4.1`.
- The unchanged repository `post-checkout` hook now exits 0.
- No hook was deleted or bypassed, and no stash, reset, removal, move,
  baseline commit, production edit, or GPU action occurred.

## 2026-08-13 — Baseline checkpoint option A approved and staged

**Status:** exact baseline file set staged; commit verification is in progress.

### Motivation
Preserve the existing Step4 foundation on the new task branch before adding
Step4-Pro-Latest behavior.

### Expectation
Stage only relevant source/config/tests/task documentation and keep generated
outputs, caches, historical result data, H800 datasets, and the pinned vLLM
clone outside the checkpoint.

### Method
- The user selected branch/checkpoint option A.
- Delegated a read-only boundary audit of 37 tracked changes and 1,293
  untracked entries.
- Staged all 37 modified tracked files.
- Staged 4 untracked source/config files, 30 untracked test files, 14 current
  task documents, and the authoritative requirements document.
- Used exact path lists; did not use `git add .`, `git add -A`, or broad
  directory staging.

### Result
- Exactly 86 files are staged.
- No generated profiler output, cache, H800 performance dataset, historical
  task artifact directory, or `vllm-step4-pro` file is staged.
- No commit has yet been created; staged-diff checks remain pending.

## 2026-08-13 — Baseline checkpoint committed and focused tests failed

**Status:** implementation paused for systematic root-cause analysis.

### Motivation
Verify that the checkpointed Step4 foundation is internally consistent before
adding Step4-Pro-Latest behavior.

### Expectation
All selected Step4/Collector/runtime-spec baseline tests pass.

### Method
- Verified exactly 86 staged files, zero unstaged tracked files, no excluded
  paths, and no whitespace errors.
- Created commit `4f2b0c31` with message
  `chore: checkpoint existing Step4 support`.
- Ran 987 focused tests with Python 3.11.15 under a 4 GiB systemd memory scope.

### Result
- Checkpoint commit succeeded.
- Test result: 859 passed, 128 failed, duration 69.92 seconds.
- Failure distribution:
  - factorized-attention runtime spec: 79;
  - Step4-Pro-V1 model: 41;
  - Step4-Pro-V1 roofline: 4;
  - DeepSeek-V4 runtime spec: 4.
- Raw log:
  `/data/ycfeng/tmp/step4_latest_baseline_pytest.log`
  (`sha256:5a8d406abc9e382322ef9ecc973c9a579fad415c1c66a854682a50f5d2f25289`).
- No Step4-Pro-Latest production implementation has started.

## 2026-08-13 — Security-review file decision

**Status:** security follow-up closed by explicit owner decision; baseline
contract investigation remains active.

### Motivation
A read-only audit reported that one temporary evidence file might contain
environment variables, while repository rules prohibit deletion without owner
approval.

### Expectation
Obtain an explicit retain/delete decision without reading or exposing the file.

### Method
Presented deletion plus credential rotation as option A and retain/no-action
as option B.

### Result
The user selected B and stated that the current environment is sufficiently
secure. The file will not be read, deleted, modified, moved, permission-changed,
quoted, or otherwise processed. No credential action will be taken.

## 2026-08-13 — Q6 scope clarified against pinned vLLM

**Status:** awaiting confirmation of the clarified combined decision.

### Motivation
The original Q6 wording mixed a legacy `Step4-Pro-V1` baseline repair choice
with the separate requirement that `Step4-Pro-Latest` follow the actual pinned
vLLM operation graph.

### Expectation
Make it explicit that no legacy V1 choice can replace or simplify the
Step4-Pro-Latest graph extracted from vLLM.

### Method
- Rechecked the existing pinned-source operation inventory for commit
  `607d1641ee3fec43653fca510d717725828890c2`.
- Compared the two Q6 descriptions with the active attention construction and
  forward paths in `vllm/model_executor/models/step4pro.py`.
- Recorded the clarified user intent and scope boundary in `requirements.md`.

### Result
- The pinned vLLM uses heterogeneous attention: shared-KV Full MFA/MQA on full
  layers and sliding-window GQA/SWA on the other layers.
- This is closest to the shared-MFA wording of Q6 option B, but Q6 itself is
  only about repairing the pre-existing V1 baseline.
- The recommended combined decision is to preserve the historical V1 contract
  while implementing Latest independently and strictly from pinned vLLM.

## 2026-08-13 — Q6 combined contract confirmed

**Status:** resolved; baseline repair can proceed after obsolete-test ownership
is explicitly settled.

### Motivation
Remove ambiguity between preserving the existing V1 model and implementing the
new Latest model from the actual vLLM graph.

### Expectation
Keep the two model identities independent so legacy compatibility cannot alter
Latest fidelity.

### Method
Presented the pinned-source result and asked for one combined decision instead
of treating V1 and Latest as competing architectures.

### Result
The user explicitly confirmed:

- V1 keeps its historical contract.
- Latest strictly follows the pinned vLLM implementation.

No production code was changed as part of this clarification.

## 2026-08-13 — Obsolete-test and formula-loader audit completed

**Status:** deletion permission pending; no test or production file changed.

### Motivation
Separate mistakenly checkpointed withdrawn tests from valid V1 regression
tests before repairing the baseline.

### Expectation
Remove only tests whose entire contract was withdrawn, retain tests that
enforce the confirmed V1 behavior, and fix the production root cause rather
than weakening assertions.

### Method
- Audited failure names, historical task decisions, checkpoint file ancestry,
  and the relevant `dsv4.py` query ordering.
- Compared each failure cluster with the newly confirmed V1/Latest boundary.

### Result
- The 79 factorized-attention and 4 DeepSeek-V4 runtime-spec failures come from
  two files newly and mistakenly added in checkpoint `4f2b0c31`; the historical
  task explicitly withdrew that migration.
- The four V1 roofline failures are valid regression tests and must remain.
- Their root cause is an unconditional `load_data()` call before the
  `SOL`/`SOL_FULL` formula-only branch.
- Deleting the two obsolete files now requires explicit owner permission.

## 2026-08-13 — Baseline repair and strict Latest fidelity approved

**Status:** approved; execution started.

### Motivation
Close the final baseline ownership gate and make the vLLM implementation the
explicit end-to-end standard for Latest definitions, tests, measurements, and
validation.

### Expectation
Remove only the withdrawn tests, fix the valid V1 production defect, and reject
any Latest profiling row not produced through the pinned vLLM operation path.

### Method
The user explicitly approved deletion and repair and restated the Latest
operation-fidelity requirement.

### Result
- Permission exists to delete the two exact obsolete test files.
- The four V1 formula-only tests remain acceptance tests.
- Latest work is gated on exact pinned-vLLM operation/provider execution.

## 2026-08-13 — Obsolete tests removed and formula-loader root cause fixed

**Status:** this failure cluster is green; remaining V1 contract failures are
under analysis.

### Motivation
Restore the approved baseline boundary and ensure formula-only HCA queries do
not depend on collector data.

### Expectation
- The two withdrawn DSV4 runtime-spec files are absent.
- All four `SOL`/`SOL_FULL` no-load tests pass.
- The complete V1 roofline test file remains green.

### Method
- Deleted, with explicit owner approval:
  - `tests/unit/sdk/database/test_factorized_attention_runtime_spec.py`
  - `tests/unit/sdk/models/test_deepseek_v4_runtime_spec.py`
- Reproduced the four valid RED cases:
  `4 failed in 4.33s`.
- Moved `ContextDeepSeekV4AttentionModule.load_data()` and
  `GenerationDeepSeekV4AttentionModule.load_data()` after the `SOL` and
  `SOL_FULL` early returns in
  `src/aiconfigurator/sdk/operations/dsv4.py`.
- Re-ran the focused cases and the complete roofline file.

### Result
- Focused formula-only cases: `4 passed in 3.72s`.
- Complete V1 roofline file: `34 passed in 7.65s`.
- `git diff --check`: PASS.
- The fix changes source ordering only; empirical/silicon modes still call
  `load_data()` before accessing measured data.

## 2026-08-13 — Historical V1 contract RED baseline reproduced

**Status:** RED confirmed; exact historical implementation recovery is in
progress.

### Motivation
Verify the remaining baseline failures after removing obsolete tests and
repairing the formula-loader defect.

### Expectation
The failures should be limited to the previously identified mixed V1 attention
contract rather than Latest code.

### Method
Ran:

```bash
PYTHONPATH=src:. \
  /home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python \
  -m pytest tests/unit/sdk/models/test_step4_pro_v1.py -q
```

### Result
- `268` tests collected.
- `227 passed`, `41 failed`, duration `3.26s`.
- Failure groups cover missing historical `FullAttentionConfig` and
  `NonFullAttentionConfig`, parser validation, explicit attention runtime
  specs, independent Full/HCA geometry, and TP-sharded KV accounting.
- Raw log:
  `/data/ycfeng/tmp/step4_v1_contract_red_20260813.log`
- SHA256:
  `d4e8c6fc170f0cfd9f2052321c53f51256de8c521c5a83f1b2b40c39390354e8`
- No Latest implementation or Collector file was changed.

## 2026-08-13 — Historical V1 baseline restored and verified

**Status:** baseline gate passed; Latest RED-test planning is now unblocked.

### Motivation
Restore the user-confirmed historical V1 boundary without allowing an
unfinished shared-MFA migration to define either V1 or Latest.

### Expectation
- V1 uses standard Full Attention plus HCA with TP-sharded full K/V.
- Generic MFA support can coexist but does not define the V1 cached model.
- All adjusted focused baseline tests and static checks pass.

### Method
- Corrected the mixed test boundary:
  - the cached V1 schema test now requires historical Full/HCA;
  - the invalid V1 factorized-runtime-spec test was removed;
  - generic MFA tests no longer claim to define the cached V1 model.
- Reconstructed frozen `FullAttentionConfig` and
  `NonFullAttentionConfig` from the approved historical requirements, tests,
  formulas, and remaining model call paths.
- Restored the cached V1 JSON Full/HCA sections.
- Added dual Pro-schema parsing so historical V1 and generic MFA are explicit
  alternatives rather than fallbacks.
- Restored V1 TP divisibility checks, parameter reporting, differentiated KV
  formulas, and formula-only loader ordering.
- Ran focused RED/GREEN cycles, the full adjusted baseline, Ruff, format,
  JSON, and whitespace checks.

### Result
- V1 model suite: `267 passed in 0.40s`.
- V1 roofline suite: `34 passed in 7.65s`.
- Final adjusted baseline: `899 passed in 51.84s`, `0 failed`.
- Static validation: Ruff PASS, format PASS, JSON PASS,
  `git diff --check` PASS.
- Final log:
  `/data/ycfeng/tmp/step4_latest_baseline_repaired_final_pytest.log`
- SHA256:
  `c7a869263afd16b8694259ecafbe7df29e5a3a02320298a01e9e6009d5b68154`
- No `Step4-Pro-Latest` production implementation or B300 collection has
  started.

## 2026-08-14 — Pinned vLLM MTP1 boundary confirmed

**Status:** MTP1 scope is blocked pending owner clarification; MTP-off Latest
work is unblocked.

### Motivation
Determine whether the requirements' MTP1 graph is part of the authoritative
pinned Step4-Pro implementation before defining or measuring it.

### Expectation
Accept MTP1 only if the pinned source contains a Step4Pro-specific construction
and runtime path. Do not silently use Step3p5MTP or an AIC-only substitute.

### Method
- Audited the pinned checkout at commit
  `607d1641ee3fec43653fca510d717725828890c2`.
- Traced Step4Pro model registration, main forward construction, MTP registry,
  speculative configuration conversion, and MTP layer construction.
- Compared the result with the requirements' explicit Step4Pro MTP1 note.

### Result
- Step4Pro main forward has no native MTP predictor.
- The available MTP path is `Step3p5MTP` with `Step3p5DecoderLayer`; it is not
  a Step4Pro implementation.
- MTP1 cannot be accepted, measured, or simulated under the pinned Latest
  contract without an owner-approved source extension.
- MTP-off Latest implementation, runtime trace, collection, and simulation may
  proceed independently.
- Evidence: `/data/ycfeng/tmp/step4_mtp1_boundary_audit_20260814.txt`.

## 2026-08-14 — Baseline checkpoint freshly reverified

**Status:** ready to commit after the MTP boundary record is included.

### Motivation
Create fresh evidence immediately before checkpointing the approved V1 repair.

### Expectation
The complete adjusted baseline must remain green with zero failures and no
whitespace errors.

### Method
Re-ran the same 899-test adjusted baseline under Python 3.11.15 and a 4 GiB
systemd memory scope, then ran `git diff --check`.

### Result
- `899 passed`, `0 failed`, `61.56s`.
- `git diff --check`: PASS.
- Log:
  `/data/ycfeng/tmp/step4_latest_baseline_repaired_final_rerun.log`
- SHA256:
  `30b93e06095b32c0bc81d5b3740c2b4f4d1875414075d13f30f064e6dfc27a63`.

## 2026-08-14 — MTP1 and B300 smoke status reconfirmed

**Status:** MTP1 is absent from the pinned Step4Pro implementation; the
requirements smoke has not been completed or evidenced.

### Motivation
Confirm the two execution prerequisites before starting Latest AIC
implementation and avoid treating resource probes as a successful vLLM smoke.

### Method
- Rechecked pinned checkout commit
  `607d1641ee3fec43653fca510d717725828890c2`.
- Compared the requirements' MTP1 contract and smoke commands with the pinned
  model registry, Step4Pro model, MTP implementation, speculative conversion,
  task progress, and available B300 evidence artifacts.

### Result
- The pinned checkout has no native `Step4Pro` MTP1 path. Its only Step-family
  MTP implementation is `Step3p5MTP`, whose predictor block is
  `Step3p5DecoderLayer`; this cannot be accepted as Latest Step4Pro ground
  truth.
- B300 quota prediction, hardware probing, and cross-zone tar transport were
  verified, but the requirements' Step4Pro service smoke was not run to a
  successful, auditable result. There is no evidence of `/health`, FA4
  backend, prefill/decode requests, four-way concurrency, or the 78-layer
  target-shape smoke.
- Evidence:
  `/data/ycfeng/tmp/step4_mtp1_boundary_audit_20260814.txt`,
  `task_memory/step4pro_v4_external_simulator_requirements.md:135-191`,
  and the B300 probe artifacts under `/data/ycfeng/tmp/b300probe/`.

## 2026-08-14 — Execution scope updated by owner

**Status:** MTP-off execution is active; MTP1 structure work is deferred.

### Motivation
Remove the confirmed MTP1 source gap from the immediate critical path without
silently changing the pinned-vLLM fidelity rule.

### Expectation
- AIC Latest can be implemented and tested without an MTP1 graph.
- B300 pinned-vLLM smoke runs in parallel.
- Formal profiling and simulation remain gated by runtime evidence and
  MTP-off correctness.

### Method
- Recorded the owner's scope decision in `requirements.md`, `plan.md`,
  `harness.md`, and `design.md`.
- Split the active work into an AIC/Collector track and a B300 smoke track.

### Result
- MTP1 structure tests, measurement, and simulation are explicitly deferred.
- MTP-off Latest AIC ops, Collector, B300 measurement, correctness, and
  prefill/decode E2E simulation are now active scope.
- The B300 smoke must use the requirements' pinned image and vLLM checkout;
  mount is allowed only when platform rules permit it.

## 2026-08-14 — Pinned-vLLM smoke/runtime trace handed off

**Status:** externalized; no active local B300 smoke resource remains.

### Motivation

Let a separate session own the whole-model pinned-vLLM smoke and provider trace
while this session stays focused on AIC implementation, operation collection,
testing, and simulation.

### Expectation

- The external agent receives enough fixed identities, environment rules,
  reusable evidence, failure history, acceptance criteria, and cleanup rules
  to execute without repeating earlier failed probes.
- This session launches no pinned-vLLM whole-model smoke.
- AIC work continues without silently treating missing external trace evidence
  as a PASS.

### Method

- Interrupted and closed the active B300 smoke agent.
- Verified zero matching RJobs, Replicas, and local smoke processes.
- Preserved all existing evidence under
  `/data/ycfeng/tmp/b300_step4_smoke_20260814/`.
- Created
  `pinned_vllm_b300_smoke_runtime_trace_execution.md`.
- Updated requirements, plan, harness, and design ownership boundaries.

### Result

- Existing B300 prediction remains PASS with 10 candidate nodes.
- The most recent hardware evidence remains
  `NVIDIA B300 SXM6 AC`, `275040 MiB`.
- Whole-model smoke status remains **not run to PASS**.
- No `/health`, prefill/decode, four-concurrency, FA4 runtime, or DeepEP
  runtime result is claimed in this session.
- Current execution scope is now AIC-only.

## 2026-08-14 — External B300 execution session resumed

**Status:** Stage 0 PASS; Stage 1 in progress.

### Motivation

Execute the externally owned pinned-vLLM smoke/runtime-provider trace without
repeating the already diagnosed entrypoint and `/jobutil` failures.

### Expectation

- Prove a writable worker path and pinned runtime source identity before model
  loading.
- Fail fast on any Git, SHA256, or Python import-path mismatch.
- Leave zero matching RJobs and Replicas after every attempt.

### Method

- Re-read the handoff, parent requirements, GPU/Docker handbooks, pinned
  scripts, and prior B300 evidence.
- Verified commit
  `607d1641ee3fec43653fca510d717725828890c2`, all five pinned SHA256 values,
  and both pinned scripts with `bash -n`.
- Added the bounded wrapper
  `tests/e2e/step4_pro_latest/run_b300_source_probe.sh` and static contract
  tests.
- Replaced full Replica inventory with server-side
  `rjob.brainpp.cn/rjob-name=<job>` selection after the full query exceeded
  the required 2 GiB memory scope.
- Increased the live-launch timeout from `60s` to the configured worker-ready
  deadline after the platform launch client was interrupted while queued.

### Result

- Static wrapper contract: `4 passed` under Python `3.11.15`.
- First Stage 1 attempt created no RJob because the full Replica inventory was
  killed at the 2 GiB boundary, exit `137`.
- Second Stage 1 attempt created one RJob but the `60s` launch wrapper
  interrupted it while queued, exit `124`; explicit deletion succeeded and
  the final exact-name RJob and label-selected Replica counts were both `0`.
- No model load or provider PASS is claimed yet.

### Evidence

- `/data/ycfeng/tmp/b300_step4_smoke_20260814/source_probe_step4pro-b300-source-probe-20260814-174004/`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/source_probe_step4pro-b300-source-probe-20260814-180227/`
- `tests/e2e/step4_pro_latest/test_run_b300_source_probe_static.py`

## 2026-08-14 — Host OOM control scope reduced to 3 GiB

**Status:** Static control-path repair PASS; live runtime verification pending.

### Motivation

The owner reported a new host OOM and requested that the controller memory
scope be reduced from 5 GiB to 3 GiB, with I/O structured to avoid another
OOM.

### Expectation

- Every controller-side `brainctl` query and live launcher runs under
  `MemoryMax=3G`.
- No namespace-wide Replica inventory or whole-file in-memory handling of
  large Git/data artifacts is used.
- Cleanup explicitly deletes the RJob and verifies resource disappearance
  before stopping the launcher process group.

### Method

- Set both B300 wrappers to a 3 GiB controller scope.
- Preserved exact-name RJob queries and exact-label Replica queries.
- Kept generated patches, manifests, tar streams, and logs disk-backed under
  `/data/ycfeng/tmp`.
- Replaced `setsid bash -c ... systemd-run` with direct
  `setsid sudo -n systemd-run`.
- Reordered cleanup to delete and poll the RJob/Replica first, then terminate
  and wait for the launcher process group.
- Updated the static contract to match the direct `-- -lc
  "${remote_command}"` invocation.

### Result

- RED reproduced the old launcher form: `1 failed` at the expected
  `setsid sudo -n systemd-run` assertion.
- Full focused static suite: `10 passed in 0.05s` under Python `3.11.15`.
- `bash -n` passed for all three B300 wrapper/remote scripts.
- `git diff --check -- tests/e2e/step4_pro_latest` passed.
- No live RJob was launched by this repair step.

## 2026-08-15 — Streamed Stage 1 source-identity probe passed

**Status:** Stage 1 PASS; one-GPU smoke preparation in progress.

### Motivation

Prove the pinned Python source and image-native vLLM binaries can run together
without transferring or reprocessing the 573 MiB local Git pack.

### Expectation

- Run all controller commands under `MemoryMax=3G`.
- Transfer only the verified patch, manifests, identity pack, and remote
  script.
- Match all 2,103 pinned vLLM source hashes and the pinned Git commit.
- Import `vllm._C`, `step4pro`, and `optimus_fa4` from the intended runtime
  union.
- Leave zero matching RJobs, Replicas, and local launcher processes.

### Method

- Corrected the source probe so the live launcher runs in a background process
  group while host-side `brainctl exec` probes run.
- Added `-i` to streamed exec operations.
- Replaced the whole-repository tar transfer and repeated `pack-objects` work
  with the reusable 404 KiB payload.
- Added a fail-fast 50-character RJob-name check after the platform rejected
  an overlong name before resource creation.
- Reused the same low-memory payload in the one-GPU smoke wrapper and changed
  its remote manifest hashing to 1 MiB streaming reads.

### Result

- Live Stage 1 result: `SOURCE_PROBE=PASS`.
- Pinned source files verified: `2103/2103`.
- Pinned Git identity:
  `607d1641ee3fec43653fca510d717725828890c2`.
- B300 memory: total `275040 MiB`, free `274114 MiB`.
- Worker environment: Python `3.12.13`, torch `2.10.0+cu129`, CUDA `12.9`.
- Key source SHA256 values matched for `step4pro.py`, `optimus_fa4.py`, and
  `optimus_fp8_moe.py`.
- Final exact RJob count: `0`; final label-selected Replica count: `0`;
  matching local processes: `0`.
- Focused static suite after the one-GPU low-memory changes:
  `10 passed in 0.05s`.

### Evidence

- `/data/ycfeng/tmp/b300_step4_smoke_20260814/source_probe_s4p-src-0815-125214/`
- `tests/e2e/step4_pro_latest/run_b300_source_probe.sh`
- `tests/e2e/step4_pro_latest/remote_b300_source_probe.sh`
- `tests/e2e/step4_pro_latest/run_b300_single_smoke.sh`

## 2026-08-15 — One-GPU model smoke blocked by qy1-pt authorization

**Status:** BLOCKED before RJob creation; no model/runtime result.

### Motivation

Run the required 14-layer model smoke after its exact-resource predict-only
check passed.

### Expectation

The platform should admit the pinned recipe's two JuiceFS mounts and create a
single B300 worker with access to the fixed model and tokenizer paths.

### Method

- Ran exact-resource predict-only with 1 B300, 32 CPU, 300000 MiB memory, the
  pinned image, and both pinned mounts.
- Adapted the removed `--auto-port-num` CLI option to pod-local port `8000`;
  all HTTP requests remain inside the worker.
- Launched RJob `s4p-one-0815-130422` with the pinned mount paths.

### Result

- Exact-resource predict-only: PASS, exit `0`, 10 candidate B300 nodes.
- Live admission: FAIL before RJob creation.
- Platform error: `no right to access the bucket: qy1-pt`.
- Exact final RJob count: `0`; exact-label Replica count: `0`; matching local
  processes after cleanup: `0`.
- Model load, `/health`, requests, provider trace, and profiler capture did
  not run and remain unproven.

### Evidence

- `/data/ycfeng/tmp/b300_step4_smoke_20260814/predict_single_exact_20260815-130411/predict.log`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/single_s4p-one-0815-130422/launch.log`

## 2026-08-15 — Dummy-weight model load reached Optimus MoE runtime

**Status:** Synthetic config and model load PASS; Optimus MoE forward blocked.

### Motivation

Follow the parent requirement that the unavailable real checkpoint must not
block B300 performance tests. Use vLLM's own random-weight loader and remove
both model/tokenizer mounts.

### Expectation

- Load a reduced 14-layer Step4Pro synthetic config with
  `--load-format dummy`.
- Exercise Full MFA head-dim 512, native SWA GQA, grouped `wo_a`, Dense FFN,
  Latent MoE, and Optimus FP8/DeepGEMM.
- Use token-ID requests without tokenizer initialization.

### Method

- Added deterministic generators for 14-layer smoke and exact 78-layer target
  configs.
- Switched the one-GPU wrapper to `--load-format dummy`,
  `--skip-tokenizer-init`, token-ID requests, and no data mounts.
- Repaired the Optimus vendored `flash_attn.cute` namespace collision without
  modifying pinned vLLM source.
- Ran isolated B300 probes for torch CUDA, Triton expert counting, Optimus
  activation quantization, and post-quant CUDA behavior.

### Result

- Static config/wrapper suite: `12 passed in 0.06s`.
- Exact dummy predict-only: PASS with 10 candidate B300 nodes.
- Pinned source verification: `2103/2103`.
- Synthetic model load: PASS, `3.16 GiB`, `8.616919 s`.
- Backend selection: `OPTIMUS_FP8`; `OptimusFp8Experts` constructed.
- B300 capability: `(10, 3)`; torch arch list includes `sm_100`.
- Basic `torch.empty/zeros`: PASS.
- `count_expert_num_tokens`: PASS, total routed rows `131072`.
- `count → zeros`: PASS.
- `8192×896 Optimus per-token FP8 quant → zeros`: FAIL with
  `cudaErrorNoKernelImageForDevice`.
- The fixed 3.23.24 wheel native library is torch2.8/cu128 and cannot load into
  torch2.10/cu129 because of an undefined `c10_cuda_check_implementation`
  symbol.
- All exact post-attempt RJob and Replica counts: `0`.

### Evidence

- `/data/ycfeng/tmp/b300_step4_smoke_20260814/single_s4p-dm4-0815-135225/`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/single_s4p-dbg-0815-135721/`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/torch_arch_s4p-arch2-0815-141415/`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/optimus_quant_s4p-opq-0815-141549/`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/count_expert_s4p-cnt-0815-142453/`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/optimus_split_s4p-split-0815-143037/`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/optimus_native_s4p-lib-0815-143641/`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/quant_alternatives_s4p-qalt-0815-144832/`

### Validated repair candidates

- Pinned-vLLM Triton `per_token_group_quant_fp8`:
  `[8192,896] → scales [8192,7]`, followed by CUDA zeros PASS.
- Image `optimus-jit 0.1.10.post8+gitcfde41ba` CuTe DSL
  `per_token_group_quant_fp8`:
  `[8192,896] → scales [8192,7]`, followed by CUDA zeros PASS.
- The Optimus JIT option preserves the Optimus family for activation
  quantization and leaves the required Optimus DeepGEMM expert GEMMs
  unchanged, but pinned source currently calls the broken native op directly.

## 2026-08-14 — Complete Latest MTP-off RED contract established

**Status:** RED verified; production implementation is the next step.

### Motivation

The first RED file checked only one Full layer, one SWA layer, and a partial
MoE sequence. It could become green while still aggregating attention and FFN
families in an order that differs from the pinned vLLM decoder loop.

### Expectation

The test contract must fail until AIC provides:

- all 78 decoder layers in execution order;
- Full MFA with required inverse RoPE and grouped `wo_a`;
- SWA GQA with Q/K/V normalization;
- Dense SiTU-GLU and serial Latent MoE/shared-expert execution;
- exact FP32-router, Optimus FA4, grouped-einsum, Optimus MoE, and DeepEP HT
  identities;
- logical and page-allocated KV bytes;
- no MTP1 graph and no TP greater than one.

### Method

- Extended the reconstructed manifest with the missing provider, operation
  order, communication, and KV-layout fields.
- Strengthened `tests/unit/sdk/models/test_step4_pro_latest.py`.
- Ran the manifest test, the first missing-production identity test, Ruff, JSON
  validation, and whitespace validation.

### Result

- Manifest contract: `1 passed in 0.08s`.
- Latest identity RED: `1 failed in 0.26s`, at the expected missing
  `Step4ProForCausalLM` registration.
- Ruff: PASS.
- JSON parse: PASS.
- `git diff --check`: PASS.
- No production AIC code was changed during this RED step.

## 2026-08-15 — Latest MTP-off model/op graph completed

**Status:** Model/op graph PASS; Collector implementation is now active.

### Motivation

Implement the complete AIC graph from the pinned Step4-Pro vLLM source while
keeping the historical `Step4-Pro-V4` contract unchanged.

### Expectation

- Build all 78 layers in vLLM execution order for both prefill and decode.
- Preserve provider identities for Optimus FA4, grouped `wo_a`, FP32 router,
  Optimus routed MoE, and DeepEP high-throughput communication.
- Keep attention, dense/shared projections, FMHA, and KV cache in BF16; use
  FP8 block quantization only for routed MoE.
- Invert the nonlinear Full-plus-SWA KV curve exactly.
- Keep MTP disabled and reject TP greater than one.

### Method

- Added the independent `Step4ProForCausalLM` config/parser/registry path.
- Added provider-aware operation identities and a 78-layer interleaved graph.
- Added logical and page-allocated KV accounting.
- Reproduced the KV capacity bug with a RED test: a budget sized for 513
  tokens returned 512 because Latest was using the base linear inversion.
- Included `Step4ProLatestConfig` in the existing nonlinear binary-search
  inversion and re-ran the focused and historical suites.

### Result

- KV RED: expected `513`, observed `512`.
- KV GREEN: expected `513`, observed `513`.
- Focused Latest suite: `28 passed`, `0 failed`, `0.20s`.
- Historical Step4/Collector/performance regression: `899 passed`,
  `0 failed`, `67.69s`.
- Context operations: `1,821`; generation operations: `1,821`.
- Logical KV at 513 tokens: `132,141,056 bytes`.
- Page-allocated KV at 513 tokens: `134,742,016 bytes`.
- Evidence:
  - `/data/ycfeng/tmp/step4_latest_model_ops_green_20260814.log`
  - `/data/ycfeng/tmp/step4_latest_baseline_repaired_final_rerun_20260815.log`

## 2026-08-15 — Collector phase resumed at the provider-data contract gate

**Status:** Consumer contract design in progress; no Collector production code
changed yet.

### Motivation

Resume from commit `c3d2af05` without repeating the completed model/op graph
work, and prevent generic vLLM rows from being accepted for Step4Pro-specific
providers.

### Expectation

- Every provider-sensitive operation has an explicit perf filename, physical
  lookup key, interpolation axis, and missing-data failure behavior.
- `Step4-Pro-Latest` model cases are SM103- and pinned-runtime-specific.
- RED tests fail because the provider loaders, registry entries, case
  population, and query paths do not exist yet.

### Method

- Re-read the task plan, requirements, harness, design, issues, and prior test
  report.
- Re-read the Collector consumer-contract, identity, population, and
  deduplication rules.
- Inspected the current operation classes, vLLM registry, Step4 V3/V4 model
  cases, framework manifest, PerfDatabase load/query patterns, and B300 system
  metadata.
- Confirmed the pinned provider-sensitive operations still fail fast:
  grouped `wo_a`, FP32 router, QKV norm/RoPE, Optimus FA4, Optimus FP8 MoE,
  and vLLM DeepEP HT dispatch/combine.

### Result

- Git branch remains `task/step4-pro-latest-b300`; model/op commit is
  `c3d2af05`.
- Existing V3/V4 model cases remain H800-only and do not include Latest.
- The stock vLLM manifest entry remains unsuitable for accepted Latest data
  because it identifies neither the pinned StepCast image nor commit
  `607d1641ee3fec43653fca510d717725828890c2`.
- New provider data must remain separate from generic attention, GEMM, MoE,
  and communication tables. ISSUE-018 tracks this active contract gate.

## 2026-08-15 — First provider-data RED tests verified

**Status:** RED PASS; grouped `wo_a` production implementation is active.

### Motivation

Prove that the tests fail on the exact missing runtime-profile, model-plan,
registry, loader, and query behaviors before implementing them.

### Expectation

The failures must come from missing provider support rather than test syntax,
imports, or an unrelated environment error.

### Method

- Added focused Collector tests for the named pinned runtime profile, SM103
  model plan, non-SM103 rejection, and grouped-GEMM registry entry.
- Added consumer tests for exact grouped-GEMM provider/shape lookup,
  token interpolation, invalid token rejection, and conflicting physical keys.
- Corrected the test command to the task's verified Python 3.11.15 conda
  environment after the absent repository `.venv` entry failed before pytest.

### Result

- Collector RED: `4 failed`, all at the intended missing profile, plan,
  SM gate, and registry behaviors.
- Consumer RED: `5 failed`, all at the existing grouped-GEMM
  `NotImplementedError`.
- Python: `3.11.15`; pytest: `8.4.2`.
- Ruff lint passed. Ruff format identified only two test files needing normal
  line wrapping; those formatting-only edits were applied before production
  code.

## 2026-08-15 — Grouped `wo_a` provider-data vertical slice completed

**Status:** GREEN; ready for B300 runtime collection after the remaining
single-GPU provider collectors are implemented.

### Motivation

Replace the grouped `wo_a` fail-fast placeholder with a data path that measures
and consumes the exact pinned Step4Pro einsum without accepting generic dense
GEMM rows.

### Expectation

- Latest collection selects SM103 and the pinned StepCast runtime only.
- The default stock vLLM runtime and `--model-cases-full` behavior remain
  unchanged.
- The case getter extracts the grouped structural identity from the built
  Latest graph.
- The benchmark executes
  `torch.einsum("ngi,gri->ngr", grouped, weight)`.
- Consumer lookup requires exact provider, group count, N, K, and quant mode;
  only token count is interpolated.
- Missing, malformed, or conflicting rows fail explicitly.

### Method

- Added named runtime profile `step4_pro_latest` and exact installed-package
  validation.
- Added `Step4ProForCausalLM_cases.yaml`, the SM103 gate, and exclusion from
  default full collection.
- Added the distinct `step4_grouped_gemm_perf` registry/data contract.
- Added the graph-derived getter and direct pinned einsum benchmark.
- Added a lazy consumer loader, physical-key collision check, and bounded
  one-dimensional token interpolation.
- Fixed collector version parsing for the pinned `.g<commit>` package suffix.

### Result

- Targeted plan ops: `8`.
- Grouped getter tasks: `74`.
- Unique benchmark invocations: `74`.
- Unique persisted physical keys: `74`.
- Deduplicated tasks: `0`.
- Structural keys: `1`, exactly
  `vllm_step4pro_torch_einsum / groups=8 / n=1024 / k=4096 / bfloat16`.
- Token range: `1` to `32768`.
- Focused regression: `139 passed`, `0 failed`, `1.92s`.
- Full Collector regression: `347 passed`, `0 failed`, `29.91s`.
- SDK/database regression: `607 passed`, `1 unrelated pre-existing failure`,
  `93.82s`. The failure is the stale CustomAllReduce TP16 scaling test recorded
  as ISSUE-019; neither its production code nor test was changed.
- Ruff check: PASS; Ruff format check: PASS; `git diff --check`: PASS.
- No B300 latency row was measured in this slice; formal GPU collection remains
  pending.

## 2026-08-15 — FP32 router provider-data RED verified

**Status:** RED PASS; production collector/consumer implementation is active.

### Motivation

Replace the router's generic-GEMM placeholder with the exact pinned vLLM
custom operation rather than timing a BF16-output substitute.

### Expectation

- The source identity is
  `torch.ops.vllm.optimus_matmul_fp32`.
- Input and weights are BF16; output logits are FP32.
- Structural key fields are provider, N, K, weight dtype, and output dtype.
- Only token count may interpolate.
- Missing registry, getter, loader, and query support must be the observed RED
  causes.

### Method

- Confirmed the pinned call path:
  `Step4ProLatentMoE.gate -> FP32ReplicatedLinear.forward ->
  torch.ops.vllm.optimus_matmul_fp32 ->
  torch.ops.OptimusMoe.matmul_fp32`.
- Added Collector registry/getter contract tests.
- Added consumer tests for exact lookup, interpolation, invalid tokens, and
  conflicting rows.
- Ran the two focused test files before changing production code.

### Result

- Focused RED: `7 failed`, `13 passed`, `1.37s`.
- Failures were exactly:
  - missing `step4_fp32_output_gemm` registry entry;
  - missing `get_step4_fp32_output_gemm_test_cases`;
  - existing `FP32OutputGEMM.query` fail-fast placeholder.
- No production code was changed during the RED step.

## 2026-08-15 — FP32 router provider-data vertical slice completed

**Status:** GREEN; B300 runtime measurement remains pending.

### Motivation

Measure and consume the actual FP32 router operation used by pinned Step4Pro,
without accepting generic BF16-output GEMM rows.

### Expectation

- Case population is derived from the built Latest graph.
- The collector imports the pinned vLLM registration module and calls
  `torch.ops.vllm.optimus_matmul_fp32`.
- A runtime probe rejects non-FP32 output or unexpected shapes before timing.
- Consumer lookup requires the exact structural key and interpolates only
  token count.

### Method

- Added a separate perf filename and vLLM registry entry.
- Added 74 graph-derived token cases for the single router structure.
- Added the direct pinned custom-op benchmark with BF16 input/weights and FP32
  output checks.
- Added a lazy loader, exact query, collision rejection, and bounded
  one-dimensional interpolation.
- Removed the old fail-fast placeholder test after the real consumer became
  available.

### Result

- Getter tasks: `74`.
- Unique benchmark invocations: `74`.
- Unique persisted physical keys: `74`.
- Deduplicated tasks: `0`.
- Structural identity:
  `vllm.optimus_matmul_fp32 / N=896 / K=7168 / BF16 weights / FP32 output`.
- Token range: `1` to `32768`.
- Focused GREEN suite: `145 passed`, `0 failed`, `1.97s`.
- Full Collector regression: `349 passed`, `0 failed`, `30.88s`.
- SDK/database regression: `611 passed`, `1 unrelated pre-existing failure`,
  `93.93s`.
- The remaining failure is ISSUE-019; router-focused tests are green.
- B300 latency and power values have not yet been collected.

## 2026-08-15 — QKV norm/RoPE provider-data RED verified

**Status:** RED PASS; production collector/consumer implementation is active.

### Motivation

Keep the two pinned attention preprocessing paths distinct instead of treating
all norm/RoPE work as one generic memory operation.

### Expectation

- Full MFA uses the exact Step4Pro K RMSNorm plus tail-RoPE path:
  `vllm_step4pro_k_norm_rope`, K-only, QH64/KVH1/HD512.
- SWA uses pinned fused Q/K norm+RoPE plus Step4Pro V RMSNorm:
  `vllm_step4pro_qkv_norm_rope`, Q/K/V, QH128/KVH8/HD128.
- Structural keys require provider, normalized tensor set, Q/KV heads, and
  head dimension; only token count may interpolate.

### Method

- Read `Step4ProAttention.forward` and `_tail_rope`.
- Read `Step4ProSlidingAttention._prepare_value_for_attention`,
  `Step3p5Attention.forward`, and
  `fused_qknorm_rope_forward_impl`.
- Added Collector registry/getter tests for both exact structures.
- Added consumer tests for both structures, interpolation, invalid keys,
  invalid tokens, and conflicting rows.

### Result

- Focused RED: `10 failed`, `20 passed`, `1.51s`.
- Failures were exactly:
  - missing `step4_qkv_norm_rope` registry entry;
  - missing `get_step4_qkv_norm_rope_test_cases`;
  - existing `QKVNormRoPE.query` fail-fast placeholder.
- No production code was changed during the RED step.

## 2026-08-15 — Resumed-session Python path correction

**Status:** Resolved; no product-code impact.

### Motivation

Re-run the already established QKV RED suite before starting production
implementation.

### Expectation

Use the same `aic-step-design` environment and Python 3.11 interpreter recorded
by the preceding task session.

### Method

- The first resumed command attempted to source
  `/data/ycfeng/anaconda3/etc/profile.d/conda.sh`.
- The path does not exist on this controller and `conda` is not installed on
  `PATH`.
- Re-read the existing task log and recovered the recorded interpreter:
  `/home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python`.

### Result

- The failed command exited before pytest collection and changed no files.
- All subsequent tests use the exact recorded environment interpreter directly.

## 2026-08-15 — QKV norm/RoPE provider-data vertical slice completed

**Status:** GREEN; B300 runtime measurement remains pending.

### Motivation

Measure and consume the two exact pinned Step4Pro attention-preprocessing paths
without substituting generic RMSNorm/RoPE kernels or the unused fused-cache
path.

### Expectation

- Full MFA runs Q tail RoPE plus Optimus K RMSNorm and K tail RoPE.
- SWA runs pinned fused Q/K norm+RoPE followed by Step4Pro Optimus V RMSNorm.
- Both structures have independent exact keys; only token count interpolates.
- Population contains 148 distinct invocations and physical keys.

### Method

- Added a separate perf filename and vLLM registry entry.
- Extracted both structural identities from the built Latest graph and crossed
  each with the 74 existing token points.
- Called pinned `Step4ProAttention._tail_rope`,
  `fused_qknorm_rope_forward_impl`,
  `Step4ProSlidingAttention._prepare_value_for_attention`, and
  `OptimusRMSNorm` directly.
- Added runtime output shape/dtype probes, a lazy loader, exact query,
  collision rejection, and bounded one-dimensional interpolation.
- Removed the obsolete placeholder test after real consumer support existed.

### Result

- Getter tasks: `148`.
- Unique benchmark invocations: `148`.
- Unique persisted physical keys: `148`.
- Deduplicated tasks: `0`.
- Structural identities:
  - `vllm_step4pro_k_norm_rope / normalized=k / QH64 / KVH1 / HD512`;
  - `vllm_step4pro_qkv_norm_rope / normalized=q+k+v / QH128 / KVH8 / HD128`.
- Token range: `1` to `32768`.
- Focused GREEN suite: `155 passed`, `0 failed`, `2.06s`.
- Full Collector regression: `352 passed`, `0 failed`, `29.56s`.
- SDK/database regression: `618 passed`, `1 unrelated ISSUE-019 failure`,
  `92.27s`.
- Ruff check, Ruff format, and `git diff --check`: PASS.
- Review identified ISSUE-022: chunked-prefill peak allocation still needs an
  explicit in-flight chunk contract before formal prefill simulation. This
  does not change the provider Attention benchmark or MTP-off decode capacity
  results.
- No B300 latency or power value was collected in this slice.

## 2026-08-15 — Full-MFA Q tail-RoPE omission fixed at source

**Status:** Resolved before commit and before any B300 data collection.

### Motivation

The first GREEN benchmark timed only K normalization and K tail RoPE. The
pinned `Step4ProAttention.forward` also applies tail RoPE to all 64 Q heads, so
the incomplete benchmark would materially undercount the provider operation.

### Expectation

The Full-MFA benchmark must return and validate both
`Q[num_tokens,64,512]` and `K[num_tokens,1,512]` while executing the pinned
method for both tensors.

### Method

- Traced the operation boundary back through
  `Step4ProAttention.forward -> Step4ProAttention._tail_rope`.
- Added a RED runtime-probe shape test; it failed because the expected-shape
  helper did not exist (`1 failed`, `0.31s`).
- Added the exact Q tensor and called the pinned `_tail_rope` method directly.
- Kept K normalization on pinned `OptimusRMSNorm`; no scaling factor or
  approximation was added.

### Result

- Runtime probe now requires both Q and K output shapes.
- Provider-focused tests pass `31/31`.
- Final focused and Collector regressions pass `155/155` and `352/352`.
- The defect was corrected before dataset generation, so no stale B300 row
  requires deletion or migration.

## 2026-08-15 — Provider-attention source/consumer audit started

**Status:** In progress; no attention production code changed.

### Motivation

Continue after the QKV slice by defining exact prefill/decode datasets for both
pinned Step4Pro attention structures.

### Expectation

- Full MFA executes the pinned Optimus hd512 FA4 path.
- SWA executes the pinned native vLLM sliding-GQA path.
- Both use independent exact structural keys and provider-specific perf files.

### Method

- Read the pinned Optimus adapter, FA utility dispatcher, and
  `FlashAttentionImpl.forward`.
- Read the Latest AIC attention graph and existing provider fail-fast query.
- Read the requirements prefill/decode workload matrices and Collector
  identity rules.
- One semantic lookup initially targeted the nonexistent
  `sdk/operations/compute.py`; the actual provider operations live in
  `sdk/operations/identity.py`. The failed lookup changed no files.

### Result

- Both structures share the pinned
  `FlashAttentionImpl.forward -> fa_utils.flash_attn_varlen_func` path.
- hd512/FA4 dispatches to Optimus; hd128 dispatches to pinned
  `vllm.vllm_flash_attn`.
- Existing `ContextAttention.query` and `GenerationAttention.query` still fail
  fast whenever a provider is present, so RED consumer tests are required next.

## 2026-08-15 — Provider-attention RED verified

**Status:** RED verified; production implementation not started.

### Motivation

Prove the new tests exercise missing provider registry, population, and
consumer behavior rather than existing generic attention paths.

### Expectation

Tests must fail only because the two provider operations, workload population,
and provider data consumers do not exist yet.

### Method

- Added registry and 50-context/139-generation case contracts.
- Added exact-structure, interpolation, invalid-input, missing-key, and
  conflicting-row consumer tests.
- Removed the obsolete test that expected provider attention to remain
  permanently unimplemented.
- Ran the focused suite with Python 3.11.15.

### Result

- RED result: `17 failed`, `19 passed`, `1.87s`.
- Three failures identify missing registry/getter/cache-cap symbols.
- Fourteen failures identify the existing provider-specific
  `NotImplementedError` paths.
- No production code or Collector YAML was changed before the RED run.
- Log: `/data/ycfeng/tmp/step4_attention_red_pytest.log`.

## 2026-08-15 — Attention physical-layout root cause and RED verified

**Status:** RED verified; physical-layout correction is active.

### Motivation

The first GREEN Attention slice allocated contiguous logical K/V tensors. The
pinned hybrid allocator instead pads every Full-MFA and SWA physical page to
`524288` bytes, and the hd512 cache uses a padded block stride with aliased K/V
planes. Ignoring those facts changes both the benchmark invocation and the
generation cache-cap population.

### Expectation

- Both providers carry `NHD`, `524288` physical page bytes, and their exact
  block strides in the invocation and persisted key.
- Full MFA uses a `524288`-byte block stride and zero K/V-plane stride.
- SWA retains only the live window plus the in-flight query chunk.
- AIC reports logical bytes separately from actual physical allocation.

### Method

- Traced pinned `unify_kv_cache_spec_page_size`,
  `_reshape_attention_kv_cache`, `get_swa_materialized_block_range`, and
  `SlidingWindowManager.remove_skipped_blocks`.
- Read the pinned regression tests for hd512 padding/aliasing and SWA
  in-flight block release.
- Added focused RED contracts across model config, Attention operation
  identity, Collector population/allocation, and CSV loading.
- Ran the four focused test files with Python `3.11.15`.

### Result

- RED result: `11 failed`, `50 passed`, `1.89s`.
- Failures are exactly the absent physical-page/layout fields, absent
  materialized-block/stride helpers, stale case population, and incomplete
  persisted keys.
- Pinned SWA evidence corrects the earlier provisional `seq_len=513` estimate:
  it retains five physical blocks at 513 tokens and releases one at 640 tokens.
- No production/config/manifest code was changed before this RED run.
- Log:
  `/data/ycfeng/tmp/step4_attention_physical_layout_red_20260815.log`.

## 2026-08-15 — Authorized B300 quant overlay reached DeepGEMM unpermute

**Status:** Quant overlay PASS; pinned `ep_gather` dimension bug blocks health.

### Motivation

Apply the owner-authorized Optimus JIT quant overlay without changing model
shape or the Optimus DeepGEMM expert GEMMs.

### Expectation

The JIT quant output must use packed UE8M0 scales accepted by DeepGEMM, and
execution must continue through both grouped expert GEMMs.

### Method

- Replaced exactly five native Optimus per-token quant calls in the runtime
  copy after the `2103/2103` pinned-source check.
- Preserved the pinned source, runtime source, SHA256 values, and unified diff.
- Emitted `sm100_1d1d` packed UE8M0 scales and enabled the DeepGEMM 2.4.2 E8M0
  oracle.

### Result

- Overlay marker emitted at runtime.
- `OPTIMUS_FP8` and `OptimusFp8Experts` remained selected.
- Model load: `3.16 GiB`, `9.196790 s`.
- DeepGEMM accepted packed UE8M0 and completed both grouped FP8 GEMMs.
- The next failure is pinned `ep_gather`: `BLOCK_D=896` is not a power of two.
- The exact target latent hidden `3584` also violates the current block rule.
- Final exact RJob and Replica counts: `0`.

### Evidence

- `/data/ycfeng/tmp/b300_step4_smoke_20260814/single_s4p-ovl-0815-145936/`
- `/data/ycfeng/tmp/b300_step4_smoke_20260814/single_s4p-e8m-0815-151740/`

## 2026-08-15 — One-GPU synthetic B300 smoke passed

**Status:** PASS.

### Motivation

Complete the one-GPU pinned-vLLM provider smoke after correcting the reviewed
SM103 runtime compatibility defects.

### Expectation

Health, prefill/decode, four-request concurrency, Full-MFA FA4, Optimus
FP8/DeepGEMM, overlay evidence, and zero-resource cleanup must all pass.

### Method

- Used synthetic 14-layer config and vLLM dummy weights.
- Applied the documented quant and gather overlays after the pinned
  `2103/2103` source check.
- Enabled strict Optimus Triton driver signatures for changing batch shapes.
- Used token-ID requests without tokenizer initialization.
- Scoped Full-MFA fallback validation to the actual FA4 forward marker; SWA
  remained free to use its native provider.

### Result

- Host and remote result: PASS.
- Scheduling: `11 s`.
- Model load: `9.083845 s`, `3.16 GiB`.
- Health ready: `100 s`.
- GPU used after load: `247628 MiB`; free: `26486 MiB`.
- Single request: `13.810807 s`, prompt/output tokens `8/4`.
- Four-request wall time: `0.457171424 s`.
- Per-request latency: `0.450382`, `0.450498`, `0.451179`, `0.450456 s`.
- Full-MFA actual forward: hd512, page size 128, Optimus FA4.
- MoE: `OPTIMUS_FP8`, Optimus JIT packed UE8M0 quant, Optimus DeepGEMM.
- Exact final RJob and Replica counts: `0`.

### Evidence

- `/data/ycfeng/tmp/b300_step4_smoke_20260814/single_s4p-ok-0815-163232/`

## 2026-08-15 — Simulator physical-KV peak contract corrected

**Status:** Static implementation and regression verification complete; B300
runtime measurement remains pending.

### Motivation

Prevent AIC memory and OOM decisions from using logical KV bytes or a
point-in-time SWA residency value that can fall when an old block is released.

### Expectation

- Standard models retain their existing logical-equals-physical behavior.
- Step4-Pro-Latest reports logical, resident allocated, and peak allocated KV
  as distinct values.
- Capacity inversion uses the monotonic peak physical curve.
- Static memory/OOM and native KV estimators use peak physical bytes.

### Method

- Added RED tests for standard-model compatibility, Latest peak allocation,
  capacity inversion, backend OOM use, summary capacity context, and the
  native KV estimator.
- The RED run failed `6/260` only because the physical/peak APIs and consumers
  were absent.
- Added default physical APIs to `BaseModel`, the pinned Latest page-aware peak
  curve, bounded capacity search over model context length, and peak-byte use
  in `BaseBackend` and `KVCacheEstimator`.
- Corrected the first GREEN attempt after evidence showed that temporary
  page-granularity plateaus were being mistaken for permanent KV saturation.

### Result

- RED: `6 failed`, `254 passed`, `2.83s`.
- Intermediate GREEN: `2 failed`, `258 passed`, `0.94s`.
- Final focused GREEN: `260 passed`, `0 failed`, `0.68s`.
- At 513 tokens:
  - logical KV: `132,141,056 bytes`;
  - resident physical KV: `204,472,320 bytes`;
  - peak physical KV: `204,472,320 bytes`.
- At 640 tokens:
  - resident physical KV falls to `174,063,616 bytes`;
  - peak physical KV remains `204,472,320 bytes`.
- With a `204,472,320-byte` KV budget, the largest safe MTP-off decode length
  is `640` tokens; token `641` crosses the next physical-page peak.
- Logs:
  - `/data/ycfeng/tmp/step4_physical_kv_peak_red_20260815.log`;
  - `/data/ycfeng/tmp/step4_physical_kv_peak_green_20260815.log`.
- Final focused rerun after formatting/comment cleanup: `260 passed`, `0
  failed`, `0.79s`.
- Collector regression: `358 passed`, `0 failed`, `31.24s`.
- Broad SDK regression: `1233 passed`, `1` unrelated ISSUE-019 failure,
  `94.90s`.
- Ruff check, Ruff format, and `git diff --check`: PASS.

## 2026-08-15 — B300 Attention runtime gate resumed

**Status:** In progress; the controller has no local GPU, so the next action is
an exact B300 RJob using the pinned image.

### Motivation

Resume from commit `84021c6c` without repeating completed static implementation
or mixing the externally owned whole-model smoke into this AIC Collector lane.

### Expectation

- Execute representative Full-MFA and SWA Attention cases in the pinned image
  on B300 SM103.
- Preserve the pinned source/provider path and verify the physical cache
  stride/alias contract before accepting any latency row.
- Load the generated rows through the unchanged AIC consumer and reproduce the
  measured latency values.

### Method

- Re-read task `requirements.md`, `plan.md`, `progress.md`, `issues.md`,
  `notes.md`, `findings.md`, and `harness.md`.
- Re-read the GPU/Docker handbooks and the Collector operation-development and
  auto-collection rules.
- Verified branch `task/step4-pro-latest-b300` at `84021c6c`.
- Verified the controller host returns no GPU from `nvidia-smi -L`, while
  Docker client/server `29.1.3` and `/kubebrain/rlaunch` are available.
- Revalidated the exact runtime profile:
  `hub.stepfun-inc.com/stepcast/stepcast:2026-08-06-server-vllm-test-0.19.0.post20.dev26.gc820e5ae1.precompiled`,
  pinned commit `607d1641ee3fec43653fca510d717725828890c2`,
  quota `b300_train_infra`, tag `B300`.
- Identified the already verified disk-backed source-transfer components in
  `tests/e2e/step4_pro_latest/`; they can be reused without rerunning the
  whole-model smoke.

### Result

- Local GPU count: `0`.
- Docker client/server versions: `29.1.3 / 29.1.3`.
- Required static Attention population remains `50` context plus `149`
  generation cases.
- No runtime row has yet been accepted in this resumed session.

## 2026-08-15 — First B300 Attention smoke failed before kernel execution

**Status:** Root cause identified and the transfer contract corrected; rerun
pending.

### Motivation

Execute two providers and both phases through the actual pinned runtime before
starting the 199-case collection.

### Expectation

The transferred AIC source should import as version `0.10.0`, construct the
Latest graph, and then enter the Optimus FA4 and native SWA kernels.

### Method

- Ran one B300 SM103 worker on
  `gpu-b300-0576.qy.cnw.istep.fun`.
- Reconstructed and verified all `2103` pinned vLLM files at commit
  `607d1641ee3fec43653fca510d717725828890c2`.
- Downloaded and checksum-verified the `step-optimus 3.23.24` wheel, then
  activated the previously verified FA4 Python overlay.
- Captured the full remote exception and exact-name cleanup evidence.

### Result

- Scheduling time: `16 seconds`.
- GPU: `NVIDIA B300 SXM6 AC`, `275040 MiB`, driver `580.159.03`, compute
  capability `10.3`.
- Pinned source verification: `2103/2103` files.
- Failure:
  `importlib.metadata.PackageNotFoundError: aiconfigurator`.
- Root cause: source-only payload omitted
  `aiconfigurator-0.10.0.dist-info`, which the package import requires.
- Accepted latency rows: `0`.
- Final matching RJobs: `0`; final matching Replicas: `0`.
- Fix implemented in the harness: transfer exact AIC distribution metadata;
  no product fallback or package-source patch.
- Evidence:
  `/data/ycfeng/tmp/step4_aic_attention_b300_20260815/smoke_s4p-aic-attn-0815-155556/`.

## 2026-08-15 — Representative B300 Attention smoke and consumer gate passed

**Status:** PASS; full 199-case Attention collection is the active next step.

### Motivation

Close the runtime/provider gate before spending a longer B300 allocation on
the complete context and generation grids.

### Expectation

- Full MFA must execute pinned Optimus FA4 with hd512 and aliased K/V storage.
- SWA must execute pinned native vLLM FlashAttention with hd128/window512 and
  separate K/V planes.
- Generated rows must be loadable by the unchanged AIC consumer.

### Method

- Launched one B300 worker in the exact pinned StepCast image.
- Verified all `2103/2103` pinned vLLM source files and activated the
  checksum-verified Optimus FA4 `3.23.24` Python overlay.
- Ran context and generation for both providers.
- Pulled the CSV rows to the controller and queried each row through
  `ContextAttention` or `GenerationAttention`.

### Result

- GPU: `NVIDIA B300 SXM6 AC`, driver `580.159.03`, SM `10.3`.
- Scheduling time: `53 seconds`.
- Runtime provider evidence:
  - Full MFA: `optimus_fa4`, Q shape `[512,64,512]`, page size `128`,
    `pack_gqa=False`;
  - SWA: `vllm_native_sliding_gqa`, QH `128`, KVH `8`, HD `128`,
    window `512`.
- Measured/queried latency values:
  - Full-MFA context: `0.047450666626294456 ms` /
    `0.047450666626294456 ms`;
  - SWA context: `0.025701334079106648 ms` /
    `0.025701334079106648 ms`;
  - Full-MFA generation: `0.03164800008138021 ms` /
    `0.03164800008138021 ms`;
  - SWA generation: `0.015354666858911514 ms` /
    `0.015354666858911514 ms`.
- Consumer source for all four queries: `silicon`.
- Physical layout recorded in rows:
  - Full MFA: page/block stride `524288/524288 bytes`, alias `True`;
  - SWA: page/block stride `524288/262144 bytes`, alias `False`.
- Final matching RJobs: `0`; final matching Replicas: `0`.
- Dataset hashes:
  - context:
    `7b11ffdfa2d458620a86edca18c74d50605f7da4e5f3c2d3a8ee75077de94a93`;
  - generation:
    `2e8ffaca7f4062ad978f9da602c278a9a8fc7ab33e52ace7e7f9bd42646dae72`.
- Evidence:
  `/data/ycfeng/tmp/step4_aic_attention_b300_20260815/smoke_s4p-aic-attn-0815-160019/`.

## 2026-08-15 — First full Attention run stopped on runtime-version identity

**Status:** Root cause fixed and unit-verified; full rerun pending.

### Motivation

Collect all `50` context and `149` generation cases through the normal
Collector model plan with resume checkpoints.

### Expectation

The named `step4_pro_latest` runtime profile must accept only the exact package
installed in the pinned image and then schedule the two requested Attention
operations.

### Method

- Launched a B300 worker and completed pinned-source, AIC, and Optimus FA4
  identity checks.
- Entered `collector/collect.py` with the Latest model plan and SM103.
- Captured the exact runtime-profile exception before changing the manifest.
- Changed the focused test expectation first and reproduced RED:
  `2 failed, 1 passed`.
- Corrected only `collector/framework_manifest.yaml` and reran the complete
  provider-case test file.

### Result

- Scheduling time: `11 seconds`.
- Planned ops correctly included both required Attention operations.
- Failure occurred before any benchmark case:
  expected `0.19.0.post20.dev26.gc820e5ae1`, actual
  `0.19.0.post20.dev26+gc820e5ae1`.
- Root cause: Docker-tag spelling was incorrectly reused as the installed
  PEP 440 package version.
- Accepted full-grid rows: `0`.
- RED: `2 failed, 1 passed`.
- GREEN: `19 passed`, `0 failed`, `1.23 seconds`.
- Final matching RJobs: `0`; final matching Replicas: `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_aic_attention_b300_20260815/full_s4p-aic-attn-0815-160726/`.

## 2026-08-15 — Full B300 Attention collection and consumer validation passed

**Status:** PASS; Attention measurement is complete.

### Motivation

Complete the required provider-specific context and generation grids after
correcting the exact installed vLLM package identity.

### Expectation

- Collect all `50` context and `149` generation rows with no Collector errors.
- Preserve the pinned Optimus FA4 and native SWA provider identities.
- Reproduce every measured latency through the AIC consumer with
  `source="silicon"`.

### Method

- Ran the Latest SM103 model plan in the pinned StepCast image on one B300.
- Used the installed runtime identity
  `0.19.0.post20.dev26+gc820e5ae1`.
- Loaded all output rows through the provider-specific AIC Attention consumer.

### Result

- Context rows: `50`.
- Generation rows: `149`.
- Collector errors: `0`.
- Consumer queries: `199/199` exact matches, all `silicon`.
- Measured latency range: `0.013210666676362356–50717.3125 ms`.
- Dataset:
  `/data/ycfeng/tmp/step4_aic_attention_b300_20260815/full_s4p-aic-attn-0815-161251/`.
- Consumer evidence:
  `attention_b300_consumer_validation_2026-08-15.json`.
- Final matching RJobs: `0`; final matching Replicas: `0`.

## 2026-08-15 — Optimus FP8 MoE static preflight passed

**Status:** PASS; B300 smoke is the active next step.

### Motivation

Validate the exact `OptimusFp8Experts` collection path and its host/remote
control scripts before allocating a B300 worker.

### Expectation

- Generate exactly `174` unique EP16/EP32 cases.
- Cover balanced and two required power-law distributions.
- Keep exact provider/activation/persisted-key identity.
- Pass syntax, focused behavior, lint, format, and whitespace checks.

### Method

- Added the B300 host and remote Optimus MoE collection scripts.
- Ran `bash -n` on all related shell scripts.
- Ran the combined collection-contract and provider/consumer focused suite.
- Corrected one Ruff import-order finding and applied the repository formatter's
  exact layout.
- Re-ran Ruff check, Ruff format, and `git diff --check`.

### Result

- Shell syntax: PASS.
- Focused tests: `39 passed`, `0 failed`, `3.24 seconds`.
- Ruff check: PASS, `0` findings.
- Ruff format: PASS, `9` files already formatted.
- `git diff --check`: PASS.
- Initial local command error: repository `.venv` had Python `3.13.13` but no
  `pytest`/`ruff`; no dependency was changed.
- Root resolution: used the task's already verified environment
  `/home/i-fengyicheng/miniconda3/envs/aic-step-design`
  (Python `3.11.15`, pytest `8.4.2`, Ruff `0.14.1`).

## 2026-08-15 — Optimus MoE B300 smoke passed after two root fixes

**Status:** PASS; the `174`-case full collection is running.

### Motivation

Execute representative masked/contiguous and EP16/EP32 workloads before the
complete measurement grid.

### Expectation

- The host wrapper must invoke the shared collection script without depending
  on filesystem executable bits.
- Runtime identity must be pinned vLLM
  `0.19.0.post20.dev26+gc820e5ae1`, Step Optimus `3.23.24`, B300 SM103.
- Three representative rows must query back exactly as `silicon`.

### Method

- First launch reproduced a host-side `Permission denied` before RJob creation.
- Added a RED static assertion, then changed the Optimus wrapper to
  `exec bash <shared-wrapper>`.
- The next runtime completed the kernels but exposed a non-fatal
  `sitecustomize` import error: metadata still reported
  `step-optimus 3.17.8.dev0`.
- Added RED assertions for the pinned wheel metadata overlay and explicit
  runtime fail-fast identity check.
- Downloaded and SHA256-verified the same `3.23.24` wheel selected by the
  pinned vLLM script, extracted only its `dist-info`, preserved the
  image-native torch-2.10 Optimus shared library, and reran the smoke.

### Result

- Final worker: `gpu-b300-0393.qy.cnw.istep.fun`.
- GPU: `NVIDIA B300 SXM6 AC`, driver `580.159.03`, SM `10.3`.
- Scheduling time: `21 seconds`.
- Pinned vLLM files: `2103/2103` verified.
- Step Optimus identity: `3.23.24`.
- Native library:
  `/usr/local/lib/python3.12/dist-packages/optimus/liboptimus_ths-torch2.10-cu129.cpython-312-x86_64-linux-gnu.so`.
- Smoke rows and exact consumer values:
  - `128` tokens, EP16, balanced:
    `0.8639573256174723 ms`;
  - `6144` tokens, EP16, power-law 1.2:
    `15.19210688273112 ms`;
  - `8192` tokens, EP32, power-law 1.01:
    `9.440223693847656 ms`.
- Consumer: `3/3` exact matches, all `source="silicon"`.
- Dataset SHA256:
  `bbf9329f0693a13487ea7b33a3a7cb4382d3067d703ca466f827f956db2ceaed`.
- Final matching RJobs: `0`; final matching Replicas: `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_aic_optimus_moe_b300_20260815/smoke_s4p-aic-moe-0815-170357/`.

## 2026-08-15 — Full Optimus MoE collection exposed default CUDA RNG contamination

**Status:** Root fix implemented and locally verified; B300 full rerun pending.

### Motivation

The first `174`-case run produced only `24` rows and `150` errors. The partial
dataset is invalid and must not be copied into the canonical B300 database.

### Expectation

Preserve CUDA graph measurement and all `174` cases while preventing one
completed graph capture from breaking input construction for the next case.
The fix must not use eager fallback or alter the pinned Optimus kernel path.

### Method

- Read the complete failure stack from
  `/data/ycfeng/tmp/step4_aic_optimus_moe_b300_20260815/full_s4p-aic-moe-0815-170643/`.
- Confirmed that the first `24` EP16 balanced cases through `6144` tokens
  completed, then every later failure occurred in the next case's
  `torch.randn` before `OptimusFp8Experts.apply`.
- Added two RED behavior tests for deterministic BF16 input construction and
  preservation of the default RNG state.
- Added `_step4_optimus_hidden_states`, which creates a private fixed-seed
  `torch.Generator` on the requested device, and routed MoE input construction
  through it.
- Removed the now-unused `torch.manual_seed(42)` mutation of the default
  generator.

### Result

- RED: `2 failed` with the expected missing-helper import.
- GREEN: `2 passed`, `23 deselected`.
- Environment:
  `/home/i-fengyicheng/miniconda3/envs/aic-step-design`,
  Python `3.11.15`, pytest `8.4.2`.
- The collector still uses CUDA graph timing and the exact pinned
  `OptimusFp8Experts` implementation. No case, precision, provider, or routing
  identity changed.

## 2026-08-15 — Pinned B300 runtime-deviation review rewritten

**Status:** PASS for continued provider/performance testing; no model-quality
or training-equivalence claim.

### Motivation

Re-check whether the extra runtime fixes are actually required and avoid the
overly broad claim that every change is numerically identical or proves no
training effect.

### Expectation

- Separate contract-required fixes from optional workarounds.
- Preserve the original model/provider design.
- State exactly which changes preserve arithmetic and which change inference
  quantization.
- Confirm that the isolated runtime copy cannot modify training artifacts.

### Method

- Re-read the pinned vLLM call paths and both gather kernels.
- Compared the pinned revision with the only later branch commit.
- Re-inspected native-quant, ABI-mismatch, Triton compile, strict-signature,
  import-metadata, and successful one-GPU evidence.
- Audited the exact overlay diffs and before/after SHA256 files.

### Result

- Optimus JIT quant is required under the fixed SM103/image/DeepGEMM contract;
  the production-quality alternative is a matching native Optimus rebuild.
- The quant overlay preserves architecture/provider identity but changes FP8
  activation quant implementation and scale encoding. It is not bitwise or
  quality-equivalence evidence.
- Both gather corrections are required for 896/3584, preserve exact element
  coverage and top-k accumulation order, and affect inference tiling only.
- Strict signatures change cache identity only; `ModuleSpec` changes import
  metadata only; the fallback-gate adjustment changes validation only.
- Dummy inference writes no checkpoint, gradient, or optimizer state. It
  cannot alter an existing training result, but it also cannot validate
  training convergence or generation quality.
- Updated `requirements.md`, `design.md`, `harness.md`, `notes.md`,
  `issues.md`, `review.md`, and the pinned execution guide in place.

## 2026-08-15 — Two-node distributed environment repair passed static gates

**Status:** STATIC PASS; live DP16/EP16 run is next.

### Motivation

Both previous two-node attempts had valid platform-distributed variables in
the holder logs, but the separately started `brainctl exec` processes lost
them and failed on missing `MASTER_ADDR`.

### Expectation

- Persist the original holder environment without inventing NCCL values.
- Restore `NODE_RANK`, `NODE_COUNT`, `MASTER_ADDR`, and `PROC_PER_NODE` before
  vLLM starts.
- Request the already validated RDMA/topology resources.
- Keep controller memory at 3 GiB and exact-resource cleanup unchanged.

### Method

- Added a failing static contract for a holder-written distributed env file
  and removal of `/proc/1/environ`.
- Wrote required and filtered platform variables with shell `%q`, mode 0600.
- Added a per-replica file probe, source, and fail-fast required-variable
  checks.
- Added `rdma/mlnx_shared=8`, `--topo-group=yes`, and
  `NVSHMEM_ENABLE_NIC_PE_MAPPING=1`.
- Re-ran the focused and complete E2E static suites.

### Result

- Environment RED: `1 failed, 2 passed`.
- Environment GREEN: `3 passed`.
- RDMA/topology RED: `1 failed, 2 passed`.
- RDMA/topology GREEN: `3 passed`.
- NVSHMEM bootstrap RED: `1 failed, 2 passed`.
- Final complete E2E static suite: `16 passed`, `0 failed`, `0.04 seconds`.
- Shell syntax and whitespace checks: PASS.
- Live DeepEP HT behavior remains to be verified.

## 2026-08-15 — Optimus full dataset rejected; true pinned-op path is locally GREEN

**Status:** LOCAL PASS; replacement B300 smoke and full collection are pending.

### Motivation

Audit whether the nominally complete `174`-row Optimus dataset actually used
the same operations and execution modes as pinned
`Fp8MoEMethod.apply`.

### Expectation

- `<6144` local tokens must call
  `torch.ops.vllm.deepgemm_optimus_moe_masked_fp8` with mandatory CUDA graph.
- `>=6144` local tokens must call
  `torch.ops.vllm.deepgemm_optimus_moe_fp8` in explicit eager mode because the
  pinned implementation performs host synchronization and `.item()`.
- No graph-capture failure may silently fall back to eager.
- Each row must record the actual kernel variant and execution mode.

### Method

- Reconciled the Collector call against pinned
  `vllm/model_executor/layers/quantization/fp8.py:968-1040` and
  `vllm/model_executor/layers/fused_moe/optimus_fp8_moe.py:286-650`.
- Added and observed two RED contracts: missing branch helper and missing
  remote kernel/mode validation.
- Replaced `OptimusFp8Experts.apply` with the two exact pinned custom ops.
- Used the pinned provider's `_get_ws_size`/`_get_workspaces` implementation to
  reserve the full collection workspace before the first measured call.
- Set `allow_graph_fail=False`, selected CUDA graph only for masked, selected
  eager only for contiguous, and added strict result-mode checks.
- Added `kernel_variant`, `execution_mode`, and `used_cuda_graph` to every row
  and strengthened remote validation.

### Result

- The earlier `174` rows are rejected: `150/174` rows were measured through a
  silent eager fallback after contiguous CUDA graph capture failed.
- RED reproduced as `2 failed` for the intended missing behavior.
- Focused GREEN suite: `32 passed`, `0 failed`, `2.99 seconds`.
- Shell syntax and `git diff --check`: PASS.
- Ruff check: PASS; one quote-format finding was corrected before the final
  format rerun.
- Replacement B300 runtime evidence is still required before accepting
  Optimus data.

## 2026-08-15 — First true-op smoke exposed workspace validation unit mismatch

**Status:** ROOT FIX LOCAL PASS; replacement smoke retry pending.

### Motivation

Close the first B300 failure without weakening the full-workspace fail-fast
gate.

### Expectation

Workspace capacity must remain valid after pinned `SharedResizableBuffer.get`
changes the buffer view dtype from `int8` to BF16 or FP8.

### Method

- Ran replacement smoke
  `s4p-aic-moe-0815-181728` on B300 node
  `gpu-b300-0635.qy.cnw.istep.fun`.
- Read the complete traceback and compared
  `expected=(15135145984,7567572992,234881024)` with the reported
  `actual=(7567572992,3783786496,234881024)`.
- Confirmed the backing allocation was unchanged; only `numel()` changed when
  pinned vLLM reinterpreted the first two buffers as BF16.
- Added a CPU RED test with an 8-byte BF16 backing buffer represented by four
  elements.
- Changed validation from element count to
  `numel * element_size`, preserving the same fail-fast capacity check.

### Result

- B300 smoke failed after producing the first masked row; it did not reach the
  second case. The RJob and Replica were both cleaned to zero.
- RED: `1 failed` with the reproduced false under-capacity error.
- GREEN focused suite: `33 passed`, `0 failed`, `3.05 seconds`.
- The fix does not resize, bypass, or relax provider workspaces; it corrects
  only the comparison unit from elements to bytes.

## 2026-08-15 — Replacement Optimus MoE B300 smoke passed

**Status:** PASS; full 174-case collection is next.

### Motivation

Verify the byte-capacity root fix and prove that the Collector executes both
custom-op branches selected by pinned `Fp8MoEMethod.apply`.

### Expectation

- Masked rows use
  `torch.ops.vllm.deepgemm_optimus_moe_masked_fp8` with mandatory CUDA graph.
- Contiguous rows use `torch.ops.vllm.deepgemm_optimus_moe_fp8` in eager mode.
- No graph fallback is accepted.
- Every measured row queries back exactly from the unchanged consumer.

### Method

Ran:

```bash
MODE=smoke bash tests/performance/step4_pro_latest/run_b300_optimus_moe_collection.sh
```

The worker reconstructed pinned vLLM commit
`607d1641ee3fec43653fca510d717725828890c2`, verified all `2103/2103` source
files, and executed three representative cases on B300.

### Result

- RJob: `s4p-aic-moe-0815-182356`.
- Node: `gpu-b300-0660.qy.cnw.istep.fun`.
- GPU: `NVIDIA B300 SXM6 AC`, driver `580.159.03`, SM `10.3`.
- Scheduling time: `11 seconds`.
- Runtime: torch `2.10.0+cu129`, CUDA `12.9`, pinned vLLM
  `0.19.0.post20.dev26+gc820e5ae1`, Step Optimus `3.23.24`.
- Rows:
  - `128` tokens, local `128`, EP16, balanced, masked/CUDA graph:
    `0.8584960301717123 ms`;
  - `6144` tokens, local `5334`, EP16, power-law 1.2,
    masked/CUDA graph: `16.631861368815105 ms`;
  - `12288` tokens, local `7563`, EP32, power-law 1.01,
    contiguous/eager: `1.5042506853739421 ms`.
- `used_cuda_graph` was respectively `True`, `True`, and `False`.
- Consumer: `3/3` exact matches, all `source="silicon"`.
- Dataset SHA256:
  `504d3d18e68a038f6facbd30293af212fb83a8d2ce56b11c9a3430a1041b2246`.
- Final matching RJobs: `0`; final matching Replicas: `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_aic_optimus_moe_b300_20260815/smoke_s4p-aic-moe-0815-182356/`.

## 2026-08-15 — Replacement Optimus MoE full B300 collection accepted

**Status:** PASS; canonical B300 parquet installed.

### Motivation

Replace the rejected `174`-row artifact with measurements that exactly follow
pinned `Fp8MoEMethod.apply` custom-op selection and never silently fall back
between CUDA graph and eager execution.

### Expectation

- `174` rows and zero errors;
- both EP16 and EP32;
- balanced, power-law 1.01, and power-law 1.2 routing;
- masked rows use CUDA graph and contiguous rows use eager;
- `174/174` consumer queries return the same latency from `silicon`.

### Method

Ran:

```bash
MODE=full bash tests/performance/step4_pro_latest/run_b300_optimus_moe_collection.sh
```

Converted the accepted CSV with the repository Collector parquet finalizer,
copied the parquet into the canonical B300/vLLM `0.19.0` partition, and queried
every canonical row through the unchanged `MoE` consumer.

### Result

- RJob: `s4p-aic-moe-0815-182707`.
- Node: `gpu-b300-0621.qy.cnw.istep.fun`.
- GPU: `NVIDIA B300 SXM6 AC`, driver `580.159.03`, SM `10.3`.
- Scheduling time: `68 seconds`.
- Collector: `174/174` completed, `0` errors.
- Measured collection time: `111.20 seconds`.
- EP coverage: EP16=`87`, EP32=`87`.
- Routing coverage: balanced=`58`, power-law 1.01=`58`,
  power-law 1.2=`58`.
- Kernel/mode coverage:
  - masked/CUDA graph/`True`: `145` rows;
  - contiguous/eager/`False`: `29` rows.
- Local token range: `1` to `65536`.
- Measured latency range:
  `0.27983999252319336` to `16.00377019246419 ms`.
- Consumer: `174/174` exact matches, all `source="silicon"`.
- Accepted CSV SHA256:
  `cbea7ec572729121df09784a0b06dcff5c92780c510b12fdda28f6982afca3fd`.
- Canonical parquet:
  `src/aiconfigurator/systems/data/b300_sxm/vllm/0.19.0/step4_optimus_moe_perf.parquet`.
- Canonical parquet SHA256:
  `4bfb1ccdfa8007d3a23576b4e7d50e10dbb11fef6500db29668a59f530cad388`.
- Final matching RJobs: `0`; final matching Replicas: `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_aic_optimus_moe_b300_20260815/full_s4p-aic-moe-0815-182707/`.

## 2026-08-15 — DeepEP HT static consumer vertical slice

**Status:** STATIC PASS; distributed runtime benchmark remains in progress.

### Motivation

Start the final provider-specific operation family without reusing SGLang
DeepEP rows or the generic vLLM NCCL/CustomAllReduce path.

### Expectation

Separate dispatch and combine rows, exact topology/transport identity, and
interpolation only across `tokens_per_dp_rank`.

### Method

- Added RED tests for the vLLM WideEP registry, 58 distributed case
  invocations, separate dispatch/combine queries, exact topology matching,
  bounded interpolation, and physical-key collisions.
- Added the pinned HT consumer schema, loader/query, model metadata, model
  cases, vLLM WideEP registry, and framework manifest route.

### Result

- RED: `9 failed`, all due to the missing vLLM DeepEP implementation.
- GREEN: `9 passed`, `27 deselected`, `0.23 seconds`.
- Case population: `58` unique distributed invocations =
  2 EP topologies × 29 local-token points; each runtime invocation will emit
  separate dispatch and combine rows.
- Runtime measurement is not yet claimed.

## 2026-08-15 — Two-node environment passed; coordinated-DP role fix reached stop gate

**Status:** ENVIRONMENT LIVE PASS; HEADLESS/API ROLE STATIC PASS; fourth live
attempt withheld by policy.

### Motivation

Continue the DP16/EP16 DeepEP smoke after restoring platform-distributed
variables without hiding new startup failures.

### Expectation

- Both nodes restore the same distributed contract.
- Rank 0 remains the only API-serving head.
- Rank 1 runs headless with no API server.
- DeepEP HT dispatch/combine and a real request must pass before acceptance.

### Method

- Attempt `s4p-2d-0815-172744` exposed an env-file readiness race; replaced
  the immediate probe with a bounded condition wait.
- Attempt `s4p-2e-0815-174618` live-verified rank/address restoration and
  exposed the pinned vLLM remote-engine headless requirement.
- Attempt `s4p-2f-0815-181337` added role-based `--headless` and exposed the
  pinned CLI's required `--api-server-count 0`.
- Added the exact pinned source/fixture contract and re-ran static tests.

### Result

- `s4p-2d` scheduling: `18 seconds`; both resources cleaned to zero.
- `s4p-2e` scheduling: `695 seconds`; restored ranks `0/1`, start ranks `0/8`,
  node count `2`, processes per node `8`; both resources cleaned to zero.
- `s4p-2f` scheduling: `26 seconds`; the same environment restoration passed;
  both resources cleaned to zero.
- Final role command for non-head nodes:
  `--headless --api-server-count 0`.
- Final complete E2E static suite: `17 passed`, `0 failed`, `0.17 seconds`.
- The required DeepEP HT live PASS is still absent.
- Per the external three-attempt rule, no fourth B300 launch was run.

## 2026-08-15 — DeepEP HT exact Buffer runtime slice

**Status:** LOCAL PASS; distributed EP16/EP32 runner and B300 smoke pending.

### Motivation

Replace the explicit `NotImplementedError` in the vLLM WideEP Collector with a
runtime path that measures the same DeepEP HT dispatch and combine Buffer calls
used by pinned `DeepEPHTPrepareAndFinalize`.

### Expectation

- initialize the exact pinned vLLM `deepep_high_throughput` manager;
- quantize dispatch payloads with the pinned FP8 block-128 path;
- keep layout, dispatch, and combine arguments identical to the pinned source;
- measure dispatch and combine separately in eager mode;
- persist the maximum latency across all EP ranks;
- reject any provider, topology, transport, or runtime-version mismatch.

### Method

- Added mock-based RED tests for the exact
  `get_dispatch_layout`/`Buffer.dispatch`/`Buffer.combine` keyword contract.
- Added public-run RED tests for separate physical rows and fail-fast case
  validation.
- Added input, distributed max-latency, and runtime-cache RED tests.
- Implemented pinned vLLM model-parallel initialization, selected
  `DeepEPHTAll2AllManager` through the EP device communicator, created its
  Buffer via `get_handle({})`, and reused
  `DeepEPHTPrepareAndFinalize` for dispatch/combine configs.
- Used `moe_kernel_quantize_input(..., block_shape=[128, 128])`, eager
  `benchmark_with_power`, fixed iteration counts, and rank-wide MAX reduction.

### Result

- First runtime-contract RED: `2 failed`, expected missing helper.
- Public-run RED: `11 failed, 2 passed`, expected `NotImplementedError`.
- Input/cache/reduction RED: `3 failed, 13 passed`, expected missing helpers.
- Final focused GREEN:
  `52 passed, 0 failed, 3.13 seconds`.
- Ruff check: PASS.
- Ruff format check: PASS.
- `git diff --check`: PASS.
- Initial command
  `.venv/bin/pytest ...` failed because this worktree's partial `.venv` has no
  pytest executable. Root cause was environment selection, not a test failure;
  the recorded conda interpreter
  `/home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python`
  was then used successfully.
- No B300 result is claimed yet. The next gate is a distributed runner and
  live EP16 smoke.

## 2026-08-15 — DeepEP HT distributed runner static gate

**Status:** STATIC PASS; EP16 live smoke pending.

### Motivation

The exact Buffer runtime could not be exercised by the ordinary Collector
worker pool because every EP rank must run the same case in the same order
under one multi-node `torchrun` world.

### Expectation

- EP16 maps to 2 nodes × 8 ranks and EP32 maps to 4 nodes × 8 ranks;
- every node reconstructs and verifies the pinned vLLM source while retaining
  the image-native `vllm._C`;
- smoke deterministically covers local-token points `1`, `8192`, and `65536`;
- full mode runs all 29 points for one topology;
- rank 0 alone writes the dataset, with exactly one dispatch and one combine
  row per case;
- all control commands retain the 3 GiB host-memory bound and exact-resource
  cleanup.

### Method

- Added RED tests for topology selection, torchrun environment identity,
  per-case physical rows, numeric summaries, pinned-source reconstruction,
  RDMA allocation, streamed payloads, and exact cleanup.
- Implemented:
  - `tests/performance/step4_pro_latest/run_step4_deepep_ht_distributed.py`;
  - `tests/performance/step4_pro_latest/remote_b300_deepep_ht_collection.sh`;
  - `tests/performance/step4_pro_latest/run_b300_deepep_ht_collection.sh`.
- Reused `remote_b300_source_probe.sh` and its `2103/2103` pinned manifest
  contract rather than mounting a pure Python checkout.

### Result

- Driver RED: import failure because the driver did not exist.
- Wrapper RED: file-not-found because the host wrapper did not exist.
- Driver/static GREEN: `11 passed`, `0 failed`, `0.18 seconds`.
- Combined DeepEP focused suite: `33 passed`, `27 deselected`,
  `0 failed`, `1.56 seconds`.
- `bash -n` passed for both shell runners.
- Python compile passed for the distributed driver.
- Ruff check passed; Ruff format identified three formatting changes, which
  were applied before the live gate.
- A static test initially rejected the allowed disk-backed
  `/data/ycfeng/tmp` path by matching the substring `/tmp`. The test was
  corrected to reject only a real `/tmp` artifact root; production code did
  not change for that test defect.
- No B300 runtime or latency result is claimed at this checkpoint.

## 2026-08-15 — Authorized fourth two-node run blocked at NCCL HCA initialization

**Status:** FAIL; root cause is the launch/RDMA environment, not the model or
headless role logic.

### Motivation

Use the owner's explicit approval to verify the final
`--headless --api-server-count 0` role split and attempt the complete DP16/EP16
DeepEP HT smoke.

### Expectation

- Restore all distributed variables on both nodes.
- Start one normal head and one headless worker node.
- Establish all 16 ranks.
- Reach DeepEP HT manager, dispatch, combine, and a real request.

### Method

- Ran `s4p-2g-0815-185302` with two 8-GPU B300 replicas,
  `rdma/mlnx_shared=8`, topology grouping, and preserved platform NCCL/NVSHMEM
  values.
- Captured the complete rank-0 server log before cleanup.
- Compared the runtime environment with the later branch's B300 bootstrap
  recipe.

### Result

- Scheduling: `15 seconds`.
- Source: `2103/2103` pinned files.
- Rank/start-rank mapping: `0/0` and `1/8`.
- Gloo: every rank connected to all `15` peers.
- Headless/API role error: resolved.
- DeepEP runtime setup detected `NCCL_IB_GID_INDEX=5`.
- `NCCL_IB_HCA` remained empty; NVSHMEM HCA inheritance was skipped.
- First NCCL all-reduce failed with `NCCL error: unhandled system error`.
- DeepEP HT manager, dispatch, combine, and request were not reached.
- Final RJob count `0`, Replica count `0`, related process count `0`.
- Evidence:
  `/data/ycfeng/tmp/b300_step4_smoke_20260814/two_node_s4p-2g-0815-185302/`.

## 2026-08-15 — AIC DeepEP HT EP16 live attempts reached the full-RDMA gate

**Status:** FIRST ROOT FIX PASS; SECOND ATTEMPT FAILS THE AUTHORIZED NCCL
PREFLIGHT CONDITION.

### Motivation

Exercise the exact pinned-vLLM `DeepEPHTAll2AllManager` path through the new
AIC distributed Collector without substituting another communication backend.

### Expectation

- Run 2 nodes × 8 B300 ranks with the pinned image and source.
- Verify all `2103/2103` pinned source files.
- Initialize CUDA, vLLM DP/EP groups, and then the DeepEP HT Buffer.
- Stop before collection if the platform does not provide a usable RDMA HCA.

### Method

1. Ran EP16 smoke `s4p-aic-deepep-16-0815-191325`.
2. Compared its CUDA failure with the already live-verified B300 scripts.
3. Replaced the inherited `LD_LIBRARY_PATH` composition with the exact pinned
   B300 contract:

   ```bash
   export LD_LIBRARY_PATH=/usr/local/cuda-13.0/compat:/usr/local/nvidia/lib64
   ```

4. Re-ran EP16 smoke as `s4p-aic-deepep-16-0815-191949`.
5. Traced the second failure through
   `initialize_model_parallel -> DP GroupCoordinator -> CudaCommunicator ->
   PyNcclCommunicator` and compared the launch specification with the pinned
   branch's two-node B300 script.

### Result

- First run:
  - predict-only: PASS;
  - replicas: `2/2` ready;
  - source identity: `2103/2103`;
  - failure: CUDA Error `803` before capability validation;
  - root cause: an incompatible inherited CUDA library path followed the
    verified CUDA 13 compatibility path.
- Second run:
  - replicas: `2/2` ready;
  - hardware capability: `(10, 3)` on all ranks;
  - all `16` ranks entered pinned vLLM distributed initialization;
  - vLLM package: `0.19.0.post20.dev26+gc820e5ae1`;
  - PyNCCL/NCCL: `2.27.5`;
  - platform values: `NCCL_SOCKET_IFNAME=eth0`,
    `NCCL_IB_GID_INDEX=5`, `NCCL_IB_HCA=''`;
  - failure: PyNCCL warm-up all-reduce returned
    `NCCL error: unhandled system error`;
  - DeepEP Buffer, dispatch, combine, and latency rows were not reached.
- Both attempts ended with exact RJob count `0` and Replica count `0`.
- Evidence:
  - `/data/ycfeng/tmp/step4_aic_deepep_ht_b300_20260815/smoke_ep16_s4p-aic-deepep-16-0815-191325/`;
  - `/data/ycfeng/tmp/step4_aic_deepep_ht_b300_20260815/smoke_ep16_s4p-aic-deepep-16-0815-191949/`.

### Root-cause conclusion

The CUDA library-path defect is resolved. The remaining failure is not a
reason to disable PyNCCL or bypass vLLM group initialization: the job still
uses only `rdma/mlnx_shared=8` and lacks the pinned branch's
`mellanox.com/mlnx_rdma=1` plus host-network launch contract. Per Q16, the next
run must first be a minimal NCCL preflight, must reject an empty platform HCA,
and must not hardcode an HCA, disable IB, use socket fallback, or replace
DeepEP HT.

## 2026-08-15 — Full-RDMA EP16 NCCL preflight transport passed

**Status:** NCCL PASS; wrapper evidence parser RED.

### Motivation

Verify whether the authorized host-network plus full RDMA resource contract
provides a real platform HCA and supports the exact EP16 rank-wide NCCL
collective required before DeepEP HT collection.

### Expectation

- platform `NCCL_IB_HCA` is non-empty without hardcoding;
- all `16` B300 ranks join one NCCL group;
- all-reduce returns rank sum `136` and participant sum `16`;
- wrapper records eight local PASS results per node without depending on
  nondeterministic concurrent log line boundaries.

### Method

- Ran:

  ```bash
  MODE=preflight EP_SIZE=16 EVIDENCE_HOLD_SECONDS=30 \
    RESULT_TIMEOUT_SECONDS=600 \
    bash tests/performance/step4_pro_latest/run_b300_deepep_ht_collection.sh
  ```

- Inspected both remote logs, the pulled evidence, and exact cleanup state.
- Added a RED wrapper contract reproducing eight concatenated rank markers.

### Result

- Predict-only identified two available 8-GPU B300 nodes.
- Scheduling completed in `11 seconds`; replicas reached `2/2`.
- Platform injection supplied:
  - `NCCL_SOCKET_IFNAME=bond0`;
  - `NCCL_IB_HCA==mlx5_bond100,...,mlx5_bond107`;
  - `NCCL_IB_GID_INDEX=3`;
  - `NCCL_NET_PLUGIN=stepfun`.
- All `16` ranks completed the real NCCL all-reduce:
  - observed rank sum: `136`;
  - expected rank sum: `136`;
  - observed participant sum: `16`;
  - expected participant sum: `16`;
  - B300 capability: `10.3`.
- The wrapper exited `1` only because
  `grep -c '^STEP4_NCCL_PREFLIGHT_RANK=PASS '` counts lines, while concurrent
  `torchrun` output placed eight intact markers on one interleaved line.
- Final RJob count: `0`; final Replica count: `0`; related local process count:
  `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_aic_deepep_ht_b300_20260815/preflight_ep16_s4p-aic-deepep-16-0815-193938/`.
- Fix record:
  - **Motivation:** prevent a successful NCCL collective from being rejected
    by concurrent stdout formatting.
  - **Expectation:** count exact PASS marker occurrences, independent of line
    boundaries, while retaining exact count and numeric checks.
  - **Method:** added a RED reproduction for eight concatenated markers,
    replaced line counting with fixed-string occurrence counting for both NCCL
    and DeepEP rank markers, and retained all exact numeric assertions.
  - **Result:** initial RED was `1 failed, 5 passed`; focused GREEN was
    `9 passed`. Shell syntax, Ruff check, Ruff format, and diff checks passed.

### Live rerun

- Command: same EP16 preflight command shown above.
- Result: `B300_DEEPEP_HT_HOST=PASS`.
- Scheduling: `16 seconds`; replicas: `2/2`.
- Per-node evidence:
  - local rank PASS count: `8` and `8`;
  - world size: `16`;
  - rank sum: `136`;
  - participant sum: `16`;
  - HCA present: `1`.
- Global markers:
  - rank PASS: `16`;
  - distributed PASS: `1`.
- Final RJob count: `0`; final Replica count: `0`; related local process count:
  `0`.
- Accepted evidence:
  `/data/ycfeng/tmp/step4_aic_deepep_ht_b300_20260815/preflight_ep16_s4p-aic-deepep-16-0815-194622/`.

## 2026-08-15 — EP16 DeepEP HT smoke exposed missing pinned worker lifecycle

**Status:** FAIL; root-cause fix is in RED.

### Motivation

After the full-RDMA NCCL gate passed, exercise the exact pinned
`DeepEPHTAll2AllManager -> deep_ep.Buffer -> dispatch/combine` path on two
B300 nodes.

### Expectation

- verify `2103/2103` pinned source files on both nodes;
- initialize all `16` ranks through vLLM;
- construct the pinned DeepEP HT Buffer;
- produce six positive-latency smoke rows.

### Method

```bash
MODE=smoke EP_SIZE=16 EVIDENCE_HOLD_SECONDS=30 \
  RESULT_TIMEOUT_SECONDS=1800 \
  bash tests/performance/step4_pro_latest/run_b300_deepep_ht_collection.sh
```

The failure was traced from `deep_ep.Buffer.runtime.sync` back through the
Collector's direct distributed initialization and compared with pinned
`WorkerWrapperBase.init_worker`.

### Result

- Scheduling: `11 seconds`; replicas: `2/2`.
- Pinned source: `2103/2103`.
- Runtime identity:
  - B300 capability: `10.3`;
  - torch: `2.10.0+cu129`;
  - vLLM: `0.19.0.post20.dev26+gc820e5ae1`;
  - DeepEP: `1.2.1+c5284713.step.torch2.10.0.cu129.py312`;
  - provider: `vllm_deepep_high_throughput`.
- NCCL and `DeepEPHTAll2AllManager` selection succeeded.
- Buffer construction failed in NVSHMEM with:

  ```text
  nvshmemi_get_device_state_ptrs: Unable to access device state. 999
  RuntimeError: ... DeepEP/csrc/kernels/runtime.cu:83 'unknown error'
  ```

- Direct Collector runtime initialized distributed groups without calling the
  pinned vLLM worker's
  `_harmonize_nvshmem_env_for_deepep_optimus()`. This omitted the actual vLLM
  setup for NVSHMEM GID inheritance, traffic class, NCCL isolation, NIC
  mapping default, and team budget.
- Environment observation, not yet classified as causal: one GPU per node had
  only `13075 MiB` and `8096 MiB` free before torchrun; no guessed memory
  threshold has been added.
- Final RJob count: `0`; final Replica count: `0`; related local process count:
  `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_aic_deepep_ht_b300_20260815/smoke_ep16_s4p-aic-deepep-16-0815-194903/`.
- Fix record:
  - **Motivation:** make direct Collector initialization reproduce the pinned
    vLLM worker lifecycle instead of duplicating or omitting NVSHMEM setup.
  - **Expectation:** invoke the actual pinned helper before distributed/NVSHMEM
    initialization.
  - **Method:** added a RED source-order contract, imported the pinned helper
    directly, and invoked it before `init_distributed_environment`.
  - **Result:** RED was `1 failed, 16 passed`; focused GREEN was
    `34 passed`. Replacement live smoke remains pending.

### Replacement smoke result

- Replacement run:
  `/data/ycfeng/tmp/step4_aic_deepep_ht_b300_20260815/smoke_ep16_s4p-aic-deepep-16-0815-195827/`.
- All GPUs on the pulled node were clean before torchrun:
  `274114 MiB` free on each of eight B300 GPUs.
- The pinned helper ran on all ranks, including:
  - `NVSHMEM_IB_GID_INDEX=3` inheritance;
  - `NVSHMEM_MAX_TEAMS=128`.
- The same CUDA `999` occurred in `deep_ep.Buffer.runtime.sync` on every local
  rank.
- Conclusion: the skipped helper was a real fidelity defect and remains fixed,
  but it was not sufficient to resolve the Buffer failure. The earlier GPU
  occupancy observation is also ruled out for this replacement.
- Exact cleanup again ended at RJob `0`, Replica `0`, related local process
  `0`.
- Next diagnostic: run the image-native DeepEP HT Buffer on one clean 8-GPU
  B300 node with `num_rdma_bytes=0`. This separates local CUDA IPC/runtime
  support from internode NVSHMEM/RDMA initialization without repeating the
  same EP16 run.

## 2026-08-15 — External full-RDMA preflight passed; DeepEP Buffer sync remains blocked

**Status:** NCCL PASS; DeepEP Buffer FAIL.

### Motivation

Apply the owner-approved full B300 network contract and separate basic
16-rank NCCL correctness from DeepEP/NVSHMEM initialization.

### Expectation

- Require platform-injected bond HCAs.
- Run 16-rank NCCL all-reduce before loading the model.
- Start the full model only after the preflight passes.
- Preserve DeepEP HT as the only accepted all-to-all backend.

### Method

- Added a TDD NCCL preflight with host network, both RDMA resources, exact
  HCA fail-fast, 16-rank numeric validation, and exact cleanup.
- First preflight exposed CUDA error `803` from worker-init's stale CUDA 12.8
  compat path.
- Restored the pinned CUDA 13 compat path and reran.
- Used the passing preflight as a hard gate for full run
  `s4p-2h-0815-202202`.

### Result

- Full-RDMA predict-only: PASS.
- Corrected preflight scheduling: `16 seconds`.
- HCA: `mlx5_bond100..107`; GID `3`; socket interface `bond0`.
- NCCL rank passes: `16/16`.
- NCCL node passes: `2/2`.
- All-reduce actual/expected: `136.0/136.0`.
- Full model scheduling: `16 seconds`.
- Full model selected `DeepEPHTAll2AllManager`.
- Every local rank entered `DeepEP HT get_handle`.
- `deep_ep.Buffer.runtime.sync` failed at `runtime.cu:83` with CUDA
  `unknown error`/`999`.
- Both runs ended with RJob `0`, Replica `0`, related local process `0`.
- Current launcher lacks the later branch's `--share-host-shm=True` option;
  no further model run was attempted.
- Evidence:
  - `/data/ycfeng/tmp/b300_step4_smoke_20260814/nccl_preflight_s4p-nccl2-0815-195936/`
  - `/data/ycfeng/tmp/b300_step4_smoke_20260814/two_node_s4p-2h-0815-202202/`

## 2026-08-15 — Recovered interrupted one-node DeepEP discriminator

**Status:** prior attempt did not execute; clean relaunch required.

### Motivation

Resume from the one-node, eight-GPU `num_rdma_bytes=0` discriminator without
mistaking a completed holder job for a completed DeepEP runtime test.

### Expectation

The exact RJob and Replica state, controller process state, and remote log must
show whether the image-native `torchrun` actually reached
`deep_ep.Buffer.runtime.sync`.

### Method

- Queried only RJob `s4p-deepep-local-0815-2019` and its exact Replica label.
- Inspected the disk-backed launch and remote-exec logs.
- Checked only related controller processes.

### Result

- RJob status: `Succeeded`.
- Replica status: `0/1 Succeeded`.
- The intended non-TTY output file
  `/data/ycfeng/tmp/s4p-deepep-local-0815-2019_remote_exec_final.log` does not
  exist.
- The earlier remote-exec log contains only the `systemd-run` scope header and
  no `nvidia-smi`, `torchrun`, rank, Buffer, or CUDA result.
- No related local controller process remains.
- Conclusion: the TTY-stopped command did not run. This is not a DeepEP PASS or
  FAIL and provides no evidence about local CUDA IPC. The completed holder must
  be deleted, verified absent, and replaced by one clean non-TTY run.

## 2026-08-15 — One-node DeepEP construction passed; teardown and cross-node path blocked

**Status:** discriminator complete; required DeepEP collection blocked.

### Motivation

Distinguish a local B300 CUDA IPC/runtime failure from the EP16 cross-node
NVSHMEM/shared-host-SHM failure without repeating the same two-node smoke.

### Expectation

- The pinned image must run one `torchrun` with eight B300 ranks.
- Each rank must invoke the pinned vLLM NVSHMEM harmonization helper.
- `deep_ep.Buffer` must use `num_rdma_bytes=0`.
- Exact Buffer construction and explicit destroy outcomes must remain visible.
- RJob and Replica cleanup must both reach zero.

### Method

- Added
  `tests/performance/step4_pro_latest/run_step4_deepep_local_ipc_preflight.py`.
- The first static command used the incomplete repository `.venv` and failed
  because `.venv/bin/ruff` does not exist.
- Re-ran static validation with the documented
  `/home/i-fengyicheng/miniconda3/envs/aic-step-design` environment:
  Python `3.11.15`, Ruff `0.14.1`.
- Ran B300 predict-only, launched one eight-GPU holder, transferred the script,
  and executed `torchrun --standalone --nproc-per-node=8` through a non-TTY
  `brainctl exec`.
- Counted fixed-string PASS markers and both constructor and destroy CUDA error
  locations separately.

### Result

- Static validation: py_compile PASS, Ruff check PASS, Ruff format PASS, and
  `git diff --check` PASS.
- GPU identity: eight `NVIDIA B300 SXM6 AC`, capability `10.3`, driver
  `580.159.03`, each with `274114 MiB` free before the run.
- Runtime:
  - Python `3.12.13`;
  - vLLM `0.19.0.post20.dev26+gc820e5ae1`;
  - DeepEP `1.2.1+c5284713.step.torch2.10.0.cu129.py312`.
- Buffer construction markers: `8/8`.
- Constructor `runtime.cu:83` CUDA `999` occurrences: `0`.
- Explicit destroy `runtime.cu:29` CUDA `999` occurrences: `8`.
- Overall remote exit code: `1`.
- Final RJob count: `0`; final Replica count: `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_aic_deepep_local_b300_20260815/s4p-deepep-local-0815-2046/`.

### Root-cause conclusion

Local eight-rank Buffer construction is functional, so the EP16 constructor
failure is narrowed to the cross-node NVSHMEM/RDMA/shared-host-SHM path. The
local explicit-destroy failure is a separate vendor-runtime teardown defect.
The available launcher does not expose the pinned DeepEP shared-host-SHM
contract, so EP16/EP32 retries, the `116` DeepEP rows, exact consumer
validation, and formal simulation are blocked. No fallback, synthetic row,
socket substitution, or H800 data was used.

## 2026-08-15 — Resumed at ISSUE-022 chunked-prefill KV accounting

**Status:** In progress; RED verified and minimal production implementation is
next.

### Fix record

- **Motivation:** the current peak-KV API models one-token decode growth, while
  the requirements explicitly sweep prefill
  `max_num_batched_tokens={8192,32768,65536}`. Using the decode-only peak would
  under-report SWA pages and peak HBM for formal prefill simulation.
- **Expectation:** carry an explicit in-flight per-sequence token count through
  the runtime memory path, preserve decode behavior at one token, and reproduce
  the pinned vLLM release-before-allocation block order for chunked prefill.
- **Method:** resumed from the recorded pinned-vLLM source trace, re-read the
  task plan/harness and current production/test symbols, and added focused
  tests for the nine required long-context/chunk combinations, the one-token
  decode default, invalid chunks, backend propagation, and batch>1 global-budget
  rejection.
- **Result:** RED completed with `14 failed, 34 deselected` in `1.23s`.
  Failures were exactly the missing `in_flight_tokens` model API, missing
  `kv_in_flight_tokens` backend API, missing
  `RuntimeConfig.max_num_batched_tokens`, and absent summary propagation.
  Evidence:
  `/data/ycfeng/tmp/step4_chunk_kv_red_20260815.log`.

### GREEN result

- Added keyword-only `in_flight_tokens` to the physical peak-KV contract while
  retaining the one-token default for decode and capacity inversion.
- Implemented the pinned vLLM SWA order exactly: release blocks from the
  previous computed-token boundary, then allocate through the current chunk.
- Added `RuntimeConfig.max_num_batched_tokens` and mapped it only for
  `batch=1` `static_ctx`, where the global scheduler budget equals the
  per-sequence chunk. Explicit batch>1 use fails fast instead of assuming a
  token-budget split.
- Required matrix:

  | Sequence | Chunk | Peak SWA blocks |
  |---:|---:|---:|
  | 32,768 | 8,192 / 32,768 / 65,536 | 68 / 256 / 256 |
  | 131,072 | 8,192 / 32,768 / 65,536 | 68 / 260 / 516 |
  | 1,048,544 | 8,192 / 32,768 / 65,536 | 68 / 260 / 516 |

- Focused GREEN: `14 passed, 34 deselected` in `0.14s`.
- Independent bounded-formula comparison:
  `STEP4_CHUNK_FORMULA_EXHAUSTIVE_PASS checked=1094700`.
- Focused SDK regression: `351 passed` in `46.85s`.
- Evidence:
  - `/data/ycfeng/tmp/step4_chunk_kv_green2_20260815.log`;
  - `/data/ycfeng/tmp/step4_chunk_kv_focused_regression_20260815.log`.

### Broad SDK regression

- Command covered all `tests/unit/sdk`.
- Result: `1936 passed, 2 failed` in `164.94s`.
- Failure 1 is the already-recorded ISSUE-019 stale TP16
  `CustomAllReduce` scaling expectation; production correctly requires the
  runtime-selected exact multi-node collective.
- Failure 2 is a same-task DeepEP test-path mismatch:
  `test_provider_specific_deepep_rejects_generic_perf_database` constructs a
  non-`fp8_block` operation and is rejected by the provider's quantization
  precondition before reaching the intended generic-database assertion. Root
  cause was a stale expectation from before the exact DeepEP consumer existed.
  The production `fp8_block` contract remains unchanged; the test now supplies
  the pinned identity and verifies missing exact-key
  `PerfDataNotAvailableError`.
- DeepEP focused verification: `7 passed` in `0.06s`; Ruff check and format
  check passed.
- Broad SDK rerun excluding only the separately recorded ISSUE-019 test:
  `1937 passed, 1 deselected` in `157.07s`.
- Collector regression: `396 passed` in `38.84s`.
- Changed ISSUE-022 files: Ruff check PASS, Ruff format check PASS, and
  `git diff --check` PASS.
- Evidence:
  - `/data/ycfeng/tmp/step4_chunk_kv_sdk_regression_20260815.log`;
  - `/data/ycfeng/tmp/step4_deepep_test_contract_20260815.log`;
  - `/data/ycfeng/tmp/step4_chunk_kv_sdk_without_issue019_20260815.log`;
  - `/data/ycfeng/tmp/step4_collector_regression_20260815.log`.

## 2026-08-15 — Resumed at the B300 canonical-dataset audit

**Status:** Blocked at the launcher API boundary.

### Motivation

The accepted reports contain B300 measurements for several pinned-vLLM
provider families, but the canonical
`src/aiconfigurator/systems/data/b300_sxm/vllm/0.19.0` directory has not yet
been reconciled against every reported output. Formal E2E must not rely on a
report alone or silently consume a generic adjacent dataset.

### Expectation

- Locate the actual output artifact for every measured Step4 provider family.
- Verify rows, latency values, duplicate physical keys, runtime identity, and
  exact consumer queries.
- Copy or transform nothing until the source artifact and consumer contract
  are proven.
- Keep DeepEP HT visibly blocked and fail fast on its missing exact rows.

### Method

- Restored `requirements.md`, `plan.md`, `harness.md`, recent `progress.md`,
  and open `issues.md`.
- Confirmed branch `task/step4-pro-latest-b300`, HEAD `84021c6c`, and preserved
  all existing tracked and untracked work.
- Re-read the Collector operation-development and auto-collection rules before
  starting the dataset audit.

### Result

The active interruption point is confirmed. Attention reports `50` context
plus `149` generation consumer-valid rows, Optimus MoE reports `174/174`, and
DeepEP still has `0/116` accepted rows. The actual canonical location and
quality status of grouped `wo_a`, FP32 router, QKV norm/RoPE, and Attention
artifacts remain to be established by the next audit step.

### Initial artifact audit result

- Canonical B300 vLLM `0.19.0` currently contains only one Step4-specific
  file: `step4_optimus_moe_perf.parquet`, with `174` rows.
- Accepted Attention CSVs were located in the full-run evidence directory:
  - `step4_context_attention_perf.txt`: `50` rows, SHA256
    `c3738dd5abdb006dd5c37295c020e5424489e280cf4d9f2051aea186a17ddcd9`;
  - `step4_generation_attention_perf.txt`: `149` rows, SHA256
    `c85dc680ac4615a9a20a130af92a82fadf9ae12d78df6eccdbd0ae4fcc5637a9`.
- The existing Attention consumer evidence records `199/199` exact
  `source="silicon"` queries, but the two files have not yet been converted
  and archived into the canonical database directory.
- The grouped `wo_a`, FP32 router, and QKV norm/RoPE reports explicitly state
  that no B300 latency row was measured. These families are not misplaced;
  their formal collection is still pending.
- A shell inspection glob also matched a dataset subdirectory and emitted
  expected `wc/head/sha256sum` directory errors. No file was changed; the
  corrected audit will enumerate regular files only.

## 2026-08-15 — B300 core-provider runner and exact-consumer validator

**Status:** Focused GREEN; broader static validation pending.

### Motivation

Grouped `wo_a`, FP32 router, and QKV norm/RoPE already have pinned-vLLM
Collector implementations but had no B300 execution path or accepted dataset.
They must use the same source-identity bootstrap as the accepted Attention run
and must be validated through their exact AIC consumers.

### Expectation

- Reuse the verified one-GPU B300 control plane without changing its default
  Attention behavior.
- Smoke all four material provider branches: grouped einsum, FP32 router,
  Full-MFA K norm/RoPE, and SWA QKV norm/RoPE.
- Full mode schedules exactly `74 + 74 + 148 = 296` cases.
- Reject wrong runtime/device/provider/shape, non-positive latency, duplicate
  physical keys, consumer mismatch, or non-silicon results.

### Method

- Added a RED contract before production runner changes.
- Parameterized the existing verified remote bootstrap with an explicit
  `COLLECTION_SUITE`, preserving `attention` as the default.
- Added a thin provider-core host wrapper and an offline validator that loads
  every row through `GroupedGEMM`, `FP32OutputGEMM`, or `QKVNormRoPE`.
- Kept source identity, pinned Optimus overlay, checkpoint/resume, exact
  runtime version, and B300 device checks unchanged.

### Result

- Initial RED: `4 failed`, all for the absent provider-core runner/validator.
- First GREEN attempt: `3 passed, 1 failed`; duplicate input was correctly
  rejected, but the generic row-count check hid the more specific duplicate
  cause.
- Root fix: validate physical-key uniqueness before final row-count
  acceptance.
- Focused final GREEN: `4 passed` in `1.16s`.
- Evidence:
  - `/data/ycfeng/tmp/step4_provider_core_runner_red_20260815.log`;
  - `/data/ycfeng/tmp/step4_provider_core_runner_green_20260815.log`;
  - `/data/ycfeng/tmp/step4_provider_core_runner_green2_20260815.log`.

## 2026-08-15 — Supplied brainctl docs led to legacy-launcher DeepEP probe

**Status:** NCCL PASS; explicit NVSHMEM PASS; DeepEP Buffer FAIL.

### Motivation

Verify the user-supplied brainctl binary and supplemental RJob guidance, then
retry with the documented legacy Process/Worker launcher instead of assuming
shared-host-SHM support.

### Expectation

- verify binary identity;
- use the documented legacy launcher path;
- initialize NCCL and NVSHMEM explicitly on 16 ranks;
- construct and destroy the minimal DeepEP Buffer before any full-model retry.

### Method

- Verified the supplied and installed brainctl SHA256 values are identical.
- Confirmed neither current RJob nor legacy CLI exposes
  `--share-host-shm`.
- Ran legacy predict-only successfully.
- Added a TDD legacy probe using full RDMA, host network, explicit
  `nvshmem.init`, DeepEP Buffer construction, and separate destroy markers.
- Removed unsupported legacy `--name` and disabled missing SSHD after focused
  RED/GREEN tests.

### Result

- Legacy NCCL markers: `16/16`.
- Explicit NVSHMEM initialization markers: `16/16`.
- DeepEP Buffer construction markers: `0/16`.
- Every rank failed:

  ```text
  runtime.cu:136 'nvshmem_n_pes() == num_ranks'
  ```

- `/dev/shm`: tmpfs `245760000k` on both nodes.
- memlock: unlimited.
- Related local process count after completion: `0`.
- Conclusion: the failure is now an NVSHMEM runtime-state/DeepEP integration
  mismatch. Shared-host-SHM is not proven as the root cause.
- Evidence:
  `/data/ycfeng/tmp/b300_step4_smoke_20260814/legacy_deepep_s4p-legdep3-0815-220757/`.

## 2026-08-15 — QKV context fix and provider-core pre-smoke regression

**Status:** PASS; replacement B300 smoke is the next action.

### Motivation

The first provider-core B300 smoke proved source identity but failed before
measurement because constructing the pinned vLLM QKV custom operation occurred
outside `set_current_vllm_config()`.

### Expectation

- Keep the entire QKV benchmark invocation inside one pinned vLLM config
  context.
- Preserve all grouped `wo_a`, FP32 router, Full-MFA QKV, and SWA QKV case and
  consumer contracts.
- Pass Python lint/format, shell syntax, and whitespace checks before another
  GPU allocation.

### Method

- Re-ran the focused unit and provider-core contract suites with Python
  `3.11.15` from conda environment `aic-step-design`.
- Ran Ruff only on Python sources, `bash -n` on the three shell runners, and
  `git diff --check`.
- One initial lint command incorrectly included shell scripts in Ruff and
  therefore emitted Python parse errors. This was a command-selection error,
  not a source failure; the corrected language-specific checks are the
  accepted evidence.

### Result

- Focused regression: `34 passed` in `4.27s`.
- Python Ruff check: PASS.
- Python Ruff format check: `4 files already formatted`.
- Shell syntax and whitespace: PASS.
- Evidence:
  - `/data/ycfeng/tmp/step4_provider_core_complete_focused_regression_20260815.log`;
  - `/data/ycfeng/tmp/step4_provider_core_ruff_check_python_only_20260815.log`;
  - `/data/ycfeng/tmp/step4_provider_core_ruff_format_python_only_20260815.log`;
  - `/data/ycfeng/tmp/step4_provider_core_shell_diff_20260815.log`.

## 2026-08-15 — DeepEP deferred; SWA QKV image-runtime diagnosis completed

**Status:** DeepEP skipped as directed; QKV root cause confirmed; no
unapproved fix applied.

### Motivation

Honor the owner's instruction to stop retrying DeepEP after the completed
attempt, preserve its missing coverage explicitly, and continue with the next
provider family. Diagnose the new SWA QKV failure far enough to distinguish a
Collector defect from an image-native provider/runtime defect.

### Expectation

- Launch no additional DeepEP measurement.
- Keep all `116` DeepEP exact keys missing and visible.
- Identify the SWA QKV failure using the exact pinned source/runtime path.
- Confirm the diagnostic B300 RJob and Replica are fully cleaned up.
- Stop before any runtime overlay that has not been explicitly approved.

### Method

- Inspected the diagnostic run
  `s4p-aic-core-0815-225633`, including package versions, the live kernel
  annotation dictionary, the full traceback, and extracted image-native
  Optimus/Cutlass sources.
- Recomputed the pinned SWA QKV width:
  `(128 + 2 * 8) * 128 = 18432`.
- Compared the QKNorm `reload_from` threshold and annotation behavior with
  Cutlass DSL 4.4.2's `inspect.getfullargspec()` consumer.
- Queried only the exact RJob name and exact Replica label under
  `MemoryMax=3G`.

### Result

- DeepEP remains deferred at `0/116`; no retry was launched.
- The QKV diagnostic smoke remains FAIL before measurement:
  `0/4` representative provider-core cases were accepted.
- Root cause: `from __future__ import annotations` stores
  `reload_from: cutlass.Constexpr` as a string, while Cutlass DSL 4.4.2 does
  not resolve postponed annotations. Width `18432 > 16384` passes
  `reload_from="smem"`, which is then incorrectly treated as dynamic.
- Runtime identities:
  `optimus-jit 0.1.10.post8+gitcfde41ba`,
  `nvidia-cutlass-dsl 4.4.2`, B300 capability `10.3`.
- Exact cleanup verification: RJob `0`, Replica `0`.
- No QKV math, kernel, provider, shape, fallback, or dataset was changed.

## 2026-08-15 — DeepEP launch example/case inventory

**Status:** read-only inventory complete.

### Motivation

Determine whether the pinned runtime image or local vLLM checkout already
contains a reusable DeepEP launch case, without treating installed runtime
modules as launch examples.

### Expectation

- Distinguish image-packaged examples from installed DeepEP/vLLM runtime code.
- Distinguish executable launch scripts, CI/evaluation cases, kernel tests,
  documentation examples, and later-commit-only probes.
- Do not launch a container, GPU job, or DeepEP process.

### Method

- Confirmed the image identity as
  `hub.stepfun-inc.com/stepcast/stepcast:2026-08-06-server-vllm-test-0.19.0.post20.dev26.gc820e5ae1.precompiled`
  with digest
  `sha256:70492b0c79e2286b6ee56973f5f3322b53d293fc9332c4f792e56209a34d182b`.
- Read the image's containerd layer indexes directly. The unprivileged Docker
  API call was denied as expected on this host; the documented read-only
  `sudo -n docker` path succeeded.
- Scanned all `257` regular files under the image's `/app/examples` layer for
  exact `deepep`, `deep_ep`, `deep-ep`, or `VLLM_ALL2ALL_BACKEND` terms.
- Compared image and pinned-repository copies of
  `examples/offline_inference/routed_experts_e2e.py` and
  `examples/offline_inference/data_parallel.py`.
- Searched the pinned checkout at commit
  `607d1641ee3fec43653fca510d717725828890c2` for DeepEP backend selectors,
  executable shell scripts, pytest cases, evaluation configs, CI commands,
  and deployment documentation.
- Compared the pinned checkout with later commit
  `9bfd9a610ea4f2890010702ee7a207cf25edf8de`.

### Result

- Image examples with exact DeepEP terms: `0/257`.
- The image contains DeepEP `1.2.1`, NVSHMEM packages, and vLLM DeepEP runtime
  modules, but no packaged DeepEP-specific example/case launcher.
- The image's `routed_experts_e2e.py` is a routed-expert capture example, not a
  DeepEP launch case. Its image and pinned-repo SHA256 values both equal
  `adc0455fcd747806d32a324acf06aa74a6c590953e256e2eac19e273e8f6c824`.
- The image does contain generic `data_parallel.py`; it accepts all vLLM
  engine arguments and defaults expert parallelism on, so a caller can add a
  DeepEP backend explicitly. The packaged example itself does not select
  DeepEP. Its image and pinned-repo SHA256 values both equal
  `e69c8cefa565571e9ccf276d13752b25719332b444f8494cd801870bb9de57f9`.
- The pinned checkout contains one Step4Pro-specific executable launcher:
  `vllm-step4-pro/rjob-step4pro-2node.sh`. It selects
  `deepep_high_throughput` and submits `2` replicas with `8` GPUs each.
- The pinned checkout also contains three CUDA scheduled-integration shell
  cases that each run both `deepep_high_throughput` and
  `deepep_low_latency`, six GSM8K DeepEP evaluation configs, a direct H100
  two-GPU CI invocation of `data_parallel.py`, and DeepEP kernel pytest cases.
  These are reusable references but are not Step4Pro cross-node RJob probes.
- `rjob-step4pro-deepep-probe.sh` is absent from the pinned working tree. It
  appears only in later commit `9bfd9a610e`, where it adds explicit NVSHMEM
  bootstrap and `--share-host-shm=True`.
- No runtime success was inferred. The requirements document itself says the
  pinned two-node script had not completed B300 end-to-end acceptance.

## 2026-08-15 — B300 Attention canonicalization and canonical consumer validation

**Status:** PASS; Attention archival is complete.

### Motivation

Move the already accepted B300 Attention measurements from evidence-local CSV
files into the canonical AIC performance database without changing any row,
key, provider identity, or measured latency.

### Expectation

- Preserve all `50` context and `149` generation rows.
- Preserve the exact pinned runtime and provider identities.
- Reject duplicate physical keys, invalid latency, or source/canonical table
  differences.
- Reproduce every measured value through the unchanged AIC consumer using the
  canonical Parquet files.

### Method

- Audited the full-run CSV files, collection summary, exact consumer evidence,
  provider splits, physical keys, runtime identity, and latency range.
- Converted the validated Arrow tables directly to the two new canonical
  Parquet files with Zstandard compression; no source file was deleted or
  moved.
- Read both Parquet files back and required exact Arrow table equality with
  the source CSV tables.
- Created a fresh `PerfDatabase` against
  `src/aiconfigurator/systems` and queried every context/generation row through
  `ContextAttention` or `GenerationAttention`.

### Result

- Source rows: context `50`, generation `149`.
- Provider rows:
  - context: Optimus FA4 `25`, native SWA `25`;
  - generation: Optimus FA4 `65`, native SWA `84`.
- Unique physical keys: context `50/50`, generation `149/149`.
- Canonical queries: `199/199` PASS, `199/199` `source="silicon"`.
- Maximum absolute error: `0.0 ms`.
- Maximum relative error: `0.0%`.
- Measured latency range:
  `0.013210666676362356–50717.3125 ms`.
- Canonical files:
  - context: `50` rows, `8407` bytes, SHA256
    `7e029ce3795ef8125bf8aa6e39f08396bcfe0c90937e5dd0e3cec858f80ff908`;
  - generation: `149` rows, `8772` bytes, SHA256
    `7483acf4ebfa15e86aebb981d8468f56b14b6767323d017ad1fee61ee80c9dd0`.
- Validation evidence:
  - `/data/ycfeng/tmp/step4_attention_canonicalization_preflight_20260815.json`;
  - `/data/ycfeng/tmp/step4_attention_canonical_consumer_validation_20260815.json`.

## 2026-08-15 — Grouped `wo_a` and FP32 router B300 collection closure

**Status:** PASS; both provider families are measured, canonicalized, and
consumer-validated.

### Motivation

Continue the next operation families without retrying DeepEP or applying the
unapproved SWA QKV annotation overlay. Isolate grouped `wo_a` and FP32 router
from the QKV smoke gate while preserving the same pinned source/runtime,
Collector cases, and consumer keys.

### Expectation

- Smoke exactly one grouped and one router case without invoking QKV.
- Full collection schedules exactly `74 + 74 = 148` cases.
- Execute the pinned source expression/custom-op paths.
- Accept only exact runtime/device/provider identities, positive finite
  latency, unique physical keys, and exact silicon consumer matches.
- Canonicalize without deleting or moving source evidence.

### Method

- Added a fail-fast `grouped_router` provider-core slice under TDD.
- Extended the existing validator with an explicit selected-family contract;
  absent QKV data remains absent rather than being replaced by an empty or
  generic table.
- Ran B300 predict-only, a two-case smoke, and the full 148-case collection in
  the pinned image.
- Validated source CSVs and then queried every row again from the canonical
  Parquet database.

### Result

- TDD RED: `2` expected failures for the missing slice; validator RED:
  `1` expected failure for unsupported `--families`.
- Focused GREEN after implementation: `8/8`; shared runner regression:
  `15/15`.
- Smoke: grouped `1/1`, router `1/1`, exact consumers `2/2`.
- Full collection:
  - grouped `74/74`;
  - router `74/74`;
  - Collector errors `0`;
  - total profiling elapsed `22.57 seconds`.
- Full exact consumers: `148/148`, all `source="silicon"`.
- Duplicate physical keys: `0`.
- Maximum absolute/relative error: `0.0 ms` / `0.0%`.
- Measured latency range: `0.009141333401203156–1.251461346944173 ms`.
- Runtime profile:
  - grouped path: `296` timed `torch.einsum` calls;
  - router path: `370` calls through
    `torch.ops.vllm.optimus_matmul_fp32`,
    `step3p5_util.apply_optimus_matmul_fp32`, and
    `torch.ops.OptimusMoe.matmul_fp32`.
- Final exact cleanup: RJob `0`, Replica `0`.
- Canonical files:
  - grouped: `74` rows, `4741` bytes, SHA256
    `82107b57c003d7c996b9673171759b83dff6755efdc86f94f948e37697cb414c`;
  - router: `74` rows, `4734` bytes, SHA256
    `1949fa57e321e661460c9f9a7deb19bbef400040b2f767138287e664eeb52d13`.
- Evidence:
  - `/data/ycfeng/tmp/step4_aic_provider_core_b300_20260815/smoke_s4p-aic-core-gr-0815-2343/`;
  - `/data/ycfeng/tmp/step4_aic_provider_core_b300_20260815/full_s4p-aic-core-grf-0815-2350/`.

## 2026-08-15 — Continuation check after the DeepEP skip decision

**Status:** AIC regression PASS; QKV remains gated on explicit approval.

### Motivation

Resume from the completed grouped/router slice without retrying the deferred
DeepEP family, and verify that the current AIC source remains stable before
advancing to the next provider.

### Expectation

- Do not launch or modify DeepEP.
- Re-run the focused Latest model, operation, backend, and Collector tests.
- Re-run shell syntax and whitespace checks.
- Keep the image-native QKV annotation repair unmodified until the owner
  explicitly approves its narrow runtime-overlay boundary.

### Method

- Used the handbook-recorded
  `/home/i-fengyicheng/miniconda3/envs/aic-step-design` environment after
  confirming that this checkout has no `.venv/bin/pytest` or
  `.venv/bin/ruff`.
- Ran the four focused AIC unit-test modules with `PYTHONPATH=src:.`,
  `MPLBACKEND=Agg`, and disk-backed `TMPDIR`.
- Ran `bash -n` over the Step4-Pro-Latest e2e/performance shell scripts and
  `git diff --check`.
- Audited the QKV failure and proposed overlay boundary without changing
  runtime code.

### Result

- Focused tests: `85 passed`, `0 failed`, `21.15 seconds`.
- Shell syntax: `14/14` scripts passed.
- Whitespace: `git diff --check` passed.
- Broad changed-file Ruff found four pre-existing import-order findings under
  the externally owned whole-model smoke path; no AIC/provider-core source
  finding was reported before Ruff stopped.
- No B300 job, Docker container, DeepEP run, or QKV overlay was started.

## 2026-08-15 — Authorized exact later DeepEP launch fix

**Status:** In progress.

### Motivation

The owner explicitly relaxed the pinned-script restriction and requested that
commit `9bfd9a610e` be applied and tested according to the modules it changes.

### Expectation

- The commit is a direct child of `607d1641ee`.
- It changes only DeepEP/NVSHMEM launch scripts, not model Python/CUDA code.
- The exact shared-host-SHM plus explicit NVSHMEM contract is tested rather
  than inferred from the earlier legacy-launcher probe.
- Predict-only and a minimal 16-rank Buffer probe precede any full-model retry.
- Controller memory remains bounded and worker temporary I/O avoids `/tmp`.

### Method

- Audited commit metadata and its full two-file diff.
- Confirmed the new probe contains explicit `nvshmem.init(...)`, NIC-to-PE
  mapping, `NVSHMEM_BOOTSTRAP_UID_SOCK_IFNAME`, and
  `--share-host-shm=True`.
- Confirmed the current checkout was still at `607d1641ee` before applying the
  authorized commit.
- Reopened the exact DeepEP lane in requirements, plan, harness, notes, issues,
  and the external execution guide.
- The first multi-file documentation patch did not match the current
  `plan.md` text because a newer 65K-provider handoff had already changed that
  section. Re-read the current files and applied narrow, current-state edits.

### Result

- Exact checkout: `9bfd9a610ea4f2890010702ee7a207cf25edf8de`.
- Exact parent: `607d1641ee3fec43653fca510d717725828890c2`.
- Changed-file scope: two shell scripts; model Python/CUDA diff count `0`.
- Static markers: explicit `nvshmem.init`, NIC-to-PE mapping,
  bootstrap socket interface, `deep_ep.Buffer`, and
  `--share-host-shm=True` all present.
- Shell syntax: `2/2` PASS.
- Installed brainctl version:
  `v2.12.0-alpha.4.1328-20260814040139-9da5d7fa9435`.
- RJob shared-host-SHM parse: exit `1`,
  `unknown flag: --share-host-shm`.
- Legacy shared-host-SHM parse: exit `1`,
  `unknown flag: --share-host-shm`.
- Standalone `rjob`: absent; commit-author path absent.
- No predict-only, RJob, Replica, or B300 allocation was created.
- Evidence:
  `/data/ycfeng/tmp/step4_deepep_launcher_surface_20260815/`.

The exact commit is applied and statically valid, but the controller lacks the
launcher API required to submit it. Execution stops here under the blocking
issue rule; dropping the flag or emulating it with an invented mount is
forbidden.

## 2026-08-15 — DeepEP deferred; AIC coverage and simulation continuation

**Status:** In progress.

### Motivation

Honor the owner's explicit instruction that the completed DeepEP attempt is
the last attempt for this phase. Continue the remaining AIC-side work without
substituting H800, generic communication, or synthetic timing data.

### Expectation

- Keep DeepEP at `0/116` accepted B300 rows and do not launch it again.
- Build a fail-fast SILICON coverage validator from the real
  `Step4-Pro-Latest` operation consumers for EP16 and EP32.
- Report missing QKV and DeepEP keys precisely rather than hiding them.
- Implement batch>1 prefill with a scheduler-aware global token budget; do not
  divide `max_num_batched_tokens` by batch.
- Preserve completed Attention `199/199`, Optimus MoE `174/174`, and
  grouped/router `150/150` canonical consumer evidence.

### Method

- Restored the existing task context, requirements, plan, harness, working
  tree, and current AIC runtime APIs.
- Re-read the requirements matrix and confirmed that prefill batch means
  instance active requests while AIC's existing static API accepts a rank-local
  batch.
- Confirmed `BaseBackend.run_static(..., mode="static_ctx")` intentionally
  rejects `batch_size > 1` with a global chunk budget and directs callers to a
  scheduler-aware driver.
- Selected a TDD sequence: exact SILICON coverage validator first, then the
  scheduler-aware prefill driver.

### Result

- No DeepEP process, GPU allocation, or Docker container was started.
- Added the task-local validator and completed its RED→GREEN unit cycle:
  `5 passed`, `0 failed`.
- The first system-scope memory wrapper failed with the documented
  `Interactive authentication required` error. Re-running the same command
  with the handbook-approved `systemd-run --user --scope -p MemoryMax=2G`
  succeeded.
- EP16 smoke coverage:
  - records: `3,642`;
  - exact silicon: `1,666`;
  - analytic/non-silicon: `1,516`;
  - missing: `460`;
  - unexpected errors: `0`;
  - unique missing contracts: `4` — two QKV providers plus DeepEP
    dispatch/combine.
- Full EP16+EP32 coverage:
  - records: `36,420`;
  - exact silicon: `16,048`;
  - analytic/non-silicon: `15,160`;
  - missing: `5,212`;
  - unexpected errors: `0`;
  - unique missing contracts: `9`.
- The validator confirmed the expected QKV and DeepEP file gaps and discovered
  one confirmed formal-E2E data gap: native SWA context attention at
  `query_tokens=65,536`.
- The apparent Optimus MoE `131,072` gap came from applying the global token
  budget as a uniform local budget on every EP rank. It remains a
  scheduler-mapping finding, not an approved collection target.
- Raw evidence:
  `/data/ycfeng/tmp/step4_aic_silicon_coverage_20260815/full_ep16_ep32.json`.

## 2026-08-15 — 65K Attention closure and scheduler-corrected coverage

**Status:** PASS for Attention and coverage classification; formal E2E remains
BLOCKED by QKV and DeepEP data.

### Motivation

Close the real `65,536`-token Attention gap, remove the false global-versus-
local MoE request, and establish the exact missing-contract set before
implementing the MTP-off requirements driver.

### Expectation

- Measure native SWA and Full FA4 with the exact pinned vLLM providers.
- Preserve B300 SM103 and pinned source identity.
- Canonicalize context Attention to `52` unique physical keys.
- Query Attention, Optimus MoE, grouped GEMM, and FP32 router with their actual
  scheduler semantics.
- Keep QKV and DeepEP missing without fallback rows.

### Method

- Added targeted `FULL_CONTEXT_SMOKE_TOKENS` and
  `SWA_CONTEXT_SMOKE_TOKENS` controls while retaining the default `512`.
- Ran separate B300 `65,536`-token native SWA and Full FA4 smokes.
- Appended only the new exact context keys and re-read the canonical Parquet
  through the unchanged AIC consumer.
- Corrected the task-local coverage validator so attention/dense use the
  busiest attention-DP rank while Optimus MoE receives the actual global
  scheduled-token count.
- Re-ran the full EP16+EP32 context/generation coverage matrix.

### Result

- Native SWA `(batch=1, query=65,536, total_context=65,536)`:
  `3.164181391398112 ms`.
- Full FA4 `(batch=1, query=65,536, total_context=65,536)`:
  `196.56292724609375 ms`.
- Both B300 runs: `SOURCE_PROBE=PASS`,
  `pinned_manifest_files_verified=2103`, B300 SM103, final RJob/Replica `0/0`.
- Canonical context Attention:
  - rows: `52`;
  - provider split: `26` Full FA4 + `26` native SWA;
  - exact consumers: `52/52`;
  - maximum absolute query error: `0.0 ms`;
  - SHA256:
    `4ba0473c26a9d418925ebce724798a848edf0c0afcdb88d092440f05a1e90ef2`.
- Complete Attention dataset: `52` context + `149` generation = `201` rows.
- Corrected full coverage:
  - records: `36,420`;
  - exact silicon: `16,660`;
  - analytic/non-silicon: `15,160`;
  - missing: `4,600`;
  - unexpected errors: `0`;
  - missing physical contracts: `6`.
- Exact-silicon family coverage:
  - Attention: `1,560/1,560`;
  - Optimus MoE: `1,520/1,520`;
  - grouped GEMM: `400/400`;
  - FP32 router: `1,520/1,520`.
- Remaining physical gaps:
  - QKV norm/RoPE: `2`;
  - DeepEP dispatch/combine at EP16/EP32: `4`.
- Evidence:
  - `/data/ycfeng/tmp/step4_aic_attention_b300_20260815/smoke_s4p-aic-swa65k-0815-024430/`;
  - `/data/ycfeng/tmp/step4_aic_attention_b300_20260815/smoke_s4p-aic-fa465k-0815-030553/`;
  - `/data/ycfeng/tmp/step4_attention_52_canonical_consumer_validation_20260815.json`;
  - `/data/ycfeng/tmp/step4_aic_silicon_coverage_20260815/full_ep16_ep32_scheduler_corrected_post_attention.json`.

## 2026-08-15 — DeepEP final disposition reconfirmed

**Status:** DEFERRED at `0/116`; no further DeepEP execution is permitted in
this phase.

### Motivation

Apply the owner's instruction to stop after the completed failed DeepEP
attempt and continue the next AIC work rather than repeatedly exercising the
same unavailable launcher/runtime contract.

### Expectation

- Preserve the exact failure evidence and accepted-row count.
- Do not create H800, generic communication, or synthetic substitute rows.
- Continue MTP-off driver, correctness, and requirements-matrix work.

### Method

- Retained the existing DeepEP Collector, consumer, and missing-key contracts.
- Marked its data family as an explicit formal-simulation blocker.
- Did not launch any DeepEP process, Docker container, RJob, Replica, or GPU
  allocation during this continuation.

### Result

- Accepted B300 DeepEP rows remain `0/116`.
- DeepEP missing contracts remain visible as dispatch/combine for EP16 and
  EP32.
- The active task advanced to the scheduler-aware MTP-off requirements driver.

## 2026-08-15 — MTP-off requirements driver and Attention coverage correction

**Status:** Driver GREEN; corrected Attention B300 collection is in progress.

### Motivation

Run the exact MTP-off prefill/decode matrix after DeepEP was frozen, and use
the real scheduler mapping to detect any dataset gap that the earlier
operation-by-operation coverage scan did not exercise.

### Expectation

- Preserve one global `max_num_batched_tokens` budget.
- Map requests to the busiest attention-DP rank.
- Query Optimus MoE with global scheduled tokens.
- Keep formal latency null when QKV or DeepEP data is missing.
- Do not retry DeepEP.
- If the requirements matrix reveals another measurable provider gap, repair
  the Collector population and recollect that provider rather than treating
  the gap as an analytic fallback.

### Method

- Completed the MTP-off driver unit RED→GREEN cycle.
- Ran the smoke matrix and the full `72` prefill plus `84` decode-budget
  matrix under a 2 GiB controller memory scope.
- Inspected every Attention `PerfDataNotAvailableError` and compared the
  scheduler-derived query shapes with the canonical `52` context and `149`
  generation rows.
- Added RED assertions for the missing scheduler shapes, then extended the
  existing Step4 Attention YAML population with:
  - context `(batch=2, query=512, total=512)`;
  - context `(batch=1, query=4096, total=4096)`;
  - context `(batch=1, query=16384, total=16384)`;
  - generation context upper bound `1,048,576`.
- Re-ran focused Collector and launch-contract tests.
- Ran a one-GPU B300 predict-only check before starting the corrected full
  Attention collection.

### Result

- Driver unit tests: `5 passed`, `0 failed`.
- Driver Ruff/format/diff checks: PASS.
- Smoke matrix: `2/2` prefill and `1/1` decode records correctly returned
  `BLOCKED`, exit code `2`.
- Initial full matrix: `72/72` prefill and `84/84` decode records correctly
  returned `BLOCKED`, exit code `2`.
- The matrix found three missing context workload shapes and one missing
  generation upper-bound shape for each Attention provider. This corrected
  the earlier false conclusion that `201` rows covered every requirements
  scheduler query.
- TDD evidence:
  - RED: `2 failed` at context `52 != 58` and generation `149 != 167`;
  - GREEN: focused `2 passed`;
  - focused Collector/Attention contract suite: `35 passed`.
- Corrected planned Attention population:
  - context: `58` rows;
  - generation: `167` rows;
  - total: `225` rows.
- B300 predict-only: exit `0`, `10` candidate B300 nodes.
- Full corrected Attention collection started as
  `s4p-aic-attnfix-0815-0347`.
- The first summary-extraction command placed the here-document redirection
  after its terminator and failed with `SyntaxError`; the corrected command
  moved the redirection to the command line and completed successfully.
- An attempted Ruff invocation included a YAML file, so Ruff parsed YAML as
  Python and reported syntax errors. Re-running Ruff only on Python files
  passed; this was a validation-command error, not a source defect.
- A second driver TDD check found that OOM classification compared total HBM
  against 100% device capacity even though the required vLLM launch uses
  `--gpu-memory-utilization 0.9`.
- OOM utilization RED→GREEN:
  - RED: `1 failed`, because `90.0 GiB` on a `100 GiB` synthetic device was
    not marked OOM;
  - GREEN: `6 passed`, with the threshold corrected to
    `capacity × 0.9`.

## 2026-08-16 — Phase 10 DeepEP proxy design and plan

**Status:** In progress; design and TDD plan recorded, production code not yet
changed.

### Motivation

Apply the owner's choice A so MTP-off prefill/decode simulation can continue
without misrepresenting NCCL timing as real DeepEP measurement.

### Expectation

- Preserve exact DeepEP as the default fail-fast behavior.
- Require an explicit proxy option.
- Use FP8 dispatch and BF16 combine transport widths.
- Keep proxy, exact silicon, analytic/non-silicon, missing, and error results
  separately visible.
- Complete the full requirements matrix without creating fake DeepEP data.

### Method

- Restored the existing task state, requirements Q22, harness gates, scripts,
  and unit tests.
- Confirmed the existing NCCL query API, operation attributes, scale-factor
  behavior, and the 8-GPU maximum measured topology.
- Added the task-level proxy design, implementation plan, and ISSUE-037.
- Recorded the `.venv/bin/python` missing-`pandas` inspection error and the
  verified system-Python read-only inspection environment.
- The first multi-file documentation patch used an inexact plan-tail context
  and was rejected without changing files. It was replaced by narrow,
  file-specific patches with exact context.

### Result

- The implementation boundary is fixed to three task-level performance
  modules plus focused unit tests.
- No AIC production operation, DeepEP loader, Collector registry, measured
  dataset, or simulation result has been changed yet.
- The next action is the mandatory RED test for the proxy contract.

### TDD RED evidence

- Test:
  `tests/unit/performance/test_step4_pro_latest_deepep_proxy.py`.
- Command:
  `/home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python -m pytest -q tests/unit/performance/test_step4_pro_latest_deepep_proxy.py`.
- Result: expected collection failure, exit code `2`, `0` tests collected.
- Expected cause:
  `ModuleNotFoundError: No module named
  'tests.performance.step4_pro_latest.deepep_proxy'`.
- The failure proves the new proxy contract does not exist yet; minimal GREEN
  implementation is now authorized.

### Proxy module GREEN evidence

- Added
  `tests/performance/step4_pro_latest/deepep_proxy.py`.
- The helper is scoped to Step4-Pro-Latest EP16/EP32, validates the original
  DeepEP provider/topology, maps dispatch to `int8` and combine to `half`,
  applies the operation scale factor, and returns explicit `PROXY` metadata.
- Re-ran the same focused command: `4 passed`, `0 failed`, `0.03s`.
- No production AIC operation or persisted performance table was changed.

### Coverage/simulation integration RED evidence

- Added explicit-opt-in, status-separation, and summary-fidelity assertions to
  the existing coverage and MTP-off driver tests.
- Command covered `22` tests.
- Result: expected `4 failed`, `18 passed`, exit code `1`.
- Expected failures:
  - coverage query does not yet accept `deepep_proxy`;
  - coverage summary has no `proxy_records` bucket;
  - scheduled query does not yet accept `deepep_proxy`;
  - scheduled summary still classifies proxy data as
    `PASS_WITH_NON_SILICON`.
- The default exact-DeepEP tests already remain `missing`, confirming the
  RED work has not introduced an automatic fallback.

### Coverage/simulation integration GREEN evidence

- Added explicit `deepep_proxy` parameters and CLI options to the coverage and
  MTP-off requirements drivers.
- Proxy operation records use `status=proxy`,
  `source=proxy_b300_nccl_alltoall`, and `result_fidelity=PROXY`.
- Coverage, scheduler-step, prefill, decode, and matrix summaries now keep
  proxy counts and latency separate from exact silicon and analytic results.
- The default value remains `None`; exact DeepEP missing data still produces
  `BLOCKED`.
- Re-ran all three focused files: `22 passed`, `0 failed`, `0.08s`.

### Static and smoke validation

**Motivation:** Verify the helper against the persisted B300 NCCL table and
prove the CLI remains explicit before starting the full matrix.

**Expectation:** Static checks pass; the proxy smoke has zero missing/error
records; the same exact-path smoke remains `BLOCKED`.

**Method:**

- Ran Ruff check and format verification on six changed Python files.
- Inspected the canonical NCCL Parquet with conda `aic-step-design`.
- Ran one EP16 context/generation coverage smoke with the proxy.
- Ran the MTP-off smoke both with and without the proxy.

**Resolved command errors:**

- Initial format verification reported four files requiring formatting.
  Applied the exact Ruff formatting diff with `apply_patch`; the rerun passed
  with all six files already formatted.
- Initial Parquet inspection used the guessed column `operation`; the actual
  schema uses `op_name` and `nccl_dtype`. The corrected read found the
  expected data.
- Initial direct-script coverage invocation could not import the repository
  `tests` package. The correct existing execution form is
  `python -m tests.performance...`; the module invocation passed without a
  source workaround.

**Result:**

- Ruff check: PASS; Ruff format: `6 files already formatted`.
- NCCL table: `504` total rows, `126` `alltoall` rows, dtypes
  `half/int8`, rank counts `2/4/8`, message elements
  `256–536,870,912`.
- Coverage proxy smoke: `PASS_WITH_PROXY`; `3,642` records,
  `1,822` exact silicon, `1,516` non-silicon, `304` proxy,
  `0` missing, `0` errors; proxy latency sum `50.16590357142832 ms`.
- Requirements proxy smoke: `PASS_WITH_PROXY`; prefill `2/2` and decode
  `1/1` completed with proxy, `0` blocked.
- Proxy smoke examples:
  - prefill 512, batch 1: `213.00583873859085 ms`, peak HBM
    `218.37548828125 GiB`;
  - decode context 2,048 at `33.33 ms` budget: batch 1 TPOT
    `73.96175826756898 ms`, so `B_max=0`.
- Exact-path smoke: expected exit `2`; prefill `2/2` and decode `1/1`
  remained `BLOCKED`, with `0` proxy records.

## 2026-08-16 — Resumed scheduler-derived Attention closure

**Status:** In progress; B300 predict-only and four targeted provider runs are
the active gate.

### Motivation

Resume from ISSUE-038 without repeating completed DeepEP, Collector population,
or runner-contract work.

### Expectation

- Measure the four missing context workloads with both pinned providers,
  producing exactly eight new B300 rows.
- Keep DeepEP frozen at `0/116`; use its approved NCCL proxy only in
  simulation.
- Preserve every prior raw artifact and merge only new physical keys into the
  canonical context table.

### Method

- Re-read the task requirements, plan, progress, issues, B300/Docker
  handbooks, AIC auto-collection skill, Collector operation-development skill,
  and the existing Attention host/worker runners.
- Confirmed the working branch, pinned vLLM commit, exact image, B300 quota
  group/tag, 3 GiB controller memory cap, and the new query-versus-total
  workload controls.
- Confirmed no current GPU/RJob process was inherited from the interrupted
  session.

### Result

- Active workloads remain:
  `(1,4096,8192)`, `(1,16384,32768)`,
  `(1,8160,1048544)`, and `(1,32736,1048544)`.
- Each existing smoke invokes both `optimus_fa4` and
  `vllm_native_sliding_gqa`; four smokes therefore yield the required eight
  rows.
- Serena project activation still reports the already-known missing
  `languages` configuration; shell, pytest, Collector, and B300 execution are
  unaffected.

### Live execution checkpoint

**Motivation:** Gate the targeted collection on current B300 availability and
make the first workload's state visible before waiting for GPU compilation.

**Expectation:** Predict-only returns usable one-GPU B300 candidates; the live
job reaches the pinned source probe before timing either provider.

**Method:** Ran the handbook-aligned one-GPU predict-only command under a
3 GiB controller scope, then launched the existing targeted Attention runner
for `(batch=1, query=4096, total_context=8192)`.

**Result:**

- Predict-only exit code: `0`; candidate B300 nodes: `10`.
- Live RJob: `s4p-aic-attn-c1-0816-1412`.
- Scheduling time: `21 s`.
- GPU: `NVIDIA B300 SXM6 AC`, capability `10.3`, driver `580.159.03`.
- Pinned source probe: `PASS`; manifest files verified: `2103`.
- Runtime collection is still active; no latency row has been accepted yet.
- Evidence root:
  `/data/ycfeng/tmp/step4_attention_closure_20260816/`.

### Targeted workload 1 result

**Motivation:** Accept the first targeted smoke only after checking its exact
physical identity and cleanup.

**Expectation:** Two positive finite rows, one per pinned provider, with no
workload drift.

**Method:** Read the raw Collector CSV, asserted provider/runtime/GPU/workload
fields, checked source-probe markers, and checked final RJob/Replica inventory.

**Result:** PASS for `(batch=1, query=4096, total_context=8192)`.

- `optimus_fa4`: `1.701456069946289 ms`.
- `vllm_native_sliding_gqa`: `0.21289066473642984 ms`.
- Rows accepted: `2/2`; source probe: PASS; SM: `10.3`.
- Final task RJob/Replica inventory: `0/0`.
- Raw context CSV SHA256:
  `9008f1de048ce316e9c9202e38d5aafca6695366427c1736f5e29f80578902e7`.
- The second workload `(1,16384,32768)` is now running.

### Targeted workload 2 result

**Motivation:** Complete the second short-context point before allocating the
long-context jobs.

**Expectation:** Preserve the same two-provider identity and accept exactly
one row per provider.

**Method:** Repeated the raw-row, source, GPU, positive-finite latency, and
resource-cleanup checks used for workload 1.

**Result:** PASS for `(batch=1, query=16384, total_context=32768)`.

- `optimus_fa4`: `27.7849858601888 ms`.
- `vllm_native_sliding_gqa`: `0.7991733551025391 ms`.
- Rows accepted: `2/2`; cumulative new rows: `4/8`.
- Final task RJob/Replica inventory: `0/0`.
- Raw context CSV SHA256:
  `e6cce34dcf7866053fd61d53fd8e8a062e68a82ef2876436cc11d9a3cd1d98ec`.
- The third workload `(1,8160,1048544)` is now running with a 3600-second
  result bound.

### Targeted workload 3 result

**Motivation:** Measure the first long-context scheduler chunk without
relaxing the exact provider key.

**Expectation:** Full FA4 retains the full paged context while native SWA
retains only its legal sliding-window residency; both still use the pinned
vLLM path.

**Method:** Ran the same source/GPU/workload/latency/cleanup acceptance checks
and inspected the measured physical allocation fields.

**Result:** PASS for `(batch=1, query=8160, total_context=1048544)`.

- `optimus_fa4`: `534.923095703125 ms`,
  `8192` physical blocks, `4294967296` allocated KV bytes.
- `vllm_native_sliding_gqa`: `0.4092746575673421 ms`,
  `68` physical blocks, `35651584` allocated KV bytes.
- Rows accepted: `2/2`; cumulative new rows: `6/8`.
- Source probe: PASS; final task RJob/Replica inventory: `0/0`.
- Raw context CSV SHA256:
  `44cae428cd043c14cc938b7e0c6a6f114f04fe766aeb6087566c3fd830f7defd`.
- The final workload `(1,32736,1048544)` is now running.

### Targeted workload 4 first attempt rejected

**Motivation:** Prevent a formally successful but resource-contaminated timing
from entering the B300 canonical dataset.

**Expectation:** A timing run must begin on an effectively idle assigned GPU,
as did the first three accepted workloads.

**Method:** Compared the worker-start and remote pre-collection GPU identity
against the first three runs before accepting the rows.

**Result:** REJECTED for data publication; raw evidence retained.

- RJob execution and source probe both passed, and two rows were emitted.
- The assigned GPU already used `247685/275040 MiB` before source
  reconstruction or Collector execution; free memory was only `26429 MiB`.
- The first three accepted jobs began with only `0–4 MiB` used.
- Rejected latencies were `2092.2350260416665 ms` for Full FA4 and
  `1.5772533416748047 ms` for native SWA.
- No rejected row was merged into the canonical Parquet.
- The RJob and Replica were fully cleaned; a fresh clean-worker measurement
  of the same exact workload is required.

### Targeted workload 4 clean rerun result

**Motivation:** Replace the rejected occupied-GPU evidence with a clean,
source-identical measurement.

**Expectation:** Initial GPU usage returns to the accepted baseline and both
provider rows retain the exact requested workload.

**Method:** Re-ran only `(1,32736,1048544)` after a fresh predict-only PASS,
then repeated every row, source, GPU, and cleanup check.

**Result:** PASS; the eight-row targeted collection is complete.

- Initial GPU usage: `4/275040 MiB`; free: `274110 MiB`.
- `optimus_fa4`: `2098.0398763020835 ms`.
- `vllm_native_sliding_gqa`: `1.5781973203023274 ms`.
- Rows accepted: `2/2`; cumulative new rows: `8/8`.
- Source probe: PASS; final task RJob/Replica inventory: `0/0`.
- Raw context CSV SHA256:
  `55580b01d43b6a6018fc8bd7f818b4ccdb3f61049a04ffe662c15881cc304ff1`.
- Rejected-versus-clean latency difference:
  Full FA4 `-0.27667969164858475%`, native SWA
  `-0.059813726419322144%`. This comparison is diagnostic only; the rejected
  rows remain excluded rather than averaged.

### Canonical Attention merge and exact consumer validation

**Motivation:** Publish only the eight clean, new physical keys while leaving
all prior context and generation measurements unchanged.

**Expectation:** Context grows `58 → 66`, generation remains byte-identical at
`167`, no key is removed or deduplicated, and all `233` canonical rows resolve
as exact silicon.

**Method:**

- Verified all four accepted raw CSV hashes, pinned source markers, B300
  identity, and initial GPU usage.
- Copied the pre-merge canonical Parquet files into the evidence directory.
- Built an isolated candidate with the original `58` context rows followed by
  the eight new rows.
- Checked both physical and consumer key uniqueness before publication.
- Ran the unchanged consumer against the candidate, then copied only the
  verified context Parquet into the canonical path.
- Re-ran all exact queries through `PerfDatabase` reading the canonical
  Parquet files.

**Resolved command/validation errors:**

1. The first candidate attempt rewrote the unchanged generation rows and
   expected byte-identical Parquet. The rows were equal but writer metadata
   changed the file bytes. That candidate was retained as failed evidence; the
   corrected attempt copied the existing generation Parquet byte-for-byte.
2. The first direct canonical query omitted `systems_root`, so
   `PerfDatabase` searched `./systems/b300_sxm.yaml`. The corrected command
   explicitly used `src/aiconfigurator/systems`; no production code changed.

**Result:** PASS.

- Context: `66` rows, provider split `33+33`.
- Generation: `167` rows, unchanged.
- Attention total: `233` rows.
- Merge delta: kept `58`, added `8`, removed `0`, deduplicated `0`.
- Candidate consumer: `233/233` exact silicon, maximum error `0.0 ms`.
- Canonical Parquet consumer: `233/233` exact silicon, maximum error
  `0.0 ms`.
- Context SHA256:
  `60a14948fa513fa361af9a7a9e1bdfddc0d4fe23000334b120bed45647314f43`.
- Generation SHA256 remains:
  `bde836b884b410b21284645ab45f0a35f82d03cac5b91299b256823067cb14c0`.
- `step4_deepep_ht_perf.parquet` remains absent.
- Evidence:
  `/data/ycfeng/tmp/step4_attention_closure_20260816/canonical_merge_20260816_1435/`.

### Post-merge coverage checkpoint

**Motivation:** Confirm the canonical data has no missing exact operation
contract before running the slower scheduler matrix.

**Expectation:** All non-DeepEP provider families remain exact silicon;
explicit DeepEP proxy removes only the documented DeepEP gap.

**Method:** Re-ran the full EP16+EP32 coverage validator with explicit
`b300_nccl_alltoall` proxy under a 2 GiB controller limit.

**Result:** PASS_WITH_PROXY.

- Total records: `36420`.
- Exact silicon: `18220`.
- Analytic/non-silicon: `15160`.
- Proxy: `3040`; proxy latency sum: `2430.509110714305 ms`.
- Missing/error records: `0/0`; missing contracts: `0`.
- Attention: `1560/1560` exact silicon.
- The full `72` prefill plus `84` decode requirements matrix is now running.

### Full proxy matrix checkpoint and exhaustive scheduler closure

**Motivation:** Avoid another first-blocker loop after the post-merge full
matrix exposed a later Attention coordinate.

**Expectation:** Enumerate every unique prefill scheduler Attention coordinate,
query both pinned provider consumers independently, and identify the complete
remaining interpolation boundary before changing Collector cases.

**Method:**

- Read the completed full proxy matrix.
- Derived all unique
  `(local_batch_size, query_tokens, total_context_tokens)` coordinates from
  `build_requirements_workloads()` and `build_prefill_schedule()`.
- Queried one real `ContextAttention` operation per provider for all
  coordinates through the canonical B300 `PerfDatabase` under a 2 GiB
  controller scope.
- Stored every coordinate, origin, query status, and latency/error in one
  read-only evidence JSON.

**Result:**

- Full proxy matrix: prefill `6/72 BLOCKED`; decode `0/84 BLOCKED`.
- The matrix's first visible blocker is
  `(batch=1, query=16384, total_context=49152)` for both providers.
- Unique scheduler coordinates: `197`.
- Consumer queries: `394 = 197 × 2 providers`.
- Successful queries: `366`; missing: `28`; unexpected errors: `0`.
- Both providers have the same `14` missing coordinates: query `16,384`,
  total context `49,152..262,144` in `16,384` increments.
- Evidence:
  `/data/ycfeng/tmp/step4_attention_closure_20260816/scheduler_attention_exhaustive_197_20260816.json`.
- Evidence SHA256:
  `6b95bfb9af77902b1a7837a181fc3d66f5b6b6f4d4f0e55674cdf769e5cf83d0`.
- No Collector YAML, canonical Parquet, or runtime behavior changed during
  this diagnostic.

### Minimum endpoint proof and Collector RED→GREEN

**Motivation:** Close all 14 missing coordinates per provider with the fewest
real measurements instead of adding every missing scheduler point.

**Expectation:** One upper total-context endpoint for each disjoint provider
structure brackets all intermediate query-16,384 coordinates.

**Method:**

- Copied the canonical loaded table in memory only.
- Inserted a structural-only marker endpoint at
  `(batch=1, query=16384, total=262144)` first for Full FA4 and then for native
  SWA; no file was written or treated as performance data.
- Re-queried all `394` provider-coordinate combinations after each insertion.
- Added a focused Collector test requiring exactly the two new invocations,
  verified RED, then added the single shared YAML workload and reran GREEN.

**Result:**

- Canonical missing queries: `28`.
- After one provider endpoint: `14`.
- After both provider endpoints: `0`.
- Unexpected errors across all proof stages: `0`.
- Lower bound: two rows, because the provider structural keys are disjoint.
- Structural-only proof:
  `/data/ycfeng/tmp/step4_attention_closure_20260816/scheduler_attention_minimal_endpoint_proof_20260816.json`.
- Proof SHA256:
  `8968f54b7f5aba21cf2c0681a4d5640916e1d85adc8f3763aaa2d76b41e8b797`.
- Collector RED: expected `1 failed`; getter returned `66` instead of `68`.
- Collector GREEN: `1 passed`; getter now returns `68` unique invocations and
  includes both provider endpoint rows.
- RED/GREEN logs:
  `/data/ycfeng/tmp/step4_attention_closure_20260816/collector_endpoint_red_20260816.log`
  and
  `/data/ycfeng/tmp/step4_attention_closure_20260816/collector_endpoint_green_20260816.log`.

### Minimal endpoint first runtime attempt rejected

**Motivation:** Apply the same clean-GPU acceptance rule used for the earlier
long-context rerun before publishing the two interpolation endpoints.

**Expectation:** The assigned B300 begins near the accepted `0–4 MiB` usage
baseline; source, provider, workload, finite latency, evidence transfer, and
cleanup all pass.

**Method:** Ran both pinned providers at
`(batch=1, query=16384, total_context=262144)` in RJob
`s4p-aic-attnep-0816-1515`, then independently checked the launch and remote
GPU snapshots, raw CSV, source manifest, runtime identity, and cleanup logs.

**Result:** REJECTED for publication; a clean rerun is required.

- Source manifest: `2103/2103`; source probe: PASS; SM: `10.3`.
- Exact rows: `2/2`; latencies were `256.19809977213544 ms` Full FA4 and
  `0.7958026727040609 ms` native SWA.
- Initial GPU usage was `255606/275040 MiB`, with only `18508 MiB` free.
- Final task RJob/Replica inventory: `0/0`.
- Raw context CSV SHA256:
  `8278f943d4a925ed15f590ac10df0b733f856421bdea6b5337cac656ac0a9877`.
- No rejected row was merged into the canonical Parquet.

### Clean endpoint rerun and canonical closure

**Motivation:** Replace the rejected occupied-GPU endpoint and close all
scheduler-derived Attention coordinates without adding synthetic rows.

**Expectation:** Accept exactly two clean pinned-provider rows, grow context
from `66` to `68`, leave generation byte-identical at `167`, and reduce the
exhaustive scheduler missing/error counts to `0/0`.

**Method:**

- Re-ran only `(batch=1, query=16384, total_context=262144)` as
  `s4p-aic-attnep-clean-0816-1519`.
- Independently validated GPU usage, pinned manifest, source path, SM103,
  provider/workload fields, finite latency, evidence transfer, and cleanup.
- Built and queried an isolated 68-row candidate before publishing it.
- Published only the validated context candidate; copied no generation row.
- Re-queried all `235` canonical rows and re-derived all `197` scheduler
  coordinates before issuing both provider queries.

**Result:** PASS.

- Clean GPU before collection: `0 MiB` used, `274114 MiB` free.
- Full FA4: `256.1752115885417 ms`.
- Native SWA: `0.7958613236745199 ms`.
- Source manifest: `2103/2103`; source probe: PASS; SM: `10.3`.
- Final RJob/Replica inventory: `0/0`.
- Merge: kept `66`, added `2`, removed `0`, deduplicated `0`.
- Canonical rows: context `68`, generation `167`, total `235`.
- Exact canonical consumers: `235/235`; maximum error `0.0 ms`.
- Scheduler queries: `394/394`; missing/error `0/0`.
- Coverage proxy: `36,420` records, exact silicon `18,220`,
  analytic/non-silicon `15,160`, proxy `3,040`, missing/error `0/0`.
- Context SHA256:
  `7cf90e1f508e9f18dceb29c51dc90e88caa1f13d3d25d38f4a3435851c79522a`.
- Generation SHA256:
  `bde836b884b410b21284645ab45f0a35f82d03cac5b91299b256823067cb14c0`.
- Exhaustive audit SHA256:
  `4d391ed29952c6ecac7f5fd90a094f27948cb270e287149a39dc2a41f0117937`.
- `step4_deepep_ht_perf.parquet` remains absent.

### Final MTP-off proxy matrix completed

**Motivation:** Complete the owner-approved simulation after closing every
scheduler-derived Attention coordinate with clean B300 measurements.

**Expectation:** All `72` prefill and `84` decode records complete without
missing/error records; every DeepEP-substituted result remains explicitly
marked `PROXY`; no fake DeepEP Parquet is created.

**Method:** Parsed the completed full-matrix artifact, checked its top-level
and per-record fidelity fields, summarized latency/throughput/HBM/OOM and
decode capacity values, and independently checked the canonical DeepEP data
path for absence.

**Result:** PASS_WITH_PROXY.

- Prefill: `72/72` completed, blocked `0`, proxy `72`.
- Decode: `84/84` completed, blocked `0`, proxy `84`.
- Proxy records: `134,976`; summed proxy latency:
  `181471.55741956536 ms`.
- Prefill latency:
  `129.22378441076575–782420.0031436655 ms`.
- Per-replica input throughput:
  `1340.1293369125042–69183.9264811136 token/s`; aggregate:
  `1340.1293369125042–103512.27987036602 token/s`.
- Prefill peak HBM: `142.00439453125–776.97998046875 GiB`;
  OOM `48/72`.
- Decode batch-1 TPOT:
  `56.955545921130614–268.6990437660492 ms`; HBM:
  `138.58154296875–294.77685546875 GiB`.
- Decode capacity: all `84/84` combinations have `B_max=0`,
  `aggregate_B_max=0`, and `first_failed_batch=1`.
- `step4_deepep_ht_perf.parquet` is absent.
- Artifact SHA256:
  `370af7429c35036e30ce24bf5a0e6a57e1e91c834a02c1cb015e83229b9440a8`.

## 2026-08-16 — Phase 10 archive synchronization and local audit

**Status:** Documentation and local changed-symbol review completed; final
lightweight verification remains the next gate.

### Motivation

The archive still described the pre-proxy checkpoint: Attention `225`, total
canonical rows `699`, and all simulation rows `BLOCKED`. Those values no
longer represented the completed Phase 10 artifacts.

### Expectation

- Record Attention `235/235` and canonical total `709`.
- Preserve the exact DeepEP `0/116` gap while recording the explicit
  `PASS_WITH_PROXY` coverage and full matrix.
- Refresh report commands, numeric results, hashes, issue status, plan status,
  and review scope without claiming an independent Phase 10 review.
- Account for every changed production Python symbol and find no orphan
  schema/helper, no no-op deduplication, and no unrelated orchestration change.

### Method

- Compared tracked Python AST symbols against `HEAD` and inventoried all
  task-owned untracked Python, shell, YAML, and JSON files.
- Searched proxy and Step4 provider symbols from producer through Collector
  registry, persisted key, AIC consumer, and focused tests.
- Updated `plan.md`, ISSUE-037, `review.md`, the Attention report, the
  simulation report, and `summary.md`.
- Recomputed hashes for the core implementation, six canonical Parquet files,
  proxy artifacts, reports, and review record.

### Resolved command/edit errors

1. The first multi-file patch used one stale plan context and was rejected
   atomically; no file changed. Narrow file-specific patches then succeeded.
2. A broad `/data/ycfeng/tmp` search emitted permission noise from unrelated
   users' directories. Subsequent evidence lookup used exact task filename
   globs and did not alter any file.
3. The first report edit named the final exhaustive Attention artifact
   generically. The path was checked against disk and corrected to
   `scheduler_attention_exhaustive_197_post_endpoint_20260816.json`.

### Result

- Plan Phase 10 is `Completed`; ISSUE-037 is resolved for proxy simulation
  with exact measurement still deferred.
- Attention report now records context `68`, generation `167`, canonical
  `235/235`, and scheduler queries `394/394`.
- Simulation report now records coverage
  `18,220 silicon + 15,160 analytic + 3,040 proxy`, missing/error `0/0`,
  prefill `72/72`, and decode `84/84`.
- The local review explicitly records that Review B300-15 did not cover Phase
  10; no independent Phase 10 approval is claimed.
- Changed-file/symbol audit completed without an unexplained production
  symbol. Evidence:
  `/data/ycfeng/tmp/step4_phase10_changed_file_symbol_audit_20260816.log`,
  SHA256
  `f8981c06212d23584b451668ad2cbcbf1dee7211c9bee83567e9e9379140346b`.

## 2026-08-16 — Fresh final archive verification

**Status:** PASS; no GPU, Docker, RJob, Replica, collection, or DeepEP
execution occurred.

### Motivation

The final Phase 10 documentation edits occurred after the prior artifact
summary check. A continuation review also found that the same local
`Review B300-16` block had been appended twice.

### Expectation

- Keep exactly one Phase 10 local-review record without changing its scope or
  claiming an independent review.
- Match all `47/47` hashes in the final `summary.md` inventory.
- Preserve the six canonical B300 files and their `709` total rows.
- Reconfirm proxy coverage, the full `72+84` matrix, and the absence of a
  fake DeepEP Parquet.
- Pass the complete focused and Collector test commands plus
  `git diff --check`.

### Method

- Removed only the duplicated review-history row and duplicated
  `Review B300-16` block, then refreshed the review hash in `summary.md`.
- Parsed every inventory path/hash from `summary.md`, recomputed each SHA256,
  checked Parquet metadata row counts, and asserted the native coverage and
  matrix JSON schemas.
- Ran the `334`-test focused Step4-Pro-Latest suite and all
  `tests/unit/collector` tests under separate `MemoryMax=2G` scopes using
  conda environment `aic-step-design`, Python `3.11.15`, and pytest `8.4.2`.
- Ran `git diff --check` and explicitly asserted that
  `step4_deepep_ht_perf.parquet` does not exist.

### Result

- Review archive: exactly `1` `Review B300-16` entry; updated review SHA256
  `7f11b88f283dc606ef10b502c8491b3065ab3410c319d8c1ac24bc5c5c531f57`.
- Summary inventory: `47/47` hashes matched.
- Canonical B300 data: `68 + 167 + 174 + 75 + 75 + 150 = 709` rows.
- Proxy coverage: `36,420` records; exact silicon `18,220`, analytic
  `15,160`, proxy `3,040`, missing/error `0/0`.
- Full matrix: prefill `72/72` with status split
  `24 PASS_WITH_PROXY + 48 OOM`; decode `84/84`, all `B_max=0`; every result
  has `result_fidelity=PROXY`.
- Focused tests: `334/334` passed in `8.08s`.
- Collector tests: `401/401` passed in `31.23s`.
- `git diff --check`: PASS.
- `step4_deepep_ht_perf.parquet`: ABSENT.
- Evidence:
  - `/data/ycfeng/tmp/step4_phase10_final_archive_verification_20260816.log`,
    SHA256
    `3f30d75b66a2ca8da2ebc968ee6e0c51814a3b7d73cb2b8dc51aa09a9ed060e9`;
  - `/data/ycfeng/tmp/step4_phase10_final_focused_fresh_20260816.log`,
    SHA256
    `19f4d8a78c2d93ca48ff81e9c1e855a65a5324753a6ac2a6308b0dea2164fad8`;
  - `/data/ycfeng/tmp/step4_phase10_full_collector_fresh_20260816.log`,
    SHA256
    `6036eb67e39d4c2183958be80f05f99ff632e2c85f3f144d709e67b230c0a1ce`.

## 2026-08-16 — Phase 11 requirement-completeness review opened

**Status:** In progress.

### Motivation

The owner requested a fresh line-by-line review against
`step4pro_v4_external_simulator_requirements.md`, normalized records for
manual review, and publication of the task changes.

### Expectation

Preserve the approved MTP-off and explicit DeepEP `PROXY` scope while closing
all simulator-reporting gaps that can be derived from the existing AIC model.
Metrics that require a live vLLM service must remain explicit `null` values
with reasons rather than being inferred or fabricated.

### Method

- Re-read the complete requirements document and the current simulation
  driver, unit tests, JSON artifact, report, plan, issues, and summary.
- Compared the required workload, latency, throughput, memory, component,
  MoE, repeat, and vLLM-comparison fields against the current JSON schema.
- Inspected the Latest KV allocator methods and the operation-name/provider
  contract used by the 1,821-op context and generation graphs.

### Result

- Already covered: all three required topologies, `24` prefill workloads,
  `7 × 4` decode workload/budget rows, full `72 + 84` matrix, OOM status,
  input/output throughput, HBM categories, and explicit DeepEP proxy fidelity.
- Missing from the artifact: requested/resolved KV dtype, logical and
  resident/peak allocated KV bytes, Full MFA/SWA/Dense/Latent MoE/dispatch/
  combine latency breakdown, per-GPU throughput, explicit unavailable
  TTFT/first-step/ITL/generation/E2E fields, and three-repeat evidence.
- Live-only fields, expert histogram/load/padding, vLLM-vs-simulator error,
  and MTP1 cannot be completed from the AIC-only scope. They will be recorded
  as external or deferred, not synthesized.

## 2026-08-16 — Phase 11 simulation reporting RED→GREEN and three-repeat run

**Status:** Implementation and artifact audit PASS; final scoped regression,
commit, and push remain pending.

### Fix record

**Motivation:** The requirement audit found that the flat manual-review table
did not expose backend fallback, retry, error/missing record counts, exception
logs, or explicit decode active-sequence/batched-token values. It also lacked
the reasons behind runtime-only null values.

**Expectation:** Preserve all existing latency and memory values while making
every required status field auditable. The explicit DeepEP proxy must remain
distinct from automatic backend fallback, with no retry or hidden error.

**Method:**

1. Reproduced the focused RED test: `17 passed, 1 failed`,
   `KeyError: backend_fallback`.
2. Added execution-observability aggregation from operation-query summaries,
   explicit decode active sequences and batched tokens, MTP1-deferred fields,
   and null-value reasons to the review row.
3. The first rerun exposed a stale unit fixture missing
   `memory_unavailable_reasons`; production output already had the field.
   Updated only the fixture to match the real schema.
4. Re-ran the focused module, then executed three independent full matrices
   under separate `MemoryMax=2G` scopes and combined them into one repeat
   audit plus CSV.

**Result:**

- Focused contract: `18/18` passed.
- Each full run: `72` prefill + `84` decode = `156` cases,
  `PASS_WITH_PROXY`.
- Combined audit: `468` case executions, `156/156` fully identical,
  non-identical `0`, and maximum spread `0.0` for every audited metric.
- Manual-review CSV: `156` rows; backend fallback `0`, retries `0`,
  error/missing records `0/0`, exception entries `0`.
- Prefill OOM: `48/72`; decode batch-1 OOM: `16/84`;
  decode `B_max=0` for `84/84`.
- Component accounting maximum absolute error:
  `6.51925802230835e-09 ms`; KV accounting maximum error: `0.0 GiB`.
- DeepEP Parquet remains absent.

### Artifacts

- `mtp_off_requirements_proxy_full_2026-08-16.json`, SHA256
  `b68008b8a86c0a67b90a51fc9b57c301c7561101a9e3613b405e75c6482dd61a`;
- `mtp_off_requirements_proxy_repeat3_2026-08-16.json`, SHA256
  `9d5c296c7c4e95859982cbb986cbda21ef65f9a56d286ebe37cc5a780f7208a1`;
- `mtp_off_requirements_proxy_review_2026-08-16.csv`, SHA256
  `4c1afd34a37877a6cb59cf79c37a326b537261ed9640ee4d657ce527ff49ac62`;
- artifact audit:
  `/data/ycfeng/tmp/step4_phase11_artifact_requirements_audit_20260816.log`.

## 2026-08-16 — Final user-requested publication audit

**Status:** PASS; staging, commit, push, and remote verification remain.

### Motivation

The owner requested another review against the authoritative simulator
requirements, normalized test records for manual review, and one task-scoped
commit pushed to the remote repository.

### Expectation

- Find no missing condition in the approved MTP-off plus explicit DeepEP
  proxy scope, or fix the root cause before publication.
- Reconfirm the `72 + 84` matrix, three-repeat evidence, review CSV, B300
  canonical data, and explicit deferred boundaries.
- Publish only task-owned code, tests, data, and documentation.

### Method

1. Re-read the full requirements document and compared its prefill, decode,
   latency, throughput, KV/HBM, component, MoE, status, and repetition
   requirements with the full JSON and normalized CSV.
2. Recomputed all `52/52` hashes in `summary.md`, checked all six Parquet row
   counts, and asserted that the DeepEP Parquet is absent.
3. Re-ran the complete proxy simulation, focused tests, Collector tests,
   Ruff, shell syntax, structured-input parsing, and `git diff --check`.
4. Corrected two audit-command assumptions after inspecting the real
   artifacts: the CSV uses byte-valued KV column names, and the current
   summary inventory contains `52`, not the earlier Phase-10 count of `47`.
   These were audit-command errors; no production code or artifact changed.

### Result

- Requirement audit: PASS; prefill `72/72`, decode `84/84`, three
  topologies, four TPOT budgets, and explicit `PROXY` fidelity.
- Repetition: `468` executions, `156/156` identical, maximum spread `0.0`.
- Manual review: `156 × 87`; fallback/retry/missing/error/exception counts
  are all `0`.
- Accounting: maximum component error
  `6.51925802230835e-09 ms`; maximum KV error `0.0 GiB`.
- Canonical B300 data: `709` rows; summary inventory `52/52`; DeepEP Parquet
  absent.
- Fresh full simulation: byte-identical SHA256
  `b68008b8a86c0a67b90a51fc9b57c301c7561101a9e3613b405e75c6482dd61a`.
- Focused tests: `343/343` passed in `8.16s`.
- Collector tests: `401/401` passed in `31.29s`.
- Ruff: `45/45`; shell syntax: `14/14`; YAML: `2/2`; JSON: `3/3`;
  `git diff --check`: PASS.
- Audit evidence:
  `/data/ycfeng/tmp/step4_phase11_publication_audit_20260816.log`, SHA256
  `1296ffcc75d5d4ef85f8567c258b247b66dbe2911db88cb2e509e5d33d484f66`.

## 2026-08-17 — Active runtime changed from DeepEP to AgRs/NCCL

**Status:** Configuration and local validation PASS; B300 live execution not
run in this change.

### Motivation

The owner changed the task's runtime requirement because DeepEP cannot run
normally in the current platform/runtime contract. The active whole-model
scripts still hard-coded DeepEP even though DeepEP measurement had already
been stopped and the simulator was using an explicit proxy.

### Expectation

- Select a supported non-DeepEP backend without automatic fallback.
- Keep Step4-Pro model shape, dummy weights, precision, Optimus FP8 MoE,
  DeepGEMM, FA4, topology, RDMA, and memory settings unchanged.
- Fail before launch if the backend is overridden.
- Require an unambiguous runtime manager marker.
- Keep the AIC NCCL `alltoall` proxy visibly separate from the vLLM runtime
  algorithm.

### Method

1. Inspected the pinned vLLM backend enum, Step MoE auto-selection logic,
   CUDA communicator, and `AgRsAll2AllManager`.
2. Confirmed that `nccl` is not a legal backend literal and that
   `allgather_reducescatter` maps to NCCL-backed `all_gatherv` dispatch plus
   `reduce_scatterv` combine.
3. Added focused RED contracts for backend selection, sequence-parallel
   disabling, AgRs log evidence, DeepEP rejection, removal of active NVSHMEM
   settings, removal of the `deep_ep` package-evidence dependency, and
   controller-side fail-fast validation.
4. Updated the active single-node, two-node, and shared remote scripts.
5. Updated the authoritative requirements, task harness/design/notes,
   issue/review records, reusable lessons, and future-work boundary.
6. Ran all local checks with `MemoryMax=2G` and disk-backed logs under
   `/data/ycfeng/tmp/step4_runtime_backend_20260817`.

### Resolved command errors

1. The first RED command used the repository `.venv`, which does not contain
   pytest. It was rerun with the task-recorded
   `/home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python`.
2. The first backend-audit one-liner had nested shell quoting around a Python
   tuple and exited with code `2`. The audit was rerun by passing Python
   directly through `systemd-run`; no code or result changed.
3. The first final backend-audit assertion expected the earlier exact
   `DeepEPHTAll2AllManager` rejection line. The implementation had already
   been strengthened to reject all `DeepEP*All2AllManager` variants with one
   regular expression. The audit was aligned to that current contract and
   passed; no production or test code changed.

### Result

- RED evidence:
  - initial contract: `3 failed, 8 passed`;
  - active dependency cleanup: `2 failed, 9 passed`;
  - controller fail-fast: `1 failed`.
- GREEN focused contracts: `11/11` passed in `0.04s`.
- Active shell syntax: `3/3` scripts passed.
- Static backend audit:
  - active runtime files: `3`;
  - backend: `allgather_reducescatter`;
  - manager: `AgRsAll2AllManager`;
  - dispatch/combine source mapping: `all_gatherv` / `reduce_scatterv`;
  - sequence parallel: `0`;
  - active `deepep_high_throughput` settings: `0`;
  - explicit active NVSHMEM settings: `0`;
  - `deep_ep` package-evidence dependencies: `0`;
  - generic DeepEP manager rejection: enabled;
  - automatic backend selection allowed: `0`.
- `git diff --check`: PASS.
- No GPU was allocated and no vLLM service was launched. Runtime latency,
  throughput, and HBM metrics remain pending live validation.

## 2026-08-17 — Phase 12 B300 predict-only validation

**Status:** PASS; single-node live smoke is the next gate.

### Motivation

The configuration-only replacement must pass the platform admission check
with the exact B300 resource shape before any GPU allocation.

### Expectation

- Use `b300_train_infra` with tag `B300`.
- Preserve the live single-node and two-node CPU, memory, GPU, RDMA, topology,
  image, and distributed-environment settings.
- Return enough candidate nodes for one 1-GPU worker and two 8-GPU workers.
- Create no RJob or Replica during predict-only.

### Method

1. Ran the validated rlaunch/brainctl command-surface check under
   `MemoryMax=2G`.
2. Verified the pinned commit, model config, three payload files, one identity
   pack/index pair, and the prior 16-rank NCCL preflight evidence.
3. Ran exact single-node and two-node `brainctl rjob launch --predict-only`
   commands with the intended live resource flags.
4. Queried only the two exact predict-only names and their exact Replica
   labels to verify no residual resources.

### Result

- brainctl version:
  `v2.12.0-alpha.4.1328-20260814040139-9da5d7fa9435`.
- Local prerequisites: PASS; NCCL rank markers `16`, node markers `2`.
- Single-node predict-only: PASS; candidate nodes `4`, including three with
  `8` free B300 GPUs and one with `7`.
- Two-node predict-only: PASS; candidate nodes `10`, each with `8` free B300
  GPUs.
- Residual resources after predict-only: RJobs `0`, Replicas `0`.
- Evidence root:
  `/data/ycfeng/tmp/step4_runtime_backend_live_20260817`.

## 2026-08-17 — Phase 12 single-B300 live smoke

**Status:** PASS; two-node AgRs live smoke is the next gate.

### Motivation

Verify that the updated active launcher still loads the pinned Step4-Pro
runtime and completes real prefill/decode requests before spending a
two-node allocation.

### Expectation

- One B300 worker reaches Running and is removed afterward.
- The pinned source, dummy model, Optimus FA4, Optimus FP8 MoE, and
  `allgather_reducescatter` setting remain intact.
- Token-ID completion requests and concurrent requests complete.
- No DeepEP or automatic backend-selection marker appears.

### Method

Ran `run_b300_single_smoke.sh` with an outer and inner controller
`MemoryMax=2G`, exact-name resource queries, and disk-backed evidence under
`/data/ycfeng/tmp/step4_runtime_backend_live_20260817`.

### Result

- Host/remote result: `ONE_GPU_HOST_WRAPPER=PASS`,
  `ONE_GPU_SMOKE=PASS`.
- GPU: NVIDIA B300 SXM6 AC, `275040 MiB`.
- Scheduling: `16s`; health readiness: `99s`.
- Model load: `8.819692s`; reported model-load memory: `3.16 GiB`.
- GPU memory: `644 MiB` before load, `247628 MiB` after load.
- First request: `8` prompt plus `4` completion tokens in `13.737685s`.
- Four concurrent requests: wall time `0.474799647s`; individual latency
  `0.464150–0.467964s`.
- Real-batch forward markers: `9`; DeepEP markers `0`; automatic-selection
  markers `0`.
- DP=1 does not instantiate an all2all manager, so an AgRs manager marker is
  not expected in this single-GPU smoke; the two-node gate must provide it.
- Cleanup: RJobs `0`, Replicas `0`; artifact hash verification PASS.
- The log contains `8` caught chat-template warmup warnings because
  `--skip-tokenizer-init` intentionally provides no tokenizer. Token-ID
  completions succeeded, so this pinned-vLLM warning is unrelated to AgRs and
  requires no runtime fallback or patch.

## 2026-08-17 — Phase 12 two-B300 AgRs live smoke

**Status:** BLOCKED; do not retry until the two-node runner lifecycle has an
approved root fix.

### Motivation

Validate the configured AgRs backend across the actual `DP=16`, two-node
runtime where the all2all manager is instantiated.

### Expectation

- Both replicas complete and write `remote_result_ready`.
- Both rank groups remain alive through the distributed smoke.
- The runtime selects `AgRsAll2AllManager`, not DeepEP or automatic backend
  selection.

### Method

1. Launched `s4p-agrs2-0817-163045` with two B300 replicas, `8` GPUs per
   node, full RDMA resources, and controller `MemoryMax=2G`.
2. Ran the existing `remote_b300_single_smoke.sh` independently on each
   replica; rank 0 served requests and rank 1 used `--headless`.
3. Inspected the per-node evidence, the shared runner lifecycle, and bounded
   rank-1 server tail after the missing result marker.
4. Interrupted the blocked host wrapper and verified exact-name RJob and
   Replica cleanup.

### Result

- Scheduling: `16s`; both replicas reached `2/2 Running`.
- Rank 0: `ONE_GPU_SMOKE=PASS`; `AgRsAll2AllManager=1`; DeepEP manager
  markers `0`; automatic backend-selection markers `0`; model loading
  `12.957171s`; GPU after load `257103 MiB`; first request `10.762580s` for
  `8` prompt plus `4` completion tokens; four-request wall time
  `10.90261888s`.
- Rank 1: node rank `1`, data-parallel start rank `8`, headless mode `1`;
  no `remote_result_ready` was produced.
- Root cause: `remote_b300_single_smoke.sh` executes `stop_server` in its
  EXIT handler before writing `remote_result_ready`. The rank-0 API node
  consequently shuts down its TCPStore after finishing the client request;
  the rank-1 headless process still uses that process group and reports
  `TCPStore` `Broken pipe` for ranks `8-15`.
- This proves rank-0 AgRs manager selection and real execution, but it does
  not prove a completed two-node distributed lifecycle. It is not a DeepEP or
  NVSHMEM regression.
- The interrupted wrapper returned `130`. Post-stop exact-name queries found
  RJobs `0` and Replicas `0`. No code was changed and no retry was launched.

## 2026-08-17 — Phase 12 two-node lifecycle root fix

**Status:** LOCALLY PASS; final live rerun is blocked by B300 quota.

### Motivation

The completed rank-0 request path must not independently terminate the shared
distributed vLLM process while the rank-1 headless group is still validating.

### Expectation

The host must wait for a success-only validation marker from both replicas
before it requests shutdown. Each remote process may stop only after that
host request.

### Method

Added a focused source-level contract in
`tests/e2e/step4_pro_latest/test_b300_two_node_smoke_contract.py` before any
runtime-script change.

### Result

- RED command:
  `systemd-run --user --scope -p MemoryMax=2G timeout 120s /home/i-fengyicheng/miniconda3/envs/aic-step-design/bin/python -m pytest -q tests/e2e/step4_pro_latest/test_b300_two_node_smoke_contract.py`
- Actual result: `3 passed, 1 failed` in `0.15s`.
- Expected failure: the host wrapper lacks
  `test -f "${evidence_root}/remote_validation_ready"` and therefore has no
  all-replica validation barrier before teardown.
- No B300 job was launched for RED. The implementation and later evidence are
  recorded below.

## 2026-08-17 — Final review/commit/push continuation

**Status:** IN PROGRESS.

### Motivation

The owner requested that the current task code and documentation be reviewed,
then committed and pushed. Phase 12 still had an uncommitted coordinated
two-node lifecycle implementation and one stale contract assertion.

### Expectation

- Preserve the approved AgRs backend and two-node coordination design.
- Remove only contract/runtime skew discovered by fresh verification.
- Do not stage unrelated caches, historical tasks, H800 data, or the separate
  pinned vLLM checkout.
- Commit and push only after fresh review and all required checks pass.

### Method

1. Verified the branch remains
   `task/step4-pro-latest-b300` at base commit `c422e0ae`.
2. Checked repository writers and file mtimes; no open writer or new task-file
   modification was observed during the stability window.
3. Re-ran the two runtime contract files under conda `aic-step-design`,
   Python `3.11.15`, pytest `8.4.2`, and
   `systemd-run --user --scope -p MemoryMax=2G`.
4. Identified the failing assertion as an obsolete expectation for
   `headless_server_exit_status`, which the approved coordinator intentionally
   removes.
5. Added a RED contract requiring both distributed paths to use
   `REMOTE_VALIDATION_READY_FILE` before `await_coordinated_shutdown`, then
   changed the two runtime touch sites to use that declared path.

### Result

- Initial fresh runtime contracts: `11 passed, 1 failed` in `0.17s`; the only
  failure was the obsolete `headless_server_exit_status` assertion.
- Lifecycle path RED: `2 failed` in `0.21s`, both for the expected missing use
  of `REMOTE_VALIDATION_READY_FILE`.
- Lifecycle path GREEN: `12/12` passed in `0.04s`.
- Active shell syntax: `3/3` passed.
- Ruff check passed; Ruff format identified and normalized one test file.
- Evidence:
  `/data/ycfeng/tmp/step4_phase12_completion_20260817/`.

## 2026-08-17 — Phase 12 coordinator and marker-scope root fixes

**Status:** LOCAL PASS; final corrected payload still needs one two-node live
run.

### Motivation

Fix the premature TCPStore teardown without adding a delay or retry, then
ensure the validation contract matches the pinned vLLM logging scope.

### Expectation

- Both replicas validate before either may stop vLLM.
- Both replicas acknowledge the shutdown arm before final shutdown.
- Final shutdown requests are dispatched concurrently.
- Every replica proves the explicit backend and a real-batch forward.
- The globally scoped AgRs manager line is required at least once across the
  complete job, not once per replica.

### Method

1. Added `remote_validation_ready`, `coordinated_shutdown_armed`,
   `remote_shutdown_armed`, and `coordinated_shutdown` marker paths.
2. Made the host wait for `2/2` validation markers, arm `2/2` replicas, wait
   for `2/2` armed acknowledgements, and send both final shutdown requests in
   parallel.
3. Ran `s4p-agrs2-0817-174506` with the first coordinator revision.
4. Compared that payload with pinned
   `cuda_communicator.py`, where the manager line uses
   `logger.info_once(..., scope="global")`.
5. Changed rank-1 validation to use its local
   `allgather_reducescatter` configuration marker plus real-batch forward,
   while the host requires at least one AgRs manager line across pulled
   replica evidence.

### Result

- Lifecycle ordering RED: `3 passed, 1 failed` in `0.15s`.
- Declared-path RED: `2 failed` in `0.21s`.
- Intermediate lifecycle GREEN: `12/12` in `0.04s`.
- Final focused contracts after the marker-scope correction: `14/14` PASS.
- Active shell syntax: `3/3` PASS.
- Ruff check/format and `git diff --check`: PASS.
- First coordinator live run scheduling: `78s`, replicas `2/2` Running.
- Rank 0 model load: `13.507140s`; health readiness: `229s`; GPU after load:
  `257103 MiB`.
- Rank 0 first request: `12.979565s` for `8` prompt plus `4` completion
  tokens; four-request wall time: `12.497070804s`.
- Rank 0 markers: AgRs `1`, DeepEP `0`, automatic selection `0`.
- Rank 1 did not write validation readiness because the first coordinator
  revision incorrectly required a local copy of the globally scoped manager
  line. The host therefore never armed shutdown.
- Cleanup terminated the still-running remote commands; their exit `137` is
  cleanup-induced and is not evidence of host or GPU OOM.
- No `Broken pipe` was found in this post-coordinator attempt.

## 2026-08-17 — Final corrected two-node rerun blocked by quota

**Status:** BLOCKED before Replica creation; resources cleaned.

### Motivation

Run the corrected coordinator and marker-scope contract exactly once on the
required `2 replicas x 8 B300` shape.

### Expectation

The platform must admit `16` B300 GPUs, create `2/2` replicas, and allow the
full ready/armed/concurrent-shutdown path to execute.

### Method

1. Submitted RJob `s4p-agrs2b-0817-175947`.
2. Observed exact RJob events and exact-label Replica state under
   `MemoryMax=2G` and `timeout 60s`.
3. Interrupted the waiting wrapper after the quota cause was established.
4. Deleted the exact RJob and repeated exact-name/label cleanup queries.

### Result

- RJob creation: PASS at `2026-08-17 17:59:49+08:00`.
- Replica creation: `0/2` for approximately `308s`.
- Platform event at `2026-08-17T10:03:57Z`:
  `Insufficient GPU quota. Request: task "worker" 8 GPU/replica x 2 replicas
  = 16 GPU (B300). Queue remaining: B300=6.`
- Runtime/model/communication metrics: not produced because no Replica was
  created.
- Cleanup artifact counts: exact RJob `0`, exact-label Replica `0`.
- Fresh post-stop queries: exact RJob `0`, exact-label Replica `0`.
- Evidence root:
  `/data/ycfeng/tmp/step4_phase12_barrier_20260817/`.
- Next live action is blocked until the same-shape predict-only check reports
  enough quota for all `16` requested B300 GPUs.

## 2026-08-17 — Phase 12 continuation verification and archive synchronization

**Status:** LOCAL PASS; final corrected two-node live acceptance remains
blocked by external B300 quota.

### Motivation

Resume from the quota interruption without relying on prior-session test
claims, and remove stale archive text that still described the already-fixed
independent teardown lifecycle.

### Expectation

- The current coordinator contracts still pass `14/14`.
- All three active shell entry points still parse.
- The two focused Python contract files remain Ruff-clean and formatted.
- The working-tree diff has no whitespace errors.
- Task records distinguish the completed local root fix from the still-missing
  corrected two-node live result.

### Method

1. Re-read the Phase 12 plan, progress, issues, review, summary, future,
   lessons, requirements, and test report.
2. Ran focused pytest, shell syntax, Ruff check, Ruff format check, and
   `git diff --check` under `MemoryMax=2G`.
3. Stored logs under
   `/data/ycfeng/tmp/step4_phase12_resume_20260817/`.
4. Synchronized the reusable lessons, review record, test report, and summary
   without changing runtime code.

### Result

- Focused contracts: `14/14` passed in `0.04s`.
- Active shell syntax: `3/3` passed.
- Ruff check: `All checks passed`.
- Ruff format: `2 files already formatted`.
- `git diff --check`: exit `0`, findings `0`.
- Runtime code changes in this continuation: `0`.
- Remaining blocker: the platform must admit
  `2 replicas x 8 B300 = 16 B300`; the last measured queue remainder was `6`.

## 2026-08-17 — Phase 12 quota-admission diagnostic

**Status:** BLOCKED; no live RJob was submitted.

### Motivation

Determine whether a fresh same-shape predict-only result can safely replace
the stale `6/16` quota event before another expensive two-node live attempt.

### Expectation

- Predict-only must reveal whether the requested replica count affects
  admission.
- A direct quota query is preferred when current identity permits it.
- No RJob or Replica may be created by the diagnostic.
- The corrected live wrapper must remain unsubmitted unless total available
  B300 quota is proven to be at least `16`.

### Method

1. Read the GPU and brainctl handbooks and rechecked the current
   `brainctl rjob launch` command surface under `MemoryMax=2G`.
2. Attempted a direct read of
   `quotagroups.stepmind.com/b300_train_infra`.
3. Ran the exact two-node per-worker resource shape with `--replica 2`.
4. Ran a no-allocation sensitivity control with `--replica 8`; this would
   require `64` GPUs if replica count were part of the prediction.
5. Queried only the two exact RJob names and exact Replica labels.
6. One documentation search initially placed Markdown backticks inside a
   double-quoted shell regex, so the shell attempted to execute `16`; it was
   rerun with safe single quoting and changed no files.

### Result

- Direct quota read: `Forbidden`; current identity cannot get
  `quotagroups.stepmind.com/b300_train_infra`.
- `--replica 2`: exit `0`, seven candidate nodes, each with `8` B300 GPUs.
- `--replica 8`: exit `0`, the same seven candidate nodes.
- Diagnostic-created resources: RJobs `0`, Replicas `0` for both names.
- Conclusion: predict-only checks per-worker node fit and ignores total
  replica demand; it does not prove `16` GPUs of quota.
- Last trustworthy total-quota evidence remains the `2026-08-17T10:03:57Z`
  event reporting B300 remainder `6`.
- Corrected two-node live submissions in this diagnostic: `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_phase12_resume_20260817/predict_two_node_1828.log`
  and
  `/data/ycfeng/tmp/step4_phase12_resume_20260817/predict_replica8_diagnostic.log`.

## 2026-08-17 — Post-concurrency stability and inventory re-audit

**Status:** LOCAL PASS; final corrected two-node live acceptance remains
blocked by external B300 quota.

### Motivation

Two task-owned files were modified after the prior `summary.md` inventory was
written. Their recorded hashes and the stated inventory cardinality therefore
needed fresh verification before any publication or commit decision.

### Expectation

- The current remote runner and two-node contract remain stable with no open
  writer.
- The post-coordinator runtime contracts still pass `14/14`.
- Shell syntax, Ruff check/format, and whitespace checks still pass.
- The active runtime inventory contains exactly `16` files, and every current
  hash matches the refreshed summary.

### Method

1. Compared SHA256 and mtime across a five-second stability window and checked
   both files with `lsof`.
2. Inspected the current remote-runner and contract diffs against the branch
   base to confirm that the later edits only complete the approved
   `REMOTE_VALIDATION_READY_FILE` path contract and its source-level test.
3. Re-ran the two focused runtime contract files under
   `systemd-run --user --scope -p MemoryMax=2G`.
4. Re-ran syntax checks on the three active shell scripts, Ruff check/format
   on the two focused contract files, and `git diff --check`.
5. Audited the active runtime inventory parsed directly from `summary.md`,
   refreshed the changed document/code hashes, and corrected its count from
   `17` to the actual `16`.

### Result

- Stable two-node contract SHA256:
  `9b749a806fb5f60e78dd7b4a32967bfaaee14d25b39452ba776579f112f166c6`;
  unchanged across the five-second window; open writers `0`.
- Stable remote runner SHA256:
  `7cc139aab85d3db29e53e2b750fd1d302d82d00a84a706368524e041d5d13e15`;
  open writers `0`.
- Focused contracts: `14/14` passed in `0.04s`.
- Two-node contract alone: `6/6` passed in `0.05s`.
- Active shell syntax: `3/3` passed.
- Ruff check: `0` findings; Ruff format: `2/2` already formatted.
- `git diff --check`: `0` findings.
- Initial inventory audit against the stale summary: `14/16` matched and
  `2/16` mismatched; missing files `0`.
- Final refreshed active-runtime inventory: `16/16` matched.
- Runtime code changes made by this continuation: `0`.
- Evidence:
  `/data/ycfeng/tmp/step4_phase12_resume2_20260817/`.

## 2026-08-17 — Phase 13 code/docs publication review

**Status:** READY FOR PRE-COMMIT VERIFICATION.

### Motivation

The owner requested review, commit, and push in that order. The current
working tree contained the completed Phase 12 AgRs runtime slice plus task
records, while the final publication report still described the already-fixed
lifecycle defect as the publication blocker.

### Expectation

- Review the complete current code/doc diff and find no unresolved blocking
  defect.
- Preserve the real remaining limitation: the corrected two-node live run is
  blocked by B300 quota, not by a known current lifecycle defect.
- Include only current task code, authoritative requirements, task records,
  and the three relevant test reports.
- Exclude caches, unrelated historical tasks, H800 data, generated local
  outputs, `.worktrees/`, and `vllm-step4-pro/`.

### Method

1. Reviewed the five current runtime/test files against base commit
   `c422e0ae`, including backend fail-fast checks, per-replica validation,
   shutdown-arm acknowledgement, concurrent release, manager-marker scope,
   and exact cleanup ordering.
2. Reviewed all modified task records and the authoritative requirements
   update for consistency with Q24-Q26.
3. Reclassified
   `test_report_2026-08-14_b300_pinned_vllm_smoke_runtime_trace.md` as
   historical DeepEP/NVSHMEM evidence.
4. Replaced stale publication text with the current quota-blocked state.
5. Used the fresh verification logs under
   `/data/ycfeng/tmp/step4_final_publication_20260817/`.

### Result

- Current code review: blocking findings `0`.
- Focused Step4-Pro-Latest regression: `347/347` passed in `8.21s`.
- Full Collector regression: `401/401` passed in `31.60s`.
- Ruff check/format: `45/45` task Python files passed.
- Shell syntax: `14/14` task scripts passed.
- The first final artifact-audit command failed with
  `KeyError: 'summary'` because it used an obsolete repeat-JSON field path.
  Inspection showed the actual fields under `repeat_audit`; the corrected
  command passed inventory `70/70`, simulation `72+84`, repeat
  `468/156/0.0`, CSV `156 × 87`, canonical rows `709`, and DeepEP-Parquet
  absence.
- The first exact staged-diff check found `7` Markdown trailing-space lines
  across the three newly added reports. They were removed at the source; no
  code, dataset, or result value changed.
- Current live limitation: corrected two-node run remains
  `BLOCKED_BY_QUOTA`; request `16`, last trusted remainder `6`, replicas
  created `0/2`.
- Commit and push are the next ordered actions after the final hash/staged
  diff audit.

## 2026-08-17 — Phase 14 runtime lifecycle and evidence hardening

**Status:** LOCAL PASS; live two-node execution remains
`BLOCKED_BY_QUOTA`.

### Motivation

The active two-node wrapper had already received a lifecycle repair, but the
remaining implementation and task records still needed a single-owner
teardown contract, strict query handling, a non-drifting predict-only probe,
and explicit evidence that a distributed model forward really completed.

### Expectation

- Both replicas remain alive while the host pulls and validates evidence.
- Exactly one host-side RJob deletion performs distributed teardown.
- Cleanup fails if either exact resource query fails or reports the target.
- Predict-only uses the exact live launch arguments and leaves no resources.
- A source-hash-bounded completion marker catches deferred CUDA failures.
- No new live RJob is submitted while current `16`-GPU quota evidence is
  unavailable.

### Method

1. Added `b300_runtime_contract.sh` with quota, cleanup, and runtime-log
   contracts, plus `test_b300_runtime_contract.py`.
2. Made the two-node wrapper reuse one `launch_args` array for predict-only and
   live launch; archived predict-only output/SHA256 and checked exact
   zero-resource state.
3. Added an explicit disk-backed B300 quota evidence contract requiring
   `>=16` GPUs and the expected charged group.
4. Removed the active shutdown-arm/acknowledgement/release marker protocol.
   The host now waits for `remote_validation_ready`, pulls both evidence trees,
   validates them, then deletes the RJob once.
5. Transferred the common contract to each replica and added
   `MODEL_FORWARD_COMPLETE` after a source-hash-verified
   `current_platform.synchronize()`.
6. Added a timeout guard requiring
   `2400 + 2 * 300 + 60 = 3060s`; the default remote/live timeout is `3600s`.

### Result

- TDD RED:
  - two-node lifecycle contract: `4 failed, 2 passed`;
  - runtime-contract transfer: `1 failed`;
  - timeout-margin contract: `1 failed`.
- Final focused runtime contracts: `26/26` passed in `0.28s`.
- Active shell syntax: `6/6` passed.
- Ruff check: `0` findings; Ruff format: `5` files already formatted.
- `git diff --check`: `0` findings.
- Controller scopes remain `MemoryMax=2G`; all logs/evidence remain under
  `/data/ycfeng/tmp`.
- No live RJob was submitted. The last trusted platform event still reports
  `6` B300 GPUs remaining for a `16`-GPU request; direct quota reads remain
  RBAC-forbidden.
- Evidence:
  `/data/ycfeng/tmp/step4_phase14_review_fix_20260817/`.

The synchronized completion marker is diagnostic instrumentation. It does not
change model weights, communication selection, or training graph, but its
extra synchronize can change request timing. Any future timing from this
instrumented smoke must remain labeled runtime-smoke evidence.

## 2026-08-17 — Phase 14 final publication revalidation

**Status:** IN PROGRESS; code/static verification and report normalization
PASS, final inventory/staged-diff audit, commit, and push remain.

### Motivation

Resume from the interrupted Phase 14 finalization without relying only on the
earlier local test claim, remove the stale four-marker lifecycle from the
publication summary, and prepare one task-scoped follow-up commit.

### Expectation

- The five current runtime contract files pass `26/26`.
- All six affected shell files parse.
- All five affected Python test files pass Ruff check and format.
- The publication report identifies the current live-evidence-pull and
  single-delete lifecycle.
- The two-node result remains `BLOCKED_BY_QUOTA`; no live RJob is submitted.

### Method

1. Recovered the existing task state and compared the Phase 14 code/doc diff
   against base commit `0b8d651c42ab2efa825cbcabbf7b078fd88d5b06`.
2. Ran the five runtime contract files, six shell syntax checks, and Ruff
   check/format serially under `MemoryMax=2G`.
3. Stored all fresh logs under
   `/data/ycfeng/tmp/step4_phase14_final_20260817/`.
4. Updated the existing final publication and non-DeepEP runtime reports;
   no duplicate report was created.

### Result

- Runtime contracts: `26/26` passed in `0.21s`.
- Shell syntax: `6/6` passed.
- Ruff check: `0` findings.
- Ruff format: `5/5` files already formatted.
- Publication inventory: `76/76` hashes matched; duplicate, missing, and
  mismatched paths were `0/0/0`.
- `git diff --check`: exit `0`, findings `0`.
- One stale-text `rg` command used Markdown backticks inside a double-quoted
  shell pattern, causing the shell to try to execute `16/16`. It was rerun
  with a single-quoted pattern, returned zero stale matches, and changed no
  files.
- Live B300 submissions: `0`.
- Remaining blocker: independent current evidence for at least `16` B300
  GPUs is unavailable; the last trusted platform event reported `6`.
